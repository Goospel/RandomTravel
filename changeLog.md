# changeLog — 완료 기록 (역순)

> 매 작업 완료 시 **맨 위에** `날짜 · 제목` + 의도·결과 한 항목. 코드 세부는 PR·커밋에 있으니 여기선 **왜/무엇을**만.
> 이 프로젝트 자체 PR 번호는 적지 않는다 — 스쿼시 머지 커밋 제목의 `(#N)`이 단일 출처다(`git log --grep "제목"`으로 찾기). 타 repo 참조는 이미 확정된 번호라 적어도 된다.

## 2026-08-10 · 🎨 여행 티켓 리디자인 (M29) — 다크 제거·틸/주홍·티켓/엽서/스탬프북

- **왜**: OS가 다크인 사용자는 여행 서비스인데 검은 화면을 봤고, indigo+무채색은 정확하지만 중립적이라 랜덤 뽑기·정복 지도라는 제품의 재미가 화면에서 안 읽혔다. claude.ai 디자인 세션에서 받은 핸드오프(zip — 색·간격·대비가 확정값인 hifi 스펙)를 구현 명세로 채택했다.
- **무엇을**: 다크 모드를 제거하고 종이빛 라이트 한 벌로 통일, 액센트를 틸(정보)+주홍(행동) 2축으로. 뽑기 카드는 절취선·노치 있는 **티켓**, 결과는 **엽서**, `/map`은 **스탬프북**으로. 화면 설명문을 대폭 걷어냈다 — 단 정직성 문구(🍃 예측·"실측 아님"·표본 수·"연관이지 인과가 아니에요"·ⓒ한국관광공사)는 전부 유지하고 **`honesty.test.ts`로 잠갔다**(문구를 지우면 테스트가 깨진다 — 돌연변이 7종 전부 사살 실측).
- **리뷰(서브에이전트 교차 검증)에서 잡은 것**: 스펙 파일 목록이 빠뜨린 `CoursePanel`을 편입(같은 🍃 정보가 한 화면에서 두 모양이던 충돌 해소), 카피 축약에서 사라진 🍃 출처를 한적 스트립 메타("공공데이터 · M/D 예측")로 복원, 정복 토스트 대비 여유 확보.
- **스펙과 다르게 한 것 3건**(전부 근거 기록, §7.18): 시안의 정적 "71배"는 실값(68.4배)과 이미 어긋나 있어 미기재 · 지역명 뒤 조사 금지 규칙 준수 캡션 · 날짜는 기존 공용 포맷 재사용.
- **비범위(후속)**: demo-login·privacy 화면 모양 현대화, `/map` 빈 칸 CTA 테두리, `rt.storySeen.v1` 고아 키 정리.

## 2026-08-10 · 🎫 심사용 데모 로그인 — 공모전 제출용 테스트 계정 (§14.1)

- **왜**: 1차 제출물의 "테스트 계정(ID/PW)"을 우리는 낼 수 없었다 — 로그인이 구글·카카오 OAuth뿐이라 **발급할 자체 계정이 없다**. 실제 구글 계정을 넘기는 우회는 심사위원의 낯선 기기에서 본인확인·2단계 인증에 막힐 수 있어(§14.3 게이트 ④) 제출물 자체가 무용지물이 될 위험을 안는다.
- **무엇을**: 심사 전용 Credentials 로그인 하나를 더 두고(앱스토어 데모 계정 관례), 진입점은 **어디서도 링크하지 않는 `/demo-login`** 한 곳으로 뒀다. 데모 계정에는 다녀온 곳 6 · 찜 3 · 거주지(서울 종로구)를 미리 채웠다 — 빈 계정으로 들어오면 정복 지도도 기록 서랍도 비어 있어 심사위원이 볼 게 없다. 9곳을 전부 다른 시·도로 골라 지도가 실제로 채워져 보인다.
- **어떻게 닫는가**: 프로바이더도 폼도 **env 두 개가 모두 있을 때만** 켜진다(기존 구글·카카오 게이트 패턴 그대로). 심사가 끝나면 Vercel env 두 줄을 지우는 것으로 경로가 닫히고, 코드는 손댈 필요가 없다. 검색 노출도 robots noindex로 막았다.
- **함정 하나**: Credentials 로그인은 어댑터의 사용자 생성 경로를 타지 않는다 — 로그인은 되는데 `user` 행이 없어 `/api/places`가 FK로 500이 나고 **동기화만 조용히 죽는다**. authorize 성공 시 멱등 upsert를 한 줄 넣어 그 실패 모드를 없앴다(데모 경로에서만 실행 — 일반 로그인 비용 0).
- **비범위**: 회원가입·비밀번호 재설정·다중 데모 계정. 심사 기간용 단일 계정이라 필요 없고, 만들면 심사 후 지워야 할 표면만 늘어난다.

## 2026-08-07 · 🔗 OG 공유 이미지 — 링크 미리보기 배선

- **왜**: 홈 URL을 카톡·슬랙 등에 붙여넣으면 미리보기 이미지가 없어 링크 한 줄로만 보였다. 💬 M13 카톡 공유는 결과 카드에만 붙어 있어 **URL 자체를 공유하는 경로**는 계속 맨몸이었다.
- **무엇을**: `public/og.png`(1200×630) + `app/layout.tsx` metadata에 `metadataBase`·`openGraph`·`twitter` 추가. 제목·설명은 기존 M18 분산 서사 카피를 그대로 재사용해 문구가 두 곳으로 갈라지지 않게 했다.
- **확인**: 빌드된 홈 프리렌더 HTML에서 `og:image`가 절대 URL(`https://travelanywhere-kr.vercel.app/og.png`)로 펴진 것을 실측 — 크롤러는 상대 경로를 못 읽어 `metadataBase`가 없으면 무성으로 죽는다.

## 2026-07-30 · 🏠 집에서 갈 만한 곳 (M28) — 회원 거주지 기반 거리 필터

- **왜**: 뽑기가 늘 전국이라 "오늘 당장 갈 수 있는 곳"을 원하는 사용자는 결과를 버리게 된다. 거리는 로그인 없이는 물을 수 없는 정보(거주지)라 **회원 전용 혜택**으로 자리도 맞다.
- **무엇을**: 거주 시·군·구 1개를 저장(`user.home_sigungu` · `GET/PUT /api/home`)하고, 조건 패널에서 **가볍게 70km / 당일치기 200km** 밴드를 고르면 그 반경 안 시·군·구에서만 뽑는다. 켜면 지역·테마·날짜·나머지 조건은 잠기고 **🍃 한적만 함께** 켤 수 있다("집에서 갈 만한 + 안 붐빌 곳" = 분산 서사와 같은 방향).
- **어떻게 작게**: 반경 필터는 결국 **시·군·구 집합**이고, 그 집합에서 뽑는 경로(`drawFromCells`)는 🔭 빈 곳·🍃 TOP5가 이미 쓴다 — 집합을 만드는 순수 함수 하나만 새로 썼다. 시·군·구 중심좌표도 **이미 커밋된 지도 조각에서 역투영**해 얻어 새 데이터 파일·생성 스크립트가 없다.
- **정직성 장치 2건**: ① count 라우트에 같은 분기를 둬 배지가 전국 후보 수를 띄우지 않게 했고(단위가 동네 수라 문구도 "N개 동네"로 갈랐다) ② 카피에 **직선거리**임을 명시했다 — 실측상 제주 거주 시 70km 안이 2곳뿐일 만큼 실제 이동과 어긋날 수 있다.
- **비범위**: 🚗 자가용·🚆 대중교통 접근성 — 전국 단위로 신뢰할 데이터가 사실상 없어(무장애 등록분 한정 텍스트뿐) 근거 없는 배지를 만들지 않았다.

## 2026-07-29 · 📋 1차 제출물 원문 전수 확인 — 스토어 조항은 앱 전용·미공지 3건·테스트 계정 게이트

- **왜**: "제출 D-3(9/18)이 빠듯한가"를 판단하려면 제출물별 **리드타임**을 알아야 하는데, plan §14.1은 사무국 메일 요약이라 "세부 가이드·양식 추후 공지" 한 줄로 뭉뚱그려져 있었다. 특히 **"앱은 스토어 등록 기간을 미리 고려하라"**는 경고가 웹에도 걸리는지가 미확인이었다.
- **무엇을**: 노션 공식 공지의 토글을 전수 펼쳐 1차 제출 항목 원문을 확인하고 §14.1에 반영 — ① **스토어 조항은 앱 전용**(웹은 "접속 가능한 웹사이트 URL 제출" 한 줄 = 리드타임 0, 우리는 상시 라이브) ② **접수 시작일("제출기간") 미공지** — 확정된 건 마감 9/21 16:00뿐이라 D-3 제출은 "그날 접수창이 열려 있다"는 전제 위에 있다 ③ **테스트 계정은 ID/PW 전제**인데 우리는 OAuth 전용이라 발급할 자체 계정이 없다. 부수 확인: 데이터 활용은 **OpenAPI 형태만 인정**(Excel·CSV 다운로드 활용은 심사 제외).
- **파생 리스크 1건(§14.3 신설)**: 심사위원은 **제3자 계정**으로 로그인하는데 M10 이후 검증은 전부 본인 계정 기준이었다 — 카카오 앱 "개발 중" 상태·이메일 동의항목 비즈앱 요구·구글 동의화면 "테스트" 게시·2단계 인증, 넷 다 본인 계정으로는 **무성으로 통과**한다. 판별 수단은 다른 계정 e2e 1회뿐이라 **8/17(월) 선행 점검**으로 캘린더 분리(9/18에 발견하면 복구 불가).

## 2026-07-29 · 🧹 잔여 "TourAPI" 표기 sweep — 에러 문구 2건 + GitHub 레포 설명

- **왜**: 공사 지침("TourAPI로 출처 표기 금지", §14.2)을 푸터에만 적용하고 끝냈는데, 같은 단어가 **사용자에게 보이는 에러 문구 2건**과 **GitHub 레포 About 설명("한국관광공사 TourAPI 기반")**에 남아 있었다. 한 곳만 고치고 전수 확인을 안 한 전형적인 누락.
- **무엇을**: `lib/tourapi.ts` 에러 메시지 2건(키 미설정·요청 한도)을 이웃 문구와 같은 "관광 데이터"로 통일 · 레포 설명을 "🎲 랜덤 국내 여행지 추천 웹앱 — 공공 관광 데이터 기반"으로 교정. **코드 주석·plan/README의 활용 API 표는 그대로** — 서비스 화면이 아니고, 활용 API명은 1차 제출물에 정확히 적어야 하는 항목이다.

## 2026-07-29 · 📖 README 전면 개정 — 공모전 서사·활용 API 표·아키텍처 + 오류 5건

- **왜**: README가 공모전 참여를 한 줄도 담고 있지 않았고(서비스명 "어디든"·프로덕션 URL도 없음), "핵심 기능" 목록이 M15(7/6)에서 멈춰 M16~M27이 통째로 빠져 있었다. 특히 지정과제 2번의 답인 🍃·🔭·⚖️·📈가 안 보여, §14.5가 지적한 "대응 기능이 토글 뒤에 숨는다" 문제가 문서에서 그대로 재현되고 있었다.
- **무엇을**: ① 공모전 절 + 오버투어리즘 대응 4종을 **기능 목록보다 위**에 배치 ② **활용 공공데이터 API 표**(3계열 4서비스·오퍼레이션 9종 — 배점 20점 축이자 1차 제출 항목) ③ **실시간 vs 일 1회 배치** 아키텍처 절(252 시군구 백분위 비교라 실시간 불가·요청 경로 0콜·신청서 제출 사실) ④ 기능 목록을 뽑기/기록 2군으로 재편해 M16~M27 보충 ⑤ 문서 3종 내비게이터·검증 명령 추가.
- **오류 5건**: dev 포트 `3000` → **`npm run dev -- -p 3456`**(OAuth 리디렉션·카카오맵 도메인이 3456이라 3000으로 띄우면 로컬 지도·로그인이 죽는다) · env 목록에 `CRON_SECRET`·`AUTH_REDIRECT_PROXY_URL` 누락 보충 · 수익 모델 "MVP 미반영" stale(§12.6 P1 익명 수집은 이미 가동) · 출처 표기를 공사 지침(§14.2)대로 **"ⓒ한국관광공사"**로(기술 설명 맥락의 서비스·오퍼레이션 식별자는 유지) · 문서 링크 5개 존재 확인.

## 2026-07-29 · 📬 로컬서버 저장 신청서 제출 + cron 완치 확정

- **왜**: 공사 FAQ가 로컬 DB 저장에 별도 신청서를 요구(§14.2 개정) — 문의 메일(00:09 발송)에 공사가 당일 08:06 답장으로 HWP 양식을 보내와 즉시 제출까지 완료.
- **무엇을**: 양식 작성(저장 대상 2종만 진하게·기술적 불가 사유 2건 명기·동기화 계획 KST 08시) → 서명 투명 PNG 삽입 → PDF 변환 → 검수 2회(1차에서 신청 사유 누락·URL 부재 발견 후 보완) → tourapi@knto.or.kr 답장 발송. 제출물·기재안은 `공모전_문서/`. **잔여 = 승인 회신 대기(3~5일+)**, 상세 §14.2.
- **덤**: 어제 수리한 혼잡도 cron **완치 확정** — 07-29 06:16 KST 첫 스케줄 발화 성공(252 시군구). 남은 건 08시 이동 후 첫 발화(07-30)가 당일 갱신분을 받는지 1회 확인(§14.3).

## 2026-07-29 · 🔧 공사 답장 지침 반영 — cron KST 08시 이동 + 출처 표기 ⓒ한국관광공사 교정

- **왜**: 한국관광공사가 2026-07-29 답장으로 두 가지 공식 지침을 줬다 — ① 집중률·빅데이터(방문자수)는 **매일 07:30 이후 갱신**되니 동기화는 그 뒤에 할 것 ② 출처는 **"ⓒ한국관광공사"/"ⓒ한국관광콘텐츠랩"**으로 적고 **"TourAPI"로 표기하지 말 것**. 우리 cron은 KST 06시라 갱신 전을 때려 하루 묵은 데이터를 받고 있었고, 푸터는 "한국관광공사 TourAPI"라 지침 위반이었다.
- **무엇을**: `vercel.json` cron `0 21 * * *` → `0 23 * * *`(UTC 23시 = KST 08시), 푸터 앵커 텍스트를 "ⓒ한국관광공사"로 교체(링크·구조 유지). 근거는 라우트 주석·plan §6.7·§14.2에 병기.
- **덤**: §14.2에 답장 절차도 기록 — 신청서 HWP 양식 수령·**PDF 제출**, 내부 검토 3~5일 이상, **사유 부실 시 반려 가능**(배치 필연성을 구체적으로 쓸 것).

## 2026-07-28 · 🔧 프로덕션 혼잡도 cron 18일 침묵 수리 — CRON_SECRET 미등록 (§6.7 후속)

- **왜**: M27 작업 중 `congestion_daily.fetched_at`이 18일 전(7/10)으로 발견 — M17 머지 이후 프로덕션 새벽 배치가 한 번도 성공한 적이 없었다(🍃·🔭·TOP5가 전부 stale 폴백 위험).
- **원인**: `CRON_SECRET`이 **Vercel env에도 `.env.local`에도 존재하지 않았다**. M17 검증 기준 ③("양쪽 등록")이 실제로는 수행되지 않았고, 라우트는 시크릿 미설정이면 무조건 401이라 매일 발화 → 401 거부 반복. 로컬 배치는 dev 모드가 인증을 생략해 정상 동작 — 그래서 로컬 검증만으로는 이 구멍이 안 보였다(검증 기준 ④ "첫 새벽 cron 로그 확인"도 누락됐던 것).
- **진단 경로**: 레포 정상(vercel.json·라우트) → Vercel cron Enabled 확인 → 대시보드 수동 Run 실측 **401** → env 검색 0건 = 확정. Hobby 로그 보존이 1시간이라 과거 실행 이력 추적은 불가 — 수동 Run 재현이 유일한 진단 수단이었다.
- **해결**: 시크릿 생성 → `.env.local` 기록 + 사용자가 Vercel env 등록(Production·Preview) + 재배포 → **Bearer 직접 호출 200**: 252/252 시군구·실패 0·4,536행·방문자 804행·**소요 130s**(60s 초과 완주 = Fluid compute 활성도 함께 실증). fetched_at 갱신 확인.
- **함정 2건**: ① 대시보드 수동 Run은 재배포 완료 직전이면 **구 배포를 때린다**(레이스 — 401이 재현돼 혼선) ② env 등록만으론 부족하고 **재배포가 있어야** 함수가 새 값을 읽는다. 내일(7/29) KST 06시 첫 스케줄 발화 확인이 잔여 체크.

## 2026-07-28 · 🍃 오버투어리즘 정면 노출 — 한적 TOP5 + 시연 딥링크 (§7.16, M27)

- **왜**: 오버투어리즘 대응 기능(🍃·🔭·⚖️)이 전부 기본 OFF 토글 뒤에 있어, 홈만 보면 "이 앱이 그 문제를 다루긴 하나"가 안 보였다. 기능을 늘리는 게 아니라 **이미 있는 것을 보이게** 하는 작업.
- **무엇**: (A) 홈 통합 카드 밖 아래 한적 TOP5 스트립 — 오늘 집중률 예측 하위 5 시·군·구를 칩으로, 탭하면 그 동네에서 원샷 뽑기(`only=` 파라미터). (B) 홈→`/impact` 서사 링크 1줄. (C) 시연 딥링크 `?quiet=1&scatter=1` — 도착하면 토글이 켜진 상태로 시작하고 URL은 지워진다.
- **낡은 예측으로 유인하지 않는다**: 데이터가 없거나 48h 초과 stale이면 스트립은 자리조차 만들지 않는다. 있는 척하는 것보다 없는 게 정직하고, 탭했는데 못 뽑는 죽은 칩(셀 매핑 없는 시·군·구)도 같은 이유로 건너뛰고 다음 순위가 채운다.
- **불변식은 건드리지 않았다**: `only`는 조건 패널과 무관한 별도 진입점이라(📍 주변 뽑기와 같은 결) 미설정 시 쿼리스트링·서버 동작이 완전히 그대로다. 무효·복수 값도 400이 아니라 그냥 일반 뽑기로 흘려보낸다.
- **설계에서 바꾼 2건**: `/impact` 링크를 스트립 **밖**에 뒀다 — 안에 두면 혼잡도가 stale일 때 서사 진입점까지 같이 사라진다. 그리고 딥링크는 조건 모드 전환을 동반한다 — 순수 모드에선 토글을 켜도 쿼리가 비어 아무 효과가 없다.
- **범위 밖 발견**: 작업 시작 시점 혼잡도 데이터의 `fetched_at`이 ~18일 전이었다. M17 최초 적재 이후 프로덕션 배치가 성공한 흔적이 없어, 🍃·🔭가 계속 stale 폴백 중이었을 가능성이 크다. 로컬 배치 1회로 데이터는 복구했으나 원인 규명은 별건.

## 2026-07-28 · 📐 M27 설계 — 🍃 오버투어리즘 정면 노출 (§7.16)

- **왜**: 오버투어리즘 대응 기능(🍃·🔭·⚖️)이 전부 기본 OFF 토글 뒤에 숨어, 심사 화면에서 "대응이 어디 있나?"가 된다(§14.5 자가평가의 적정성·실용성 감점 축). 백로그 2번을 마일스톤으로 승격.
- **무엇**: plan §7.16 신설 — (A) 홈 한적 TOP5 스트립(오늘 예측 상위 5 시·군·구, stale이면 미노출) + **칩 탭 = 그 시·군·구 원샷 뽑기**(`only=` 파라미터, M21 셀 뽑기 소경로 재사용) (B) 홈→`/impact` 링크 1줄 (C) 시연 딥링크 `?quiet=1&scatter=1`(토글 초기 ON + URL 스트립).
- **설계 개정 1건**: §14.5 초안의 "문제 정의 화면"은 새로 만들지 않는다 — M26 `/impact` ①패널이 이미 그 역할이라 중복 화면 대신 홈에서 잇는 링크로 종결. 홈 통합 카드 **안**에는 아무것도 추가하지 않는다(M24 정보 과다 해소 보호) — 스트립은 카드 밖 아래.

## 2026-07-28 · 📈 임팩트 대시보드 + 익명 이벤트 수집 (§7.15, M26)

- **왜**: "랜덤 추천이 실제로 분산을 만드는가"는 말로는 증명이 안 된다. 문제(현실의 방문 편중) → 설계(뽑기 확률) → 실측(실제 제안 분포)을 한 화면에 나란히 놓아야 반론에 숫자로 답할 수 있다. 그리고 ③을 채우려면 서버 수집이 필요해 §12.6 P1 을 함께 열었다 — 심사 시점까지 표본이 쌓이려면 **조기 배포가 곧 표본 크기**다.
- **무엇**: 공개 페이지 `/impact` 3패널 + 익명 수집(`travel_event` · `POST /api/events`) + 수집 안내 `/privacy` + 푸터 링크. 새 외부 API 0 — ①은 M17 이 이미 적재한 방문자 데이터, ②는 M22 의 가중 로직, ③은 §12.5 이벤트 스키마를 그대로 쓴다. 차트 라이브러리도 안 들였다(순수 div 가로 막대, M12 선례).
- **익명을 규율이 아니라 구조로**: 이벤트 테이블에 `users` 외래키를 **두지 않는다**. 코드 규율로 지키는 익명성은 언젠가 새지만, 조인할 컬럼이 없으면 실수로도 못 잇는다. 로그인 사용자도 이 기록만은 계정과 무관하다.
- **②를 시뮬레이션에서 해석적 계산으로**: 가중 선택의 첫 확률은 정확히 `w/Σw`라 1,000회를 돌릴 이유가 없다(표본 노이즈만 생긴다). 시드 고정 스냅샷은 화면 산출물이 아니라 **샘플러 검증 테스트**로 남겼다.
- **정직성 규칙을 화면에 못박음**: ②에 "설계 확률 — 실측 아님"·"시·도 선택 기준"(후보 0 지역은 건너뛰므로 근사), ③에 표본 수 상시 노출과 n<100 "표본 누적 중" 배지, 퍼널에 "연관이지 인과가 아니다". 표본이 적을 때 결론을 사칭하지 않는 것이 이 화면의 존재 이유와 직결된다.
- **전송은 sendBeacon 우선**: 뽑기 직후 지도 앱으로 이탈해도 브라우저가 전송을 마쳐 준다(일반 fetch 는 언로드에서 잘린다). **실패는 전부 조용히 무시** — 로컬 기록은 이미 끝났고, 수집 실패로 앱을 막을 이유가 없다.
- **실측**: 편중 서울↔세종 **71.1배**, 설계 확률 세종 15.7%↔서울 1.9% — 둘 다 §6.9B 에 적힌 값과 일치해 화면이 같은 계산을 보고 있음이 교차 확인됐다. 이벤트 5종 적재·오스키마 400·표본 배지 노출까지 로컬 e2e 통과.
## 2026-07-28 · 📐 M26 설계 — 📈 임팩트 대시보드 (§7.15)

- **왜**: 공모전 배점 자가평가(§14.5)에서 "랜덤 추천이 실제 분산을 만든다는 근거가 없다"가 기획력·적정성의 최대 반론으로 나왔다 — 그 정량 방어 화면을 백로그 1번에서 마일스톤으로 승격.
- **무엇**: plan §7.15 신설 — `/impact` 3패널(① 방문 편중 실측 ② 뽑기 설계 확률 ③ 실사용 분포·반응 퍼널) + **§12.6 P1 승격**(익명 이벤트 서버 수집: `travel_event` 테이블·`POST /api/events`·`/privacy` 고지 — 공모전 제출이 P1 착수 조건 "공유 시작"을 충족). 추가 외부 API 0, 전부 기존 자산(M17 적재분·M22 귀속 로직·§12.5 이벤트 스키마) 재사용.
- **설계 개정 1건**: §14.5 초안의 "시드 고정 1,000회 시뮬레이션을 화면으로 승격"을 **해석적 확률 계산**(w/Σw)으로 바꿈 — 정확값이라 표본 노이즈가 없고, 스냅샷은 샘플러 검증 테스트로 남는다. 정직성 규칙 3건: "설계 확률 — 실측 아님" 라벨, 표본 수 상시 노출(n<100 "누적 중"), 퍼널은 연관이지 인과 아님 주석.

## 2026-07-28 · 🐕 반려동물 동반 + ♿ 무장애 여행지 필터 (§6.11, M25)

- **왜**: 분산 서사를 "누구에게나 같은 주사위"로 넓히려면 반려인·관광 약자가 실제로 쓸 수 있는 축이 필요했다. 둘 다 기존 `TOUR_API_KEY` 하나로 닿고 같은 '대상 한정' 축이라 한 마일스톤으로 묶었다(설계 §6.11).
- **무엇(♿)**: `tourFetch` 가 엔드포인트를 `서비스/오퍼레이션` 형태로도 받게 해 **URL 앞부분만 갈아 끼우면** 기존 뽑기 파이프라인(조합 생성·lDong 시·도 버킷·중복/비여행지 재추첨·후보 수 집계)이 통째로 재사용된다. 인자를 세 겹 헬퍼에 뚫는 대신 이 방식을 고른 덕에 24h count 캐시도 서비스별로 저절로 갈렸다. 편의시설 상세는 개요와 같은 지연 로드 왕복에 얹었다.
- **무엇(🐕)**: 상류 목록이 지역·타입 필터를 받지 않아 전국 전용 소경로다 — 랜덤 인덱스로 1건 뽑고 그 contentId 로 상세를 조인해 카드를 만든다. 이 조인이 어차피 필수라 **개요가 공짜로 따라와** 개요 지연 로드가 유일하게 면제된다.
- **정직성 규칙 두 가지**: ① 대상 축은 동시에 하나만 — 쿼리 빌더·상태·라우트 3겹으로 강제하고, 직접 URL 로 둘이 동시에 오면 무시하지 않고 **400** 으로 거부한다(어느 쪽을 무시해도 "동반 가능/무장애"의 의미가 조용히 바뀐다). ② 🐕 가 적용하지 못한 조건은 감추지 않고 결과에 명시한다.
- **🐕 풀 편중 대응(사용자 결정)**: 상류 목록이 "동반 가능한 **장소**"라 매장·카페·숙소 비중이 크고(첫 표본 12건 중 쇼핑 9·관광지성 0) 기존 비여행지 거부 필터로는 소매 체인이 안 걸러진다. 하드 제외는 빈 결과를 늘리므로 **관광지성(관광지·문화시설·여행코스·레포츠) 우선 소프트 편향**으로 갔다 — 상업 항목이면 다시 뽑되 예산이 끝나면 마지막 후보를 그대로 수용한다(기존 코스 식사 슬롯의 "완벽주의 금지" 전례). 동시에 풀 성격을 카피로 드러냈다("함께 갈 수 있는 명소·카페·숙소·매장", 배지는 "동반 **가능**") — 숨기면 허위 서사가 된다. **실측: 관광지성 0/12 → 3/12, 응답 중앙값 ≈ 3.8s**.

## 2026-07-28 · 📐 M25 설계 — 🐕 반려동물 동반 + ♿ 무장애 필터 (§6.11)

- **왜**: openAPI 확장 후보 검토에서 같은 축(대상 한정)·같은 키로 묶이는 두 필터를 한 마일스톤으로 채택 — 분산 서사가 "반려인·관광 약자에게도 같은 주사위"로 확장된다(공모전 지정과제 서사와 정합).
- **무엇**: plan §6.11 신설 — 🌊 동형 대상 한정 축, 대상 축 3종 상호배타(교집합은 크로스 조인이라 기각), `drawByType` 재사용(서비스 base 파라미터화), 소스 장애 시 soft-skip 불가(조용한 대체 금지), 정적+지연 2단 배지, 구현 전 게이트 7항.
- **스파이크 실측(2차까지)**: 🐕는 별도 서비스가 아니라 **`KorService2` 통합 오퍼레이션 `detailPetTour2`**로 판명 — 활용신청 불필요·즉시 사용 가능(9,972건, §5.5 랜덤 인덱스 동작, 동반 칩 필드 동봉, 단 지역·타입 필터 미지원 → 1단계 전국 전용). ♿는 `KorWithService2` **활용신청을 브라우저로 실제 수행·자동승인 완료**(개발계정 일 1,000회, ~2028-07-28) — 승인 직후 게이트웨이 403 잔존(반영 지연)이라 구현은 해소 확인 후.

## 2026-07-28 · 🧹 죽은 이모지 필드 제거 (M24 후속, §7.14)

- **왜**: Genesis 리스킨이 UI 이모지를 선 아이콘으로 전면 교체하면서 `SEA_CAT3`·`SEASONAL_CALENDAR`의 `emoji` 필드가 렌더되지 않게 됐다. 당시엔 "API 응답 형태라 유지"로 남겼는데, 읽는 곳이 없는 필드는 다음 편집자에게 "어딘가 쓰이겠지"라는 오해만 남긴다. designGuide도 "데이터에 이모지를 박지 않는다"를 규칙으로 못박았다.
- **무엇**: 소비처 0을 재확인한 뒤 상수 데이터 25행·`SeasideBadge`/`SeasonalBadge` 타입·`tourapi` 주입 3곳(제철 배지·제철 맛집 `matched`·바다 배지)·`season.test.ts` 픽스처에서 필드를 걷어냈다. `/api/random` 응답 shape이 줄지만 소비자가 이 앱 자신뿐이라 안전.
- 카톡 공유에 제철 이모지를 쓰는 대안은 기각 — `kakaoShare`는 `Place`만 받아 제철 배지를 모르므로 `ShareContext` 확장 + 호출부 배선이 필요한 새 기능이다(죽은 필드 정리와 별건).

## 2026-07-28 · 🎨 Genesis 리스킨 + 홈 지도·뽑기 통합 (§7.14, M24)

- **왜**: 디자인 핸드오프(`claude-design/Genesis 디자인 시스템 적용.zip`)가 두 가지를 요구했다 — ① emerald/zinc 팔레트를 Genesis(indigo·General Sans/DM Sans·그림자 없는 border-first)로 전면 교체 ② 홈이 여전히 정보 과다라 2열을 **1열 통합 카드**로 접고 지도를 뽑기 룰렛의 본체로 승격.
- **무엇(리스킨)**: 색을 `--g-*` CSS 변수로 옮기고 `@theme inline`으로 Tailwind 유틸리티화 — **다크가 `dark:` 클래스가 아니라 변수 스왑**이 되면서 "라이트만 쓰고 다크가 깨지는" 실패 모드가 구조적으로 사라졌다. 채워진 indigo 위 글자색은 테마마다 달라야 대비가 통과하므로(다크에서 흰 글자 2.98:1 실패) `--g-on-primary` 토큰을 신설했다. 정적 요소의 `shadow-*`는 전부 삭제(떠 있는 팝오버만 예외). 이모지는 선 아이콘 39종으로 교체하고, 데이터에 박힌 이모지(코스 슬롯·탐험가 레벨·재방문 평가)도 아이콘 키/라벨로 치환했다.
- **무엇(통합)**: 홈이 `max-w-[720px]` 한 카드 — 스탯 행 → 지도 → (뽑는 중) 릴 → hairline → 뽑기 영역. 뷰 토글·정복 pill·하단 안내문·릴 1개를 덜어냈고 `MapHero`는 삭제. 뽑기 버튼 카피가 "지도 굴리기"로 바뀌고, `ConquerSvg`가 `phase`/`highlight`를 받아 **뽑는 중엔 조각이 물결처럼 번쩍이고, 결과엔 당첨 조각만 남기고 나머지가 흐려지며 펄스 마커 + 지역 칩**이 뜬다. 이 CSS 연출이 M23의 JS 감속 룰렛(`lib/roulette`)을 대체해 그 모듈은 삭제했다 — 정보는 오히려 늘었다(마커·칩으로 당첨 동네를 계속 가리킨다).
- `/map`은 구조를 그대로 두고 스타일만 교체 + 🔭 빈 곳 CTA를 정복률 히어로 바로 아래로 올렸다(기존엔 최하단이라 스크롤해야 보였다).
- 핸드오프와 의도적으로 다른 2건: 릴 아래 분산 서사 문구를 유지했고(시안엔 없지만 §7.9가 요구하는 카피), 다크 트리거는 `[data-theme]` 대신 `prefers-color-scheme`을 썼다(앱에 테마 토글 UI가 없어 죽은 코드가 된다). 시안 코드의 flick 스태거 공식은 실제로 스태거가 0이 되는 계산이라 본문이 명시한 의도대로 고쳐 넣었다.

## 2026-07-28 · 🔁 뽑기 UPSTREAM_ERROR 서버 1회 재추첨 + TOUR_API_KEY 힌트 조건화 (§6.5)

- **왜**: 상류 호출 1건이 8초 타임아웃을 넘기면 "타임아웃은 같은 URL 재시도 금지" 정책 탓에 뽑기 전체가 즉시 실패해 "관광 데이터가 응답이 없어요"가 노출됐다. 게다가 에러 패널이 `UPSTREAM_ERROR`이기만 하면 키 설정 힌트를 붙여, 단순 지연을 서버 설정 고장처럼 보이게 했다.
- **무엇**: `/api/random` 세 경로(일반·주변·빈 곳)의 뽑기 호출을 기존 `retryOnce`로 감싸 1회 재실행 — 뽑기를 통째로 다시 굴리면 인덱스·버킷이 새로 뽑혀 **다른 URL**을 때리므로 같은 URL 재시도 금지 원칙과 무충돌(실패율 p → p²). 판정은 순수 함수 `isRetryableDrawError`(UPSTREAM_ERROR만 true, EMPTY_POOL·BAD_REQUEST 제외).
- 키 힌트는 이제 서버 메시지에 `TOUR_API_KEY`가 실제로 들어있을 때만 표시한다.

## 2026-07-28 · 🚫 비여행지 거부 필터 (§6.10)

- **왜**: 랜덤 뽑기가 TourAPI 풀을 정제 없이 써서 "청소년교육센터"·"○○시립도서관" 같은 생활·행정 시설이 여행지로 뽑혔다. 실측 ~250건(풀 21,250건의 1.2%).
- **무엇**: 순수부 `lib/placeFilter.ts`(`isDrawablePlace`)로 **cat3 6종 + 제목 키워드** 2겹 거부 — cat3가 빈 채로 오는 항목이 실재해 코드만으로는 못 거른다. 배선은 전 뽑기 경로가 지나는 초크포인트 `tourapi.pickItemFrom` 한 줄(거부 시 재추첨)이라 호출부 수정 0.
- 후보 수 배지(`countCandidates`)의 ~1% 과다 집계는 의도적 미보정 — 근사값 성격이고 보정하려면 전 항목 스캔이 필요하다.

## 2026-07-28 · ECC 아이디어 채굴 → 메타 시스템에 이식 5종 (RandomTravel 코드 변경 없음)

- **왜**: ECC 플러그인(affaan-m/ECC, ⭐234k) 설치 검토 → 기존 시스템(superpowers·자동 메모리·훅 하드가드)과 전면 중복이라 **설치 비추천** 결정. 대신 쓸 만한 발상만 goospel-claude-config repo에 이식.
- **무엇** (config repo PR #25~#29, 전부 머지):
  1. 프로젝트 간 함정 재발 스캔 스크립트 — consolidate 때 전 프로젝트 T-### 요약을 한 표로 모아 승격 후보 탐지 (#25)
  2. hookify 선언형 룰 가드 러너 — 승격 사다리에 warn/block 중간 계단 + 초기 룰 2건 (#26)
  3. `/hookify` 세션 회고 커맨드 — 반복 교정·미기록 함정을 룰/T-###/원칙 초안으로 제안 (#27)
  4. 그 첫 실전 산출물 `warn-multibyte-grep` 룰 — Git Bash grep/sed 멀티바이트 무성 실패 경고 (#28)
  5. 실환경 검증 3종 PASS 중 발견된 `git -C` 회피 경로 수정 (#29)
- 이 changeLog 파일도 이날 신설(글로벌 표준 양식). 상세는 config repo `docs/2026-07-28-*.md` + 자동 메모리 `ecc-plugin-mining`.
