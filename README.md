# 🎲 RandomTravel — 랜덤 국내 여행지 추천

> 버튼 하나로 오늘 떠날 국내 여행지를 무작위로 뽑아주는 가벼운 웹앱.
> "어디 갈지 고민될 때, 운명에 맡겨."

한국관광공사 **TourAPI**의 실제 관광 데이터를 기반으로, **순수 랜덤**과 **조건 랜덤** 두 가지 모드로 국내 여행지를 추천한다.

---

## ✨ 핵심 기능

- 🎰 **순수 랜덤 모드** — 버튼 하나로 전국 어디든 즉흥 추천
- 🎯 **조건 랜덤 모드** — 지역·테마를 고른 뒤, 그 안에서 랜덤
- ☔🎪🌊🦀 **확장 조건** — "지금 비 안 오는 곳" · "축제 진행 중" · "바다 보이는 곳" · "제철 재료 산지" 필터. **조건을 하나도 안 걸면 완전 랜덤**
- 🃏 **결과 카드** — 사진·주소·설명 + 카카오맵 길찾기 + "다시 뽑기"
- ❤️ **찜 / 최근 본 곳** — `localStorage`에 가볍게 저장 (로그인 불필요)
- 🗺️ **내 여행 지도** — "다녀왔어요" 체크한 곳이 지도에 핀으로 누적 + 카카오맵·네이버지도 앱으로 바로 열어 저장
- 🔐 **로그인·동기화(선택)** — **구글·카카오** 로그인 시 찜/방문이 **여러 기기에서 동기화**. 로그인 안 해도 그대로 사용(localStorage 폴백)

## 🛠️ 기술 스택

| 영역 | 사용 |
|---|---|
| 프레임워크 | Next.js (App Router) + TypeScript |
| 스타일 | Tailwind CSS |
| 데이터 | 한국관광공사 TourAPI · 기상청 초단기실황 API (서버 프록시 경유) |
| 지도 | 카카오맵 (링크 + JS SDK 임베드) |
| 인증 | Auth.js v5 (구글·카카오 OAuth · JWT 세션) — 선택적 로그인 |
| 저장 | localStorage(비로그인 폴백) + Neon Postgres(로그인 시 기기 간 동기화) · Drizzle ORM |
| 배포 | Vercel |

## 🚀 시작하기

```bash
npm install

# 환경변수 준비 — 예시 파일을 복사해 서비스키를 채웁니다
cp .env.local.example .env.local
#  (Windows PowerShell) Copy-Item .env.local.example .env.local

npm run dev   # → http://localhost:3000
```

`.env.local`에 채울 값 (자세한 안내는 [`.env.local.example`](.env.local.example)):

```
TOUR_API_KEY=공공데이터포털_일반인증키(Decoding)      # 필수 (M1)
# WEATHER_API_KEY=기상청_단기예보_서비스키             # ☔ M11 (지금 비 안 오는 곳)
# NEXT_PUBLIC_KAKAO_MAP_KEY=카카오_JavaScript_키        # 🗺️ M8부터
# AUTH_SECRET / DATABASE_URL / AUTH_GOOGLE_ID / AUTH_GOOGLE_SECRET / AUTH_KAKAO_ID / AUTH_KAKAO_SECRET  # 🔐 M10 로그인·동기화(구글·카카오)
```

> 서비스키는 [공공데이터포털](https://www.data.go.kr) → "한국관광공사_국문 관광정보 서비스" 활용신청(무료)으로 발급. 날씨 필터용 "기상청_단기예보 조회서비스"도 같은 계정으로 활용신청. **둘 다 서버에서만 쓰이며 브라우저에 노출되지 않는다** (자세한 이유는 설계 문서 참고).

## 💰 수익 모델 (장기 구상)

사용자의 뽑기·반응·방문 행동 데이터를 **익명 집계 통계·트렌드 리포트**로 가공해 관광 업계(지자체·여행사 등)에 판매하는 모델. 개인 단위 데이터 판매가 아닌 통계 상품화이며, MVP에는 반영하지 않는 장기 구상 — 상세는 [plan.md](plan.md) §12.

## 📐 설계 문서

상세 설계·데이터 흐름·랜덤 알고리즘은 **[plan.md](plan.md)** 참고.

작업하며 모르고 물어봐서 배운 기술 개념(면접 대비)은 **[claude-docs/learning-notes.md](claude-docs/learning-notes.md)** 에 누적한다.

## 📋 진행 상황

- [x] 서비스 컨셉 & 설계 확정
- [x] 프로젝트 골격 셋업 (M1)
- [x] TourAPI 프록시 (API Route) (M1)
- [x] 두 모드 + 조건 선택 UI (M2)
- [x] 결과 카드 + 뽑기 애니메이션 (M3)
- [x] 찜 / 최근 / 방문 기록 (M4)
- [x] Vercel 배포 · 함수 리전 서울 고정 (M4·M5)
- [x] 조건 확장 1차 (🌊 바다 · 🦀 제철) (M6)
- [x] 조건 확장 2차 (🎪 축제) (M7)
- [x] 내 여행 지도 + 지도 앱 딥링크 (🗺️) (M8)
- [x] PWA — 폰 홈 화면 설치 (📱) (M9)
- [x] 로그인·동기화 (🔐 구글·카카오 OAuth + Neon) (M10)
- [x] 조건 확장 3차 (☔ 지금 비 안 오는 곳) (M11)
- [x] 전국 정복 지도 (🧩 250개 시·군·구 채우기 + 정복률) (M12)
