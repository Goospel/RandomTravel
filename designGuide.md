# 디자인 가이드 — "어디든" (여행 티켓)

> UI를 만들거나 고칠 때 **이 파일이 단일 출처**다. 여기 없는 색·크기·모양을 즉흥으로 쓰지 않는다.
> 새 패턴이 필요하면 먼저 이 파일에 추가(왜 필요한지 한 줄)하고 나서 코드에 쓴다.
> 현재 규칙은 **여행 티켓 리디자인**(2026-08-10 디자인 핸드오프) 기준 — 그전 M24 Genesis 리스킨(indigo·다크 스왑)을 대체한다.

## 컨셉 한 줄

**종이 위의 여행 티켓** — 뽑기는 **티켓 한 장**(절취선·노치), 결과는 **엽서**, 다녀온 기록은 **스탬프**,
지도는 모눈 위에 얹힌 종이 지도. 배경은 종이빛에 도트가 깔리고, 색은 **바다빛 틸**과 **스탬프 주홍** 둘뿐이다.
장식은 최소로 두되 위계는 border·배경 대비 + **은유**로 만든다.

## 라이트 전용 (다크 없음)

**`@media (prefers-color-scheme: dark)` 변수 스왑을 쓰지 않는다.** 종이빛 한 벌만 유지한다 —
여행 서비스인데 OS가 다크인 사용자에게 검은 화면이 나가던 것을 걷어낸 결정이다.
`dark:` 변형도 만들지 않는다. 색은 컴포넌트에서 **한 번만** 쓴다.

## 색 — 전부 CSS 변수

값을 컴포넌트에 직접 쓰지 않는다. 정의는 [globals.css](app/globals.css)의 `:root`,
Tailwind 유틸리티는 `@theme inline`이 만들어 준다 (`bg-g-surface` · `text-g-text-2` · `border-g-border` …).

액센트는 **2축**이다 — 화면에서 색이 곧 역할이다.

| 축 | 토큰 | 쓰는 곳 |
|---|---|---|
| **틸 = 정보·상태** | `--g-primary` `#0E7C86` | 정복 조각, 활성 상태, 링크, 포커스 링, 로고 마크 |
| 틸 hover | `--g-primary-hover` | 채워진 틸 hover |
| 틸 딥 | `--g-primary-deep` `#0A5C66` | **밝은 잉크를 얹는 색 면**(홈 티켓 스텁·`/map` 히어로) |
| 틸 연한 배경 | `--g-primary-soft` (+`--g-primary-soft-border`) | 아이콘 칩, 후보 수 배지, 완주한 시·도 |
| 틸 텍스트 | `--g-primary-text` | 연한 배경 위 글자 |
| **주홍 = 행동** | `--g-accent` `#C4471E` | 채워진 주 CTA(홈 `지도 굴리기` · `/map` `빈 칸에서 뽑기`) **오직 여기만**. 단 **틴티드 섀도는 홈 CTA 하나뿐**(아래 모양표) |
| 주홍 hover | `--g-accent-hover` | 위 버튼 hover |
| 주홍 밝은 | `--g-accent-bright` `#E4572E` | **텍스트를 얹지 않는 것만** — 지도 결과 마커, 하트 아이콘, 편중 막대 |
| 주홍 연한 | `--g-accent-soft` / `--g-accent-text` | 🔭 빈 곳 배지, `/impact` ① 캡션 |
| 채워진 면 위 잉크 | `--g-on-primary` / `--g-on-accent` | 틸 면 / 주홍 면 위 글자 |

| 역할 | 토큰 | 용도 |
|---|---|---|
| 페이지 배경 | `--g-bg` `#FBF6EC` | `<body>` (도트 패턴 포함) |
| 카드면 | `--g-surface` | 카드·패널 |
| 한 단 눌린 면 | `--g-surface-2` | 세그먼트 트랙, 보조 타일, 썸네일, 비선택 탭, 푸터 |
| 카드 안 인셋 | `--g-surface-3` | 조건 패널, `/impact` 퍼널 타일 |
| 본문 텍스트 | `--g-text` `#16323A` | 제목·본문 (선택된 칩·세그먼트의 **배경**으로도 쓴다) |
| 보조 텍스트 | `--g-text-2` | 설명·메타 (**종이 배경 위**) |
| 캡션·그룹 라벨 | `--g-text-3` | 11–12px 캡션·그룹 라벨 (**흰 면 위**) |
| 카운트 숫자 | `--g-num` `#6E6146` | 탭 카운트·잠긴 칸·빈 상태 아이콘 (웜 그레이) |
| 보더 | `--g-border` | 카드·구분선·hairline |
| 강한 보더 | `--g-border-strong` | 화면 테두리, 헤더 pill, 점선 배너 |
| 링 트랙 | `--g-ring-track` | 진행바·막대 트랙(흰 면 위) |
| 링 채움 | `--g-sun` `#F2B705` | 틸 면 위 정복률 링·레벨 진행바 **전용** |
| 지도 | `--g-map-empty` / `--g-map-stroke` / `--g-sido-stroke` / `--g-dot-alt` | 빈 칸·조각 경계·시·도 외곽·진행 점 교대색 |
| 모눈 | `--g-grid` | 지도를 얹는 면의 24px 격자 (`.bg-g-grid`) |
| **success** | `--g-success-soft` / `--g-success-text` | 🍃 한적 예측, 켜진 추가 조건, 👍 또 갈래 |
| **warning** | `--g-warning-soft` / `--g-warning-text` | notice·에러 패널·😐 그럭저럭 |
| **error** | `--g-error-soft` / `--g-error-text` | 👎 또 안 감 (주홍과 같은 계열값) |

- **새 색 추가 금지** — 위 토큰으로 해결한다. 의미색은 success/warning/error 셋뿐이고, 강조는 **틸(정보)/주홍(행동)** 2축으로만 나뉜다.
- **장식용 연한 그레이는 없다**(옛 `--g-neutral` 폐지). 화면에 있는 글자는 전부 읽는 글자다 — 흐리게 하고 싶으면 크기·굵기로 눌러라.
- 토큰이 아닌 리터럴은 **아래 둘뿐**이고 그 자리 말고는 쓰지 않는다. 새로 만들지 말고 토큰을 먼저 찾아라
  (중립 배지 바탕은 `--g-ring-track` `#F4EFE4` 재사용 — 막대 트랙과 같은 값이라 새 틴트가 아니다).
  1. **연한 틴트 3종** — `#F2FAF9`(🍃 칩·후보 수 배지) · `#F3FAF2`(켜진 추가 조건) · `#FFFDF7`(임팩트 링크 배너).
  2. **알파 오버레이** — 색 면 위에서만: `--g-on-primary` 반투명(틸 면의 링 트랙·레벨 트랙·레벨 pill,
     그리고 색 면 위 버튼 경계 `border-g-on-primary/50`) · `rgba(22,50,58,.86)`(= `--g-text` 반투명 —
     사진 위 지역 칩) · 홈 주 CTA 틴티드 섀도.

### 대비 — 타협 불가 (함정 3개)

종이 배경(`#FBF6EC`·`#FDFAF3`·`#F7F2E7`)은 흰색보다 **어둡지 않다**. 흰 배경 기준으로 잰 값을 그대로 못 쓴다.

1. ⚠️ **밝은 잉크를 얹는 면은 `--g-primary`가 아니라 `--g-primary-deep`.** `--g-primary` 위 `--g-on-primary`는
   4.61:1이라 opacity로 위계를 만들 여유가 없다. `--g-primary-deep`이면 7.2:1이라 `.75`까지 흐려도 통과(4.9:1).
2. ⚠️ **채워진 주홍 위 흰 글자는 `#E4572E`에서 3.46:1로 실패한다.** 잉크를 밝히지 말고 배경을 `--g-accent` `#C4471E`로
   내린다(`--g-on-accent` 4.6:1). `--g-accent-bright`는 **글자가 없는 요소 전용**이다.
3. ⚠️ **11–12px 캡션에 `--g-num` 금지**(읽는 글자 아님). 흰 면 위 캡션은 `--g-text-3`, 종이 위 설명문은 `--g-text-2`.

검증된 짝만 쓴다: `--g-text-2`↔`--g-bg` · `--g-text-3`↔`--g-surface`/`--g-surface-3` · `--g-num`↔`--g-surface-2` ·
`--g-text-3`↔`--g-surface-2`(푸터 · **5.05:1**) · `--g-on-primary`↔`--g-primary-deep` · `--g-on-accent`↔`--g-accent` · `*-soft`↔`*-text`.

**비텍스트 대비도 3:1이다.** 색 면 위에 색 면을 얹으면 경계가 사라질 수 있다 — `--g-accent` 버튼을
`--g-primary-deep` 히어로에 얹으면 면끼리 **1.56:1**이라 버튼 모양이 안 읽힌다. 이럴 땐 그림자가 아니라
**`--g-on-primary/50` 저불투명 테두리**로 경계를 만든다(그 조합 실측 **3.44:1**). 색 면 위 색 면은 항상 실측할 것.

## 타이포

`--g-display` = **General Sans**(숫자·제목) · `--g-body` = **DM Sans**(본문·UI).
Tailwind로는 `font-display` / `font-body`. 배선은 [layout.tsx](app/layout.tsx)의 `next/font`.

> 둘 다 **한글 글리프가 없다** — 실제 한글은 폴백(Apple SD Gothic Neo·Malgun Gothic)이 렌더한다.
> 의도된 동작이다. Pretendard 등 한글 폰트를 추가하지 않는다(사용자 결정).

| 역할 | 스펙 |
|---|---|
| 홈 타이틀 (`어디든`) | display 24 / 1.15 / 700 / `-0.03em` |
| `/map`·`/impact` 타이틀 | display 32 / 1.15 / 700 / `-0.03em` (모바일 24) |
| **결과 엽서 제목** | display **22** / 1.3 / 700 / `-0.03em` (엽서의 주인공) |
| 섹션 제목 | display 15–17 / 1.3–1.35 / 700 / `-0.02em` |
| 큰 숫자(스탯) | display 18–34 / 1 / 700 / `-0.03em` (`/map` 히어로 34) |
| 본문 | body 15 / 1.6–1.7 / 400 |
| 보조 본문 | body 13–14 / 1.5–1.6 / 400 |
| 캡션 | body 11–12 / 1.5–1.6 / 500 |
| **그룹 라벨**(지역·테마·날짜·추가 조건) | body **11 / 700 / `0.1em` uppercase**, `--g-text-3` |
| 주 버튼 | body 17 / 1 / 700 · 보조 버튼 14–15 / 700 |
| 세그먼트·칩 | body 12–13 / 1–1.2 (선택 700 / 비선택 500) |

한 화면에 **폰트 두께는 2종까지**(500·700).

## 모양·간격

| 요소 | 규칙 |
|---|---|
| 카드·섹션 | `rounded-2xl`(16px) + `border border-g-border` + `bg-g-surface` |
| 카드 안 인셋 패널 | `rounded-[14px]` + `bg-g-surface-3` |
| **주 버튼** | `rounded-[20px]` + `[corner-shape:squircle]` · `h-[60px]`(홈 CTA) |
| 보조 버튼 | `rounded-2xl` + `[corner-shape:squircle]` · `h-[46px]` · 테두리 `1.5px` |
| 액션 타일·추가 조건 토글 | `rounded-[14px]`~`rounded-2xl` + `[corner-shape:squircle]` |
| 칩·세그먼트·pill·링 | `rounded-full` |
| 아이콘 단독 버튼 | 정사각 + `rounded-full` (42px) |
| 파일 탭(기록 서랍) | `rounded-t-[10px]`, 선택 탭만 흰 배경 + 테두리(아래 없음) |
| **그림자** | **정적 요소에는 없다.** 예외는 **홈 주 CTA(`지도 굴리기`) 하나**뿐: `0 10px 22px -12px rgba(154,56,24,.85), 0 1px 2px rgba(22,50,58,.08)` (버튼 색을 머금은 틴티드 섀도). **종이면 위에 뜬 버튼에만 의미가 있다** — 색 면(틸 히어로) 위에서는 섀도가 보이지 않으니 복사하지 말고 위 「비텍스트 대비」의 테두리를 쓴다(`/map` `빈 칸에서 뽑기` 전례). 떠 있는 것(팝오버)은 `shadow-lg`. |
| 간격 | 4px 배수. 카드 패딩 `16–20`, **카드 사이 `14`**, 인라인 `6–10` |

**`[corner-shape:squircle]`은 점진적 향상이다.** 미지원 브라우저는 지정된 `border-radius`의 평범한
라운드 렉트로 폴백한다 — 별도 처리 불필요. 초타원(애플의 continuous corner curvature) 모서리이며,
`radius`가 높이의 절반이어도 캡슐이 되지 않는 것이 육안 확인 포인트다.

## 패턴 (일러스트 자산 없음)

이미지 파일을 추가하지 않는다. globals.css의 CSS 그라디언트 3종만 쓴다.

- **종이 도트** — `<body>`에 상시. `radial-gradient(var(--g-border) 1px, transparent 1px)` / 16px.
- **모눈** — `.bg-g-grid`. 지도를 얹는 면(홈 티켓 지도면·`/map` 스탬프 지도)에만. 24px 격자.
- **사선** — `.bg-g-hatch`. 결과 엽서의 사진 없는 자리.

## 아이콘 — 이모지 금지

UI에 이모지를 넣지 않는다. [components/icons.tsx](components/icons.tsx)의 `<Icon name>`을 쓴다
(24 viewBox · stroke **1.7px** · round cap/join · `fill="none"`).
(이모지는 사용자 요청으로 해금됐지만 이 리디자인에서도 쓰지 않았다 — 선 아이콘 일관성이 더 나은 결과였다.)

크기: 인라인 12–14 · 타일/스텝 15–17 · 헤딩 옆 13(22px 칩 안) · CTA 안 20 ·
빈 상태는 **자리 크기를 따른다** — 기록 서랍 26(56px 점선 원 안) · `/map` 핀 뷰 36(빈 지도 면) · 결과 엽서 사진 폴백 40.
아이콘은 전부 `aria-hidden` — 의미는 옆 텍스트나 `aria-label`이 전달한다.

- 새 아이콘이 필요하면 `ICONS`에 **키를 추가**하고 그 키로 참조한다.
- **데이터에 이모지를 박지 않는다** — `lib/*`의 상수는 `icon: IconName` 키를 갖고 렌더가 조회한다
  (`COURSE_SLOTS`·`lib/level.ts` TIERS 전례). 서버 notice 문구도 이모지 대신 조건 이름을 쓴다.
- 예외로 남긴 기호: `↓`(코스 다리 거리) · `≈`(후보 수 근사) · `✓`(SVG 조각 툴팁).
- **카톡 공유 텍스트**(`lib/kakaoShare.ts`)는 UI가 아니라 메시지 본문이라 이모지를 그대로 쓴다.

## 모션

정의는 전부 [globals.css](app/globals.css)에 — 새 keyframe도 거기에 추가한다.

- `animate-card-reveal`(0.4s) 결과 카드·칩 등장 · `animate-fade-up`(0.32s) 토스트 · `animate-slot-spin`(0.6s) 슬롯 릴
- `animate-map-flick`(0.9s infinite) 뽑는 중 지도 조각 번쩍임(delay 스태거) · `animate-pin-pulse`(1.8s infinite) 결과 마커 펄스
- SVG 안에서 `transform` 애니메이션을 쓰면 **`transform-box: fill-box; transform-origin: center` 필수**(안 주면 뷰박스 원점 기준으로 튄다).
- 지속시간 0.3~0.4s, `ease-out` 기준. 0.5s 넘는 장식 모션은 무한 반복 연출(flick·pulse)만.
- **`prefers-reduced-motion: reduce`에서 전부 꺼진다** — globals.css의 `* { animation: none !important }`가 담당한다.
- hover: 아웃라인 버튼만 `hover:-translate-y-px` 200ms. 채워진 주 CTA는 **배경색만** 바뀐다(그림자 변화 없음).

## 접근성 (타협 불가)

- 키보드 포커스: 전역 `:focus-visible`이 `--g-primary` 아웃라인을 그린다 — `outline-none`을 쓰려면 대체 표시 필수.
  (주홍 CTA 위에서도 틸 아웃라인이 충분히 구분된다.)
- 아이콘 단독 버튼엔 `aria-label`.
- 연한 배경 위 텍스트는 위 「대비」의 검증된 짝만 쓴다. 임의 조합으로 대비를 깨뜨리지 않는다.
- 새 색 조합을 도입하면 **대비를 실측**한다(브라우저 계산 스타일로 측정 — 이 환경은 스크린샷이 자주 멈춘다).

## 문구 톤

- 한국어, 해요체 짧은 문장. 서사 키워드: "전국 어디든 같은 출발선", 스탬프·도장·빈 칸, 그리고 **굴리기**(지도가 룰렛이다).
- **화면이 이미 보여주는 것을 문장으로 반복하지 않는다.** 설명문 대신 버튼·레이아웃·범례로 사용법이 읽히게 한다
  — 라벨 하나로 되면 라벨만 남긴다.
- 시점 중립: 로딩·서사 문구에 '오늘' 류 특정 시점 표현을 넣지 않는다(§7.9 원칙 5).
- ⚠️ **줄여도 지우면 안 되는 문구 5종**: 🍃 표기의 `예측`+기준일 · `/impact` ②의 `설계 확률 — 실측 아님` ·
  ③의 표본 수 상시 노출(+`표본 누적 중`) · ③ 퍼널의 `연관이지 인과가 아니에요` · 푸터의 `ⓒ한국관광공사` 출처 표기.
- ⚠️ **데이터에서 오는 값(지역명·배수)을 정적 문구에 박지 않는다.** 이름은 받침 유무로 조사가 갈리고
  (`제주과`), 배수는 공공 데이터가 갱신되면 어긋난다 — 계산 결과를 렌더하거나, 아예 숫자를 말하지 않는다.

## 작업 방식

1. 화면 단위 리디자인은 **정적 HTML 시안 → 승인 → 구현** 순서(코드 먼저 고치지 않기).
2. 새 컴포넌트는 기존 컴포넌트([ResultCard.tsx](components/ResultCard.tsx), [FilterPanel.tsx](components/FilterPanel.tsx))의 클래스 관례를 복사해서 시작한다.
3. 이 가이드와 코드가 어긋나는 걸 발견하면 — 코드를 가이드에 맞추거나, 가이드를 고친다. 방치 금지.
