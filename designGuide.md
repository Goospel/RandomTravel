# 디자인 가이드 — "어디든" (탐험 로그 / 1C)

> UI를 만들거나 고칠 때 **이 파일이 단일 출처**다. 여기 없는 색·크기·모양을 즉흥으로 쓰지 않는다.
> 새 패턴이 필요하면 먼저 이 파일에 추가(왜 필요한지 한 줄)하고 나서 코드에 쓴다.
> 현재 규칙은 M16 리디자인(탐험 로그 컨셉) 이후 코드베이스에서 실제 쓰이는 관례를 명문화한 것.

## 컨셉 한 줄

**탐험 로그** — 전국을 한 조각씩 채워가는 담백한 기록장. 화려함보다 여백·위계·일관성.

## 색

| 역할 | 라이트 | 다크 | 용도 |
|---|---|---|---|
| **액센트** | `emerald-600` | `emerald-400~500` | 주 버튼, 활성 토글, 정복 조각, 포커스 링 |
| 액센트 연한 배경 | `emerald-50` (+`border-emerald-200`) | `emerald-950` (+`border-emerald-900`) | 활성 칩, 🍃 배지, 강조 패널 |
| 액센트 텍스트 | `emerald-700` | `emerald-300` | 연한 배경 위 글자 |
| **중립 배경** | `white` / `zinc-50` | `zinc-900` / `zinc-950` | 페이지·카드·인풋 |
| 중립 보더 | `zinc-200` | `zinc-800` | 카드·구분선 |
| 본문 텍스트 | `zinc-900` | `zinc-100` | 제목·본문 |
| 보조 텍스트 | `zinc-500~600` | `zinc-400` | 설명·메타 정보 |
| **긍정/방문** | `emerald-*` | 〃 | 다녀옴, 👍또갈래 |
| **주의/축제** | `amber-50/700` | `amber-950/300` | 🎪 축제 배지, 😐그럭저럭, 코스 식사 스텝 |
| **찜/부정** | `rose-50/600` | `rose-950/300` | ❤️ 찜, 👎또안감 |

- 새 유채색 팔레트 추가 금지 — 위 4계열(emerald·zinc·amber·rose)로 해결한다.
- 라이트/다크는 **항상 짝으로** 지정한다(`bg-emerald-50 dark:bg-emerald-950` 식). 한쪽만 쓰면 다크에서 깨진다.

## 타이포

Geist Sans(`--font-geist-sans`). 스케일은 이 5단만:

| 단계 | 클래스 | 용도 |
|---|---|---|
| 캡션 | `text-xs` | 배지, 메타, 힌트 |
| 본문·버튼 | `text-sm` | 대부분의 UI 텍스트 |
| 강조 본문 | `text-base` | 리스트 항목 제목 |
| 카드 제목 | `text-lg font-bold` | 결과 카드 장소명, 패널 제목 |
| 히어로 숫자 | `text-3xl font-bold` | 정복 수, 큰 통계 |

중간 크기(`text-xl`, `text-2xl`)는 히어로 부제 등 예외에만 — 남발하면 위계가 죽는다.

## 모양·간격

| 요소 | 규칙 |
|---|---|
| 카드·패널 | `rounded-2xl border`(중립 보더) + 배경. 그림자 대신 보더가 기본 |
| 카드 내부 블록 | `rounded-xl` |
| 칩·필·버튼(작은 것) | `rounded-full` |
| 작은 인풋·목록 행 | `rounded-lg` |
| 그림자 | 기본 없음. 떠 있는 것(팝오버·토스트·플로팅 버튼)만 `shadow-lg`, 살짝 뜨는 토글 등 `shadow-sm` |
| 간격 | Tailwind 기본 스케일. 카드 패딩 `p-4`~`p-5`, 섹션 간 `gap-4`/`space-y-4` 기준 |

## 모션

정의는 전부 [globals.css](app/globals.css)에 — 새 keyframe도 거기에 추가한다.

- `animate-card-reveal`(0.4s) 결과 카드 등장 · `animate-fade-up`(0.32s) 토스트/라벨 · `animate-pop-in`(0.3s) 핀 강조 · `animate-slot-spin` 슬롯 릴
- 지속시간 0.3~0.4s, `ease-out` 기준. 0.5s 넘는 장식 모션 금지.
- **모든 애니메이션은 `prefers-reduced-motion: reduce`에서 꺼져야 한다** (globals.css 블록에 클래스 추가).

## 접근성 (타협 불가)

- 키보드 포커스: 전역 `:focus-visible` emerald 아웃라인이 이미 있음 — 커스텀으로 `outline-none`을 쓰려면 대체 포커스 표시 필수.
- 아이콘·이모지 단독 버튼엔 `aria-label`.
- 연한 배경 위 텍스트는 위 표의 검증된 짝(50↔700, 950↔300)만 사용 — 임의 조합으로 대비 깨뜨리지 않는다.

## 문구 톤

- 한국어, 해요체 짧은 문장. 서사 키워드: "전국 어디든 같은 출발선", 정복·탐험·조각.
- 이모지는 기능 표지로만(🍃 한적, 🎪 축제, 📍 주변, 🧭 코스, ❤️ 찜) — 장식용 남발 금지.

## 작업 방식

1. 화면 단위 리디자인은 **정적 HTML 시안 → 승인 → 구현** 순서(코드 먼저 고치지 않기).
2. 새 컴포넌트는 기존 컴포넌트([ResultCard.tsx](components/ResultCard.tsx), [FilterPanel.tsx](components/FilterPanel.tsx))의 클래스 관례를 복사해서 시작한다.
3. 이 가이드와 코드가 어긋나는 걸 발견하면 — 코드를 가이드에 맞추거나, 가이드를 고친다. 방치 금지.
