# RandomTravel — 설계 문서

> 최종 갱신: 2026-07-28

---

## 1. 개요

랜덤으로 국내 여행지를 추천하는 **반응형 웹앱**.

- **목적**: 가볍게, 본인이 직접 쓰는 재미 프로젝트. 과한 인프라·운영 부담 없음.
- **핵심 가치**: "어디 갈지 고민하는 시간" 자체를 없앤다. 결정은 주사위에 맡긴다.
- **부수 가치(분산)**: 시·도 균등 랜덤(§5.5)은 유명 관광지 쏠림을 깨는 **분산 장치**이기도 하다 — 이 서사를 UI 문구로 드러내는 원칙·카피는 §7.9(M18).
- **설계 원칙**: DB 없음 · 로그인 없음 · 별도 백엔드 서버 없음. 최소 구성으로 실제 동작하는 것.
  - 이 원칙은 **P0(MVP) 기준** — 데이터 수익화 단계(P1+)에선 이벤트용 **DB만 추가**된다. **로그인은 그때도 불필요**(통계는 익명 세션 ID로 충분, §12.5).

## 2. 핵심 경험 — 조건은 전부 선택사항, 비면 완전 랜덤

> **핵심 불변식**: 모든 뽑기는 같은 파이프라인을 탄다 — `지역 풀 → 필터 적용 → 지역·타입 확정 → 랜덤 추출(§5.5)`.
> **조건(필터)을 하나도 넣지 않으면 필터 0개 = 완전 랜덤.** 조건은 어디까지나 풀을 좁히는 장치다.
> **기준일(📅, §6.8)은 조건이 아니라 판정 시점 입력** — 날짜 단독 선택은 완전 랜덤과 동일하다(풀을 좁히지 않는다).
> **'완전 랜덤'의 균등 정의 = (타입×17시·도) 조합 균등 + 항목 균등**(§5.5 — M22 승격). ⚖️ 분산 모드(§6.9)는 **기본 OFF**라 이 불변식 무손상 — 켠 경우만 시·도 확률이 기운다.

### 2.1 두 가지 모드 (UI 진입점)

| 모드 | 설명 | 흐름 |
|---|---|---|
| 🎰 **순수 랜덤** | 조건 0개로 즉시 뽑기. "운명에 맡긴다" 컨셉. | 메인의 큰 버튼 → 바로 결과 |
| 🎯 **조건 랜덤** | 아래 조건을 걸고, 그 범위 안에서 랜덤 | 조건 선택 → 뽑기 → 결과 |

두 모드는 메인 화면 상단 토글로 전환. 조건 모드에서 아무 조건도 켜지 않고 뽑으면 순수 랜덤과 완전히 동일하게 동작한다(같은 파이프라인이므로 자동 보장).

### 2.2 조건(필터) 체계

| 분류 | 조건 | 동작 | 데이터 소스 |
|---|---|---|---|
| 정적 | 지역 (17개 시·도, 다중) | 지역 풀을 선택 지역으로 한정 | 상수 |
| 정적 | 테마 (관광 타입 8종, 다중) | 뽑을 관광 타입 한정 | 상수 |
| **동적** ☔ | **비 안 오는 곳만** | 지금 강수 없는 지역만 풀에 남김 | 기상청 초단기실황 API |
| **동적** 🎪 | **축제 진행 중인 곳만** | 오늘 진행 중 축제가 있는 지역만 남김 | TourAPI `searchFestival` |
| 정적 🌊 | **바다 보이는 곳만** | 뽑는 대상을 바다 관광지(해수욕장·섬 등)로 한정 | TourAPI 분류코드 상수 |
| **동적** 🦀 | **제철 재료 산지만** | 이번 달 제철 품목의 주산지 지역만 풀에 남김 | 자체 제철 달력 테이블 |
| **동적·프리컴퓨트** 🍃 | **한적한 곳만 (예측)** | 성수기 예측 시·도를 풀에서 제거 — 일 1회 배치 적재분 DB 조회 | 한국관광공사 관광지 집중률 예측 API (§6.7) |
| 정적 🐕 | **반려동물 동반만** (M25) | 뽑는 대상을 동반 가능 카탈로그로 교체 — 1단계 전국 전용(지역·테마 잠금) | TourAPI `KorService2/detailPetTour2` (§6.11) |
| 정적 ♿ | **무장애 여행지만** (M25) | 뽑는 대상을 무장애 카탈로그로 교체 | TourAPI `KorWithService2` (§6.11) |

- 걸린 조건들은 **AND**로 결합. 예: "강원+제주 중 · 비 안 오는 곳 · 맛집" 가능.
- 단 **대상 한정 축(🌊·🐕·♿)은 동시 1개만** — 교집합은 카탈로그 크로스 조인이 필요해 1단계 기각(§6.11 상호배타).
- **📅 방문 시점(§6.8)은 이 표의 조건이 아니다** — 걸린 동적 조건들의 판정 기준일만 바꾼다(날짜 단독 = 완전 랜덤).
- 동적 조건 상세 설계는 §6.

## 3. 화면 구성

| 화면 | 구성 요소 |
|---|---|
| **메인** | 모드 토글(순수/조건) · 큰 "🎲 뽑기" 버튼 · (조건 모드 시) 조건 패널 — **좌 지도 / 우 뽑기·결과 2열**(§7.13). 🔭 빈 곳에서 뽑기는 홈에서 제거, `/map` 전용(§7.13B) |
| **조건 패널** | 지역 선택(17개 시·도 그리드) · 테마 선택(칩 8종) · 다중 선택 허용 · **추가 조건 토글 7종**(☔ 비 안 오는 곳 / 🎪 축제 중 / 🌊 바다 / 🦀 제철 산지 / 🍃 한적한 곳 / 🐕 반려동물 동반 / ♿ 무장애 — 대상 축 🌊·🐕·♿는 동시 1개, §6.11) · **📅 방문 시점 칩**(오늘/내일/이번·다음 주말(토) — 기준일 선택, §6.8) · **⚖️ 분산 모드 토글**(기본 OFF — 방문자 적은 시·도 가중, §6.9) |
| **뽑는 중** | 룰렛/슬롯 머신 애니메이션 (재미 요소, 기대감 연출) |
| **결과 카드** | 대표 사진 · 이름 · 주소 · 한 줄 설명 · 지도 앱 열기·길찾기(§7.2) · "다시 뽑기" · 찜 ♥ · 다녀왔어요 ✔ · (🎪·🦀 필터 시) 진행 중 축제·제철 재료 배지 · 🕒 반나절 코스 만들기(결과 아래 타임라인 패널 `CoursePanel`, §7.10) |
| **기록(선택)** | 찜한 곳 · 최근 본 곳 · 다녀온 곳 목록 (localStorage) |
| **내 여행 지도** | 다녀온 곳을 카카오맵 임베드에 마커로 누적 표시 (§7.1) · 정복 모드 하단 🔭 빈 곳에서 뽑기 CTA(§7.11) |

> 모바일 우선(mobile-first)으로 디자인하되, PC에서도 자연스럽게 보이도록 반응형.

## 4. 데이터 흐름

```
[사용자] → [프론트엔드(Next.js)] → [API Route(서버)] ─→ [TourAPI]     관광지·축제
                                    ↑ 서비스키 숨김   └→ [기상청 API]  실시간 강수 (☔ 필터)
         ← ─ ─ ─ ─ ─  랜덤 여행지 1곳 (사진·주소·설명)  ─ ─ ─ ─ ←
```

### 왜 API Route(서버 중간층)가 필요한가 — 설계의 핵심

1. **서비스키 보호**: 브라우저에서 TourAPI를 직접 부르면 서비스키가 네트워크 탭·소스에 그대로 노출된다. 서버에서만 호출하면 키가 환경변수에 숨는다.
2. **CORS 회피**: 공공 API는 브라우저의 교차 출처 요청을 막는 경우가 많다. 서버-투-서버 호출은 CORS 제약이 없다.
3. **동적 필터 집계**: 여러 지역의 날씨를 병렬 조회·캐싱해 하나의 지역 풀로 합치는 로직(§6)은 서버에 있어야 깔끔하다.

→ Next.js의 **API Route(`app/api/random/route.ts`)** 하나로 이 중간층을 해결. 별도 백엔드 불필요.

## 5. TourAPI 활용

> 한국관광공사 국문 관광정보 서비스 — **KorService2 채택 확정**(2026-07 리서치·구현 검증).
> 베이스 `https://apis.data.go.kr/B551011/KorService2/` · **모든 오퍼레이션명에 접미사 `2` 필수**.

### 5.1 사용 엔드포인트 (KorService2)

| 엔드포인트 | 용도 |
|---|---|
| `areaBasedList2` | 지역·타입 기반 목록 조회 — **랜덤 추출의 메인** |
| `detailCommon2` | 개요(overview)·상세 보강 — **`contentId`만 넘긴다**(v2는 구버전 YN 플래그 제거, 넣으면 오류) |
| `detailImage2` | 추가 이미지 (선택) |
| `areaCode2` / `categoryCode2` | 지역·분류 코드 (초기 1회 또는 상수로 고정) |
| `searchFestival2` | 날짜 조건 축제 조회 — 🎪 **축제 필터의 소스** (오늘 진행 중 축제·지역 도출) |
| `locationBasedList2` | 좌표 반경 목록 조회 — 📍 주변에서 뽑기(§7.6) · 🧭 반나절 코스(§7.10) |
| `areaCode2` | 시·도별 시군구 코드표 — 🔭 셀 매핑 생성기 소스(§7.11, 개발 시 1회 17콜) |

### 5.2 공통 파라미터 & 응답 처리 (검증 확정)

- `serviceKey` (필수) · `_type=json` · `MobileOS=ETC` · `MobileApp=RandomTravel`
- `numOfRows` · `pageNo` (페이지네이션) · `arrange` (정렬, `O`=대표이미지 있는 것 우선) — M1은 arrange 미지정(균등성은 인덱스 난수화에서 나옴)
- **서비스키**: 반드시 **Decoding 키**를 `URLSearchParams.set`으로 넘긴다(정확히 1회 인코딩 → 이중 인코딩 `%252B`로 인한 키 미등록 오류 회피). Encoding 키·문자열 직결합 금지.
- **성공 판정**: HTTP 200이어도 실패일 수 있음 → 응답 `response.header.resultCode === "0000"`으로 판정. 인증·쿼터 오류(코드 30·22)는 게이트웨이가 **XML 봉투**로 주므로 JSON 파싱 실패 분기에서 잡아 안내(키 오류 vs 한도 초과 구분).
- **0건 방어**: 결과 0건이면 `body.items`가 빈 배열이 아니라 **빈 문자열 `""`**로 올 수 있음 → 배열 파싱 전 방어. 1건이면 `item`이 객체, 다건이면 배열로 옴(둘 다 흡수).

### 5.3 관광 타입 코드 (`contentTypeId`)

| 코드 | 타입 | 코드 | 타입 |
|---|---|---|---|
| 12 | 관광지 | 28 | 레포츠 |
| 14 | 문화시설 | 32 | 숙박 |
| 15 | 축제·공연·행사 | 38 | 쇼핑 |
| 25 | 여행코스 | 39 | 음식점 |

### 5.4 지역 코드 (`areaCode`)

| 코드 | 지역 | 코드 | 지역 | 코드 | 지역 |
|---|---|---|---|---|---|
| 1 | 서울 | 7 | 울산 | 35 | 경북 |
| 2 | 인천 | 8 | 세종 | 36 | 경남 |
| 3 | 대전 | 31 | 경기 | 37 | 전북 |
| 4 | 대구 | 32 | 강원 | 38 | 전남 |
| 5 | 광주 | 33 | 충북 | 39 | 제주 |
| 6 | 부산 | 34 | 충남 | | |

### 5.5 "진짜 랜덤" 알고리즘

TourAPI는 결과가 페이지로 쪼개져 오므로, 단순히 1페이지에서 고르면 항상 비슷한 결과가 나온다. 전체에서 균등하게 뽑으려면:

1. **전체 개수 확인** — 조건(지역·타입)으로 `numOfRows=1, pageNo=1` 호출 → 응답의 `totalCount` 획득
2. **랜덤 위치 선택** — `1 ~ totalCount` 중 정수 하나를 무작위 선택 (`randomIndex`)
3. **여행지 1건 조회** — `numOfRows=1, pageNo=randomIndex`로 해당 1건만 가져옴
4. **상세정보 결합** — 받은 `contentId`로 `detailCommon2` 호출해 개요·이미지 보강 → 결과 카드 완성

- **순수 랜덤 모드**: ~~M1 잠정 — 타입 균등 + 타입 내 항목 균등(`areaCode` 미지정 전국: 관광지 수 비례로 서울·경기 쏠림)~~ → ✅ **M22 승격(§6.9A): (타입×17시·도) 조합 균등** — 지역 미지정이면 **17개 시·도 lDong 버킷으로 실체화**해 조합 셔플(areaCode 쿼리는 항목 ~40% areacode 공백으로 기각 — §6.9A 실측). 배포 카피("17개 시·도에 같은 주사위")와 알고리즘이 이때부터 전 경로 정합(§13 해소). 후보 수 집계는 무변(lDong 도달률 99.99% — 잔차 0.01%, 분포만 변경).
- **조건 랜덤 모드**: 사용자가 고른 `areaCode`·`contentTypeId`로 위 알고리즘 적용.

### 5.6 호출 예산·캐싱·중복 방지

"다시 뽑기"가 핵심 UX인데 뽑기 1회 = **API 2~3콜**(totalCount → 1건 → detailCommon)이라, 대비 없이는 개발계정 일일 쿼터(보통 1,000회 수준 — 활용신청 화면에서 실수치 확인)가 연타에 금방 닳는다.

- **totalCount 캐시**: 조합(지역×타입×cat3)별 totalCount는 거의 안 변함 → **24시간 캐시** → 재뽑기는 1~2콜로 감소.
- **detailCommon 지연 호출** ✅**구현**(2026-07-28): 카드 기본 정보(이름·주소·대표이미지)는 `areaBasedList` 응답으로 구성하고, 개요는 `/api/overview` 로 **카드 노출 뒤** 받는다(`hooks/useOverview.ts`). contentId 가 매번 달라 24h 캐시가 **사실상 항상 미스**라, 뽑기 응답 안에서 기다리면 모든 뽑기가 이 왕복을 통째로 떠안았다 — 프로덕션 실측 개요 왕복 **중앙값 ~6초**(5.6·7.6·6.2·0.1초). 제거 후 카드 노출이 슬롯 애니메이션 하한(§7.9 `MIN_SPIN_MS` 1.2초)에 붙어 지연이 연출에 흡수된다(UI 실측 1.24~1.27초 고정).
  - 개요는 저장 대상이 아니라(`travelStore` 는 큰 필드 미저장) 찜·최근·다녀옴 기록에 영향 없음. 카톡 공유 설명은 주소 우선이라 개요 도착 전 공유해도 문구 동일.
- **❌ 조합 count 병렬화 — 시도 후 되돌림**(2026-07-28): 🌊 `drawSeaside` 는 가중치를 만들려고 모든 조합 count 를 어차피 다 부르므로 `Promise.all` 이 공짜로 보였으나, **성공률이 떨어져 되돌렸다**(병렬 3회 중 2회 타임아웃 실패 15.5·8.0초 ↔ 직렬 3회 전부 성공). 원인: `AbortSignal.timeout` 은 **생성 시점부터** 카운트라, 병렬로 던진 요청이 커넥션 풀에서 대기하는 동안 8초 예산을 다 태운다. 상류가 동시 요청을 흡수 못 하는 이상 병렬 + 호출당 타임아웃은 구조적으로 충돌한다. `drawByType`·`drawEmptySpot` 은 애초에 첫 성공 조합에서 빠져나가는 구조라 병렬화 대상이 아니다(안 불러도 될 조합까지 불러 쿼터만 태움).
- **중복 방지**: 뽑힌 `contentId`가 "최근 본 곳" 최근 N개(기본 10)에 있으면 재추첨(최대 2회). 단 **totalCount ≤ N이면 규칙 해제** — 작은 풀에서 무한 재뽑기 방지.
- **운영 전환**: 남에게 공유해 트래픽이 늘면 공공데이터포털 **운영계정 전환 신청**(쿼터 상향).
- **배치 예산(🍃 혼잡도, §6.7)**: 쿼터는 활용신청(서비스)별 **독립** — 일 1회 배치 ~265콜은 뽑기(KorService2) 쿼터를 잠식하지 않는다(잠식 0).
- **코스 예산(🧭, §7.10)**: 코스 1회 = 슬롯 3×(count+item) ≈ 6콜(개요 생략·count 24h 캐시 재사용), 재뽑기는 슬롯당 1~2콜 — 상세·워스트 산수는 §7.10.
- **빈 곳·분산 예산(🔭 §7.11 · ⚖️ §6.9)**: 🔭 1회 전형 3~4콜·워스트 25콜(후보 수는 0콜 — DB·캐시 계산), ⚖️ 가중 재료 0콜(DB 1쿼리/1h)·워스트 count 콜은 COMBO_BUDGET(34) 준용. 균등 승격(§6.9A)으로 무지역 뽑기 URL이 (타입×지역) 단위로 분화 — 24h 캐시 웜업 후 현행 동일.

## 6. 동적 조건 설계 — 지역 풀 필터 파이프라인

모든 뽑기가 공유하는 파이프라인. 동적 조건은 2~3단계에 꽂히는 필터일 뿐이다.

```
지역 풀 (기본: 17개 시·도 전체 / 지역 조건 있으면 선택 지역만)
  → 🎪 축제 필터   (켜져 있으면: 진행 중 축제 없는 지역 제거)
  → ☔ 날씨 필터   (켜져 있으면: 지금 강수 있는 지역 제거)
  → 🦀 제철 필터   (켜져 있으면: 이번 달 제철 품목 주산지가 아닌 지역 제거)
  → 🍃 한적 필터   (켜져 있으면: 성수기 예측 지역 제거 — 배치 적재분 DB 조회, §6.7)
  → 남은 풀에서 지역 1개 랜덤 선택
  → §5.5 4단계 랜덤 추출 (테마 조건 반영 · 🌊 바다 조건 시 바다 분류코드로 한정)
```

요청 예: `/api/random?areas=32,39&types=39&noRain=1&festivalOnly=1&seaside=1&seasonal=1&quiet=1&date=20260712` — **파라미터를 전부 생략하면 완전 랜덤**. `date`는 풀을 좁히지 않는 **기준일 축**(§6.8) — 각 동적 필터의 판정 기준일은 기본 '오늘', 📅 칩으로 변경(☔만 오늘 전용).

### 6.1 ☔ 날씨 필터 — 기상청 초단기실황

- **소스**: 공공데이터포털 "기상청_단기예보 조회서비스"의 초단기실황(`getUltraSrtNcst`). TourAPI와 같은 포털이라 활용신청만 추가하면 됨.
- **판정**: 응답의 `PTY`(강수형태) 코드 사용. `0`(강수 없음)만 "비 안 옴"으로 취급.

| PTY | 의미 | PTY | 의미 |
|---|---|---|---|
| **0** | **강수 없음 ✅** | 3 | 눈 |
| 1 | 비 | 5 | 빗방울 |
| 2 | 비/눈 | 6·7 | 빗방울눈날림·눈날림 |

- **호출 방식**: 기상청 API는 격자 좌표(`nx`, `ny`) 기반 → **시·도별 대표 격자좌표**가 필요하다.
  - ✅ **M11 구현 결정**: 격자쌍 17개를 손으로 옮기면 오타 위험이 커서, `constants.ts`엔 시·도 대표 **위경도**(`AREA_LATLNG`)만 두고 격자 변환은 순수함수 `latLngToGrid`(기상청 LCC 공식, `lib/weather.ts`)로 계산한다. 서울(37.5665,126.978)→(60,127) 앵커를 단위 테스트로 못 박음.
- **비용 관리**: 풀에 남은 지역 수만큼 병렬 호출(최대 17). 초단기실황은 매시 40분경 발표 → **지역별 30분 캐시**(Next.js fetch `revalidate`)로 뽑기마다 재호출하지 않는다. ✅ base_time은 45분 컷으로 직전 정시 물러남(발표 지연 방어), 항상 KST.
- **한계(인지하고 감)**: 시·도당 대표점 1곳이라 넓은 도(강원 등)의 지역 내 편차는 못 잡는다. 재미 프로젝트 수준에서 허용. ✅ **개별 지역 조회 실패는 보수적으로 제외**(비 안 옴 판정 불가 → 후보에서 뺌), 전 지역 실패면 필터만 건너뛰고 안내(§6.5).

### 6.2 🎪 축제 필터 — TourAPI searchFestival

- **소스**: `searchFestival` — 날짜 파라미터로 **오늘 기준 진행 중**(시작일 ≤ 오늘 ≤ 종료일) 축제 목록 조회.
- **동작**: 진행 중 축제들의 `areacode`를 모아 "축제 있는 지역 집합"을 만들고, 지역 풀과 교집합.
- **캐싱**: 축제 목록은 하루 안엔 거의 안 변함 → **1시간 캐시**면 충분.
- **표시**: 이 필터로 뽑힌 결과 카드에 그 지역의 진행 중 축제를 배지로 노출 — "왜 이 지역이 뽑혔는지" 맥락 제공.
- **테마 조건과 독립**: 축제 필터는 *지역*만 좁힌다. 테마로 '축제·공연·행사(15)'를 골라 축제 자체를 뽑는 것과는 별개 기능.

### 6.3 🌊 바다 필터 — TourAPI 분류코드 (상수 기반)

- **소스**: 추가 API·키 불필요 — 이미 쓰는 `areaBasedList`에 소분류(cat3) 파라미터만 얹는다.
- **확인된 실코드** (대분류 A01 자연 > 중분류 A0101 자연관광지, 2026-07 조사):

| cat3 | 의미 | cat3 | 의미 |
|---|---|---|---|
| `A01011100` | 해안절경 | `A01011300` | 섬 |
| `A01011200` | 해수욕장 | `A01011400` | 항구/포구 |

- **다른 필터와 동작 축이 다름**: 지역 풀을 좁히는 게 아니라 **뽑는 대상 자체**를 바다 관광지로 한정한다(테마 조건과 같은 축). 자연히 타입은 관광지(12) 계열로 고정.
- **랜덤 방식**: 코드 4종 각각 `totalCount`를 받아 **합산 가중치**로 코드 하나 선택 → §5.5 4단계 적용 (개수 적은 분류로의 쏠림 방지).
- 지역 조건과 조합 가능: "강원 중 · 바다" = 강원의 해수욕장·섬·항구 랜덤.

### 6.4 🦀 제철 필터 — 자체 제철 달력 테이블

- **조사 결론(2026-07)**: "이번 달 제철 재료 → 산지 시·도"를 주는 완성형 공공 API는 **없다**. 제철 정보와 산지 정보가 분리·불완전 → **정적 테이블 직접 구축**이 정답.
- **구현**: `constants.ts`에 `월 → [{품목, 주산지 시·도[]}]` 매핑. 12개월 × 3~6품목이면 충분하고, 제철 정보는 거의 불변이라 유지비 0. 골격 예시:

| 월 | 품목 | 주산지 |
|---|---|---|
| 3월 | 주꾸미 | 충남·전북 |
| 5월 | 멸치 | 부산·경남 |
| 9~10월 | 전어 | 전남·충남 |
| 11~2월 | 대게 | 경북 |

- **동작**: 이번 달 제철 품목들의 주산지 집합으로 지역 풀 교집합. 결과 카드에 **"이 지역 지금 제철: 대게 🦀" 배지** — 왜 이 지역이 뽑혔는지 스토리가 된다.
- **품목 자격**: **여행 동기가 되는 계절 별미만** 등재한다. 마늘·고추 등 연중 상비 향신료/양념은 사철 유통돼 '제철이라 특별히 맛있는' 유인이 없고 "지금 제철: 마늘 🧄" 배지가 여행 스토리가 안 되므로 **제외**한다(가드: `season.test.ts`). 매실처럼 수확기가 짧고 산지 여행 유인이 있는 계절 과실은 유지.
- **품목-맛집 매칭(음식점 조합)**: 제철 필터를 **음식점(39)** 테마와 함께 걸면, 그 지역 랜덤 식당이 아니라 **제철 품목명으로 그 지역 맛집을 실제 검색**(`searchKeyword2`)해 재료-식당을 맞춘다. 이때 `SeasonalItem.dish`(회·해산물 true / 과일·채소 false)로 검색 대상을 가린다 — "수박 맛집"은 없으므로(농산물), 그 지역에 dish 제철거리가 없으면 일반 식당으로 **폴백하되 그 식당엔 제철 배지를 붙이지 않는다**('수박 제철 지역의 횟집' 같은 미스매치·오해 제거). dish 매칭 성공 시 배지는 **매칭된 그 품목만** 노출(지역 전체가 아니라 뽑힌 맛집과 일치). 로직 `lib/tourapi.ts`(`pickSeasonalRestaurant`)·순수부 `lib/season.ts`(`dishSeasonalItemsForArea`)·가드 `season.test.ts`.
- **축제 보강(선택)**: `searchFestival` 진행 중 축제명에 제철 품목명이 들어 있으면(예: 영덕대게축제) 카드에 함께 노출 — 정적 테이블에 '지금 살아있는' 신호를 더함.
- **시그니처 조합**: 제철 필터 + 테마 '음식점(39)' = **"지금 제철 산지의 맛집 랜덤"** — 위 품목-맛집 매칭으로 실제 이행(예: 7월 전남 → 완도 전복집 + 🐚전복 배지). UI 프리셋 버튼 후보.

### 6.5 빈 풀(조건 과다)·API 장애 대응

**빈 풀** — 필터를 다 통과한 지역이 0개일 수 있다(예: 장마철 전국 비).

- **1차**: "조건에 맞는 지역이 없어요" 안내 + 어느 필터에서 다 걸러졌는지 표시
- **2차 제안(택1)**: ① 조건 무시하고 그냥 뽑기 ② ☔ 상황이면 실내 테마(문화시설 14 등) 전환 제안

**API 장애·지연** — 공공 API는 응답이 수 초씩 걸리거나 간헐적으로 실패한다.

- **로딩 마스킹**: 뽑기 애니메이션(§3)이 도는 동안 API 호출 — 정상 응답이면 지연이 연출로 흡수된다. 애니메이션 **최소 노출시간**(예: 1초)도 보장 — ⏳ 설계만 있던 항목, **M18(§7.9)에서 구현**.
- **타임아웃·재시도** ✅**구현**(2026-07-28, `lib/apiFetch.ts` — TourAPI·기상청 공용): 호출당 타임아웃 8초 + 재시도 1회 → 실패 시 "관광 데이터가 응답이 없어요. 다시 한번 뽑아보세요."(재시도 버튼 = 상단 "다시 뽑기"). **단 타임아웃은 재시도하지 않는다** — 설계 당시엔 "실패면 재시도"였으나, 타임아웃은 상류가 *죽은 게 아니라 느리다*는 뜻이라 즉시 다시 때리면 또 기다려 꼬리가 2배(8→16초)가 된다(프로덕션 실측: 뽑기 12.9초 = 8초 타임아웃 + 재시도 4.9초). 재시도의 값어치는 연결 리셋 같은 **즉시 실패**에 있고 그건 그대로 재시도된다.
  - ⚠️ **호출 1건당 상한이지 요청 전체 상한이 아니다** — 조합을 순회하는 경로는 (호출 수 × 상한)까지 늘 수 있다. 콜드 캐시 + 느린 상류가 겹치면 일반 뽑기 13초 실측(2026-07-28). 요청 단위 데드라인은 미구현.
  - ⚠️ **`next` fetch 캐시와 공존 확인**: `signal` 을 넘겨도 데이터 캐시는 안 꺼진다(next 16.2.10 `patch-fetch` 실측 — `signal` 은 통과, 백그라운드 재검증 때만 제거).
- **동적 필터 소스 장애**: ☔·🎪 API가 죽어 있으면 해당 필터만 건너뛰되, 건너뛴 사실을 결과에 표시(조용히 무시하지 않는다).
- **배치 데이터 stale(🍃 한적)**: 혼잡도 적재분이 낡았으면(조회 날짜 행들의 max(fetched_at) 48h 초과) 🍃 필터만 건너뛰고 표시, 후보 수 배지는 dynamic 폴백 — §6.7.

### 6.6 조건 후보 백로그 (2026-07-03 조사)

정식 채택 전 후보들 — 데이터 실존 여부를 조사해 판정만 박아둔다.

| 후보 | 소스 | 판정 | 메모 |
|---|---|---|---|
| 😷 미세먼지 좋은 곳 | 에어코리아 API (시도별 실시간 PM10/2.5, 포털 키, `getCtprvnRltmMesureDnsty`) | ✅ 가능 | **차기 1순위** — ☔ 필터와 동일 구조 |
| ❄️ 눈 오는 곳 / 🌡️ 기온 | 기상청 초단기실황 기존 필드 (PTY=3, T1H) | ✅ 가능 | ☔ 구현 후 공짜 확장 |
| ~~🐕 반려동물 동반~~ | ~~한국관광공사 `KorPetTourService` (지역 조회 지원, 포털 키)~~ | ✅ **M25 채택(§6.11)** | 정식 경로는 `KorService2` **통합** `detailPetTour2`(활용신청 불필요 — 2026-07-28 실측). 독립 서비스는 포털 미등록(403만 실존) |
| ~~♿ 무장애 여행지~~ | ~~한국관광공사 `KorWithService1` (약 6만 건, 포털 키)~~ | ✅ **M25 채택(§6.11)** | 현행 서비스는 `KorWithService2` — 활용신청 승인 완료(2026-07-28) |
| 🏕️ 캠핑 | 고캠핑 API (포털 키) | ✅ 가능 | 지역 서버 파라미터 불확실 — 응답의 도/시군구 필드로 필터 |
| ~~🧍 한적한 곳~~ | ~~관광지 혼잡도 **예측** API (KT 기반, 향후 30일)~~ | ✅ **M17 채택(§6.7)** | "한적 예측"으로 정식 채택 — 일 1회 배치 프리컴퓨트 + 🍃 토글 |
| 🎭 공연 중인 곳 | KOPIS 공연 API (시도·공연상태 조회) | ⚠️ 키 별도 | 포털 키 아님 — KOPIS 자체 발급 |
| 🚗 가까운 곳 | 하버사인 직선거리 (API 불필요, 브라우저 위치권한) | ⚠️ 근사 | 직선거리지 이동시간 아님 |
| 🌸 벚꽃·단풍 시기 | 공식 예측 API **없음** (기상청은 예측 발표 안 함) | ❌ | 과거 관측 통계로 '평년 시기' 정적 테이블 대체 가능 |
| 🧍 전국 실시간 혼잡도 | **없음** | ❌ | 실시간은 서울 121곳 한정(서울시 API)뿐 |

### 6.7 🍃 한적 필터 — 관광지 집중률 예측 (M17)

§6.6 백로그 "🧍 한적한 곳" 후보의 정식 채택. **"지금 붐비는 시기로 예측되는 지역"**을 풀에서 걸러낸다. 요청 파라미터 **`quiet=1`**(`parseBool`, §6 요청 예).

- **소스 (실호출 검증 2026-07-10)**: 둘 다 한국관광공사 B551011 계열 — **기존 `TOUR_API_KEY` 그대로**(공공데이터포털 활용신청만 2건 추가 — 신청 직후 반영 지연 가능).
  - **집중률** `TatsCnctrRateService/tatsCnctrRatedList`: 관광지×날짜 행, 조회일~+29일(30일 예측), `cnctrRate`(0~100 문자열). 필수 `areaCd`+`signguCd`(법정동). `numOfRows=5000`이면 **시군구당 1콜로 30일 전체**(실측 최대 종로구 3,390행). 시골 군 표본 20/20 커버(영양군 ~9곳 포함).
  - **방문자수** `DataLabService/locgoRegnVisitrDDList`: 전국 하루 804행(268 시군구×관광객 3구분 a현지인/b외지인/c외국인), `startYmd=endYmd` 1콜. **약 1개월+ 지연 실측** → 실시간 아님. M17은 **적재만** → ✅ **⚖️ M22(§6.9)가 역가중으로 활용 개시**(잔여: 히트맵). ⚠️ `tou_div_cd` 실값은 "1"현지인/"2"외지인/"3"외국인(스키마 주석 a/b/c는 오기 — M22에서 정정).
  - 쿼터: 활용신청별 **독립 쿼터** — 배치 콜은 뽑기(KorService2) 몫 **잠식 0**. 재시도 포함 상한 ~500콜/회 → 일 2회 실행까지 안전(§5.6).
- **⚠️ 지표 의미론 — `cnctrRate`는 절대 혼잡이 아니다**: KT 이동통신 기반 ML 예측, **그 관광지의 최붐빔 시기=100인 자기 상대치**. 지역 간 절대 비교가 아니라 **"그 지역이 지금 성수기인가"**를 잰다 → 🍃 필터의 정의 = **성수기 예측 지역 회피**. 절대 유동 규모 비교는 방문자수 `touNum` 몫. UI 문구는 항상 **'예측' + 데이터 기준일**(실시간 사칭 금지).
- **아키텍처 — 유일한 배치 프리컴퓨트 필터**: ☔·🎪는 요청 경로 원격 호출(≤17콜)이지만 혼잡도는 **전국 250콜 규모**라 성립 안 함 → **일 1회 배치**: `vercel.json` crons(기존 파일에 추가 — `regions` 유지) `0 21 * * *`(UTC 21시 = **KST 06시** — Hobby cron은 지정 '시' 안에서 **뒤로만** 최대 59분 지연, 일 단위, **총 2개 상한**이라 후속 cron 필요 시 경로 하나에 합침) → **`GET /api/cron/congestion`**(Vercel cron은 **GET**으로 호출 — POST만 두면 405 무성 실패) → Neon 적재. **뽑기·카운트 요청 경로는 DB 조회만**(외부 API 0콜 — 지연·쿼터·장애 전파 구조적 차단).
  - 인증: `Authorization: Bearer ${CRON_SECRET}` 검증(Vercel env 등록 시 cron 요청에 자동 첨부). **개발 모드(NODE_ENV=development)는 검증 생략** — 최초 적재 경로(아래)용.
  - `export const maxDuration = 300` — **Fluid compute 활성 확인 필수**(레거시 함수 모드면 Hobby 상한 60s로 회귀).
  - 배치 절차: ① 집중률 250 시군구 × 1콜(동시성 8, 시군구별 실패 재시도 1회) — **응답 도착 즉시 집계 → 청크 upsert(≤1,000행)**: 한 방 INSERT는 바인드 파라미터 65,534 상한 임박이라 금지. 타임아웃·부분 실패에도 진행분 보존, 재실행만으로 자연 복구(idempotent) ② 방문자수 — 최근 가용일 역탐색(`visitorProbeDates`: 오늘-28일부터 과거로 최대 13일) 1일치 upsert, 집중률과 **병렬** ③ 보존(`retentionCutoff`): congestion은 `baseYmd < 오늘-7일` 삭제, visitor는 180일. **날짜를 가정하지 않는다** — 응답 행의 `baseYmd`를 그대로 키로 저장(KST 새벽엔 첫 행이 '오늘'이 아닐 수 있음).
  - **최초 적재**: cron은 **프로덕션 배포에서만 발화**(PR 프리뷰 미작동) → 로컬 dev 서버(`localhost:3456`, DB는 **공용 Neon**)에서 `/api/cron/congestion` 1회 호출이 유일한 사전 검증 경로. dev 모드 검증 생략이라 **시크릿이 셸 히스토리·채팅 어디에도 등장하지 않는다**.
  - 날짜 처리는 **항상 KST**(`kstYmd`·`visitorProbeDates`·`retentionCutoff` 전부 순수 함수 — 서버 UTC 함정, §6.1 base_time 계열).
- **⚠️ 코드 체계 함정 — 세 코드가 서로 다르다**: TourAPI areaCode(시·도 1~39) / 통계청 5자리(`KOREA_SIGUNGU.code`, 정복 지도) / **법정동 5자리(이 API들)**. 통계청·법정동은 **같은 값이 다른 지역을 가리킨다**(법정동 11110=종로구 vs 통계청 11110=노원구) — **코드값 직결 금지, (시·도 areaCode + 시군구명) 키 매핑**. `lib/congestionCodes.ts`(AUTO-GENERATED — `scripts/genCongestionCodes.mjs`가 `scripts/data/`의 **공식 코드표 xlsx**(활용 매뉴얼 동봉본, 출처 URL 주석)에서 생성)로 250개 전수 매핑 + **특례 4건**: ① 남구→미추홀구 28177(2018 개명) ② 세종시→세종특별자치시 36110 ③ **부천시→원미 41192+소사 41194+오정 41196 3코드 집계** ④ 군위군→대구 27720(2023 편입 — 앱 `area`는 경북 유지). 시·도 레벨 변환은 `LDONG_TO_AREA`(§6.2) 재사용.
- **데이터 흐름 — 판정·배지 공용 1회 조회**: `db/congestion.ts`(적재/조회 래퍼 — 스키마와 동거해 lib '순수 로직' 관례 보호, `lib/kma.ts`처럼 단위 테스트 비대상)의 `getCongestionDay(dateYmd)`가 **`baseYmd ≤ 기준일` 중 최신** 날짜의 전국 행을 일괄 반환. **`unstable_cache`(revalidate 3600)** 필수 — ① 일 1회 갱신 데이터 ② **neon-http 드라이버는 POST fetch라 기존 `revalidate` 캐시 관례가 안 먹히는 유일한 원격 소스** ③ Neon 무료 티어는 유휴 suspend(콜드 재개 0.5~2s)라 캐시 없으면 뽑기·후보 수(고빈도)에 직격.
  - **pctRank는 저장하지 않는다** — 조회 시점에 `rankDaily`(순수)로 그날 존재하는 전국 행 분포에서 계산. 배치 2차 패스 불필요 + 부분 실패로 잔존한 직전 성공 행도 분포에 자연 포함.
- **판정 (순수부 `lib/congestion.ts`, TDD — `QUIET_AREA_CUT` 등 상수도 여기 export)**:
  1. `aggregateDaily(rows)` — 시군구×날짜별 `cnctrRate` **중앙값**(`medianRate`) + 스팟 수 + 과밀 스팟 비율(`crowdedShare`, rate≥80 — 저장만, 후속 히트맵·판정 대안).
  2. `rankDaily(dayAggregates)` — 같은 날짜 전국 시군구 중앙값 분포의 **백분위 `pctRank`**(0=가장 한적). 고정 임계값이 아닌 분위수 — 전국 동시 계절 변동에 강건.
  3. `quietAreaCodes(ranked, cut)` — **시·도 판정: 소속 시군구 pctRank의 중앙값 ≤ `QUIET_AREA_CUT`(0.4)**. §6 시·도 풀 단위와 정합. 결측 시군구는 판정에서 제외(보수).
  4. `narrowByQuiet(base, quietSet)` — **순수부 동형 = `narrowByWeather`**(base: number[]|null + 집합 교집합). `drawRandom` 좁히기 **5단계**(🦀 제철 4단계 다음, 🌊 바다 분기 앞 — 기존 "5) 바다" 주석 번호는 6)으로 갱신). **래퍼 동형 = ☔ 날씨 단계**: try/catch로 `getCongestionDay` 조회 → 실패·stale 시 `notices.push`("혼잡도 데이터를 못 불러와 🍃 조건은 건너뛰었어요") 후 **필터·배지 모두 스킵**.
- **판정 기준일·신선도 (단일 규칙)**: 기준일 = `kstYmd(now)` — 📅 방문 시점 칩(**→ §6.8 M19로 정식 채택**)이 이 파라미터만 바꿔 그대로 연결. `getCongestionDay`가 반환한 날짜 행들의 **`max(fetched_at)` > 48h → stale** → 필터 소프트 스킵 + 후보 수는 dynamic 폴백. 30일 선행 예측이라 '오늘 이하 최신' 행은 배치가 하루 이틀 죽어도 존재 — **fetched_at이 실질 신선도 축**(cron 전달은 best-effort·재시도 없음·드물게 중복 — 중복은 upsert라 안전).
- **후보 수 배지 — 정적 편입(정확한 숫자)**: ☔·🎪(dynamic)과 달리 DB 조회라 결정적. **`planCandidateCount(p, month, quietSet?: Set<number> | null)` 시그니처 확장**(제철 calendar 주입과 동형 — 순수 유지). quiet 켜짐 + 교집합 0 → **`{ kind: "empty" }`** 반환. ⚠️ 라우트에서 지역 풀을 사전 교집합하는 방식은 금지 — CountParams의 "빈 배열=전국" 의미와 충돌해 **전멸이 전국으로 뒤집힌다**. quietSet 주입·stale 판단은 `countCandidates`(tourapi)에서 — stale은 예외가 아니라 **정상 경로로 `dynamic` 반환**(명시 분기).
- **결과 카드 근거 배지**: `PickedInfo.congestion?: { sigunguName: string; pctBelow: number; baseYmd: string }` — **`pctBelow = round(pctRank×100)`**. 계산은 `buildBadges` 동기 수행: **`BadgeCtx`에 `sigunguRanks: Map<법정동cd,{pctRank}>` + 기준 `baseYmd`를 실어**(weatherObs 동형) 뽑힌 좌표 → `sigunguAt`(§7.4 점-다각형, 최근접 스냅 ~7.5km) → (area+이름) → 법정동 → pctRank. 표기 예: **"🍃 집중률 하위 12% · 한적 예측 (7/10 기준)"**. **노출 조건: 🍃 켠 뽑기 + `pctRank ≤ 0.5`일 때만** — 필터는 시·도 단위라 개별 시군구가 높을 수 있어(시·도 중앙값 0.35에 시군구 0.68) 자기모순 카드 방지. 판정 실패·데이터 없음·0.5 초과 → 배지 생략(허위 서사 금지). `koreaMap` 서버 편입은 **클라 번들 무영향**(§7.4 지연 로드는 클라 원칙) — 콜드스타트 파스 비용만 소폭 수용.
- **DB (Drizzle, additive-only)**: 시군구 코드 컬럼 철자는 **두 테이블 모두 `sigungu_cd`로 통일**(API 원어는 signguCd/signguCode로 서로 달라 오히려 혼동 — 주석으로 명시).
  - `congestion_daily` (PK `sigungu_cd`+`base_ymd`): `spot_count` int · `median_rate` double · `crowded_share` double · `fetched_at` bigint(ms)
  - `visitor_daily` (PK `sigungu_cd`+`base_ymd`+`tou_div_cd`): `tou_num` double
  - 마이그레이션은 신규 테이블 CREATE만(기존 무변) — 기존 규칙대로 **머지 전 Neon 선적용**.
- **불변식·테스트**: `quiet` 미설정 = 쿼리스트링·서버 동작 완전 무변(**"조건 0개 = 완전 랜덤"** 유지). 갱신 — `query.test.ts`(buildRandomQuery quiet) · `candidateCount.test.ts`(quietSet 주입·empty·dynamic 분기). 신규 — `congestion.test.ts`: 집계(중앙값 짝/홀·빈 입력) / 백분위(동률·0·1 경계) / 시·도 판정(컷 경계·결측 제외) / narrowByQuiet(교집합·전멸) / 코드 매핑(**250 전수 키 존재·특례 4건·11110 충돌 회귀 가드**) / `kstYmd`·`visitorProbeDates`·`retentionCutoff`(월말·연초·UTC 자정 경계).
- **손대는 파일**: 기존 — `app/page.tsx` · `components/FilterPanel.tsx` · `lib/query.ts` · `app/api/random/route.ts` · `app/api/random/count/route.ts` · `lib/tourapi.ts`(DrawParams·5단계·BadgeCtx) · `lib/candidateCount.ts` · `types/tour.ts` · `components/ResultCard.tsx` · `vercel.json`(crons 추가) · `db/schema.ts` · `.env.local.example` · `CLAUDE.md`(시크릿 키 목록). 신규 — `lib/congestion.ts`(순수) · `lib/congestionCodes.ts`(생성물) · `db/congestion.ts`(적재/조회 래퍼) · `app/api/cron/congestion/route.ts` · `scripts/genCongestionCodes.mjs` · `scripts/data/`(코드표 xlsx 원자료).
- **검증 기준**: 검증 세트(tsc·eslint·vitest·build) + ① 로컬 dev에서 배치 1회 — **소요시간 실측**(300s 대비 여유 확인, 초과 조짐이면 집중률/방문자수 cron 분리 백로그 발동) + Neon 행수 확인(250 시군구 × 30일 ±) ② 🍃 켠 뽑기 — 배지·notice·후보 수 숫자 확인 ③ `CRON_SECRET`를 Vercel Production env + `.env.local` 양쪽 등록 ④ 머지 후 **첫 새벽 cron 로그에서 실행 성공 + 첫 `baseYmd`가 당일인지** 확인(KST 06시에 당일 예측 미생성이면 cron 시각 조정).

### 6.8 📅 방문 시점 칩 (M19)

§11.1 백로그 "📅 방문 시점 칩"의 정식 채택. 뽑기의 **기준일**(오늘/내일/이번 주말(토)/다음 주말(토))을 선택해 **켜진 조건의 판정 날짜만** 바꾼다 — 집중률 API가 30일 예측이라 데이터는 이미 Neon에 있고(§6.7), `getCongestionDay`가 날짜별 캐시로 파라미터화돼 있어 **호출부의 날짜 인자만 바꾸면 되는** 저비용 확장. 진행 순서는 **M18(§7.9) 뒤** — 둘 다 `app/page.tsx`·FilterPanel을 만지고, 여기 신규 문구(☔ 잠금 안내·notice)가 §7.9 카피 원칙의 적용 대상.

> M20 반나절 코스(§7.10)의 `date`도 같은 축 — 코스 구성 무변, 🍃 코스 헤더 배지의 기준일만 전환된다.

- **불변식(최우선)**: 날짜는 필터가 아니라 **기준일 축** — **날짜 단독 선택은 후보 집합·뽑기 분포 무변**. 조건 0개면 `date`가 URL에 있어도 서버 동작은 완전 랜덤과 동일(§2 — 테스트 선작성). `hasAny`(= **'선택 초기화' 버튼·에러 패널 게이트** — 후보 배지는 hasAny 무관 상시 표시)에 date 불포함 유지하되, **초기화 버튼 노출 조건은 `hasAny || dateYmd !== null`로 확장**('내일'만 선택 시 초기화 대상은 있는데 버튼이 없는 UI 모순 방지). `clearFilters`엔 포함('오늘' 복귀). **상시 마이크로카피 1줄**(날짜 행 바로 아래): "날짜는 조건을 판정할 기준일이에요 — 날짜만으론 후보가 줄지 않아요."(칩은 하이라이트인데 하단 '아무것도 안 고르면 완전 랜덤' 문구가 뜨는 시각 모순 해소 + "날짜가 필터인가요?" 오해 선제 답변).
- **요청 파라미터**: **`date=YYYYMMDD`**(절대 날짜 — 주말 계산은 클라 몫, 서버는 검증만). `lib/query.ts`에 **`parseDateYmd(raw, now)`** 신설: 8자리·실존 날짜 + 범위 **`오늘 ≤ date ≤ 오늘+29`(KST — 집중률 예측 창)** 밖이면 null(무시 — 경계 정리 관례). '오늘' 선택 시 클라는 **파라미터 자체를 생략**. **요청당 단일 시계**: 라우트에서 `const now = new Date()` 1회 생성 → `parseDateYmd(raw, now)`와 `drawRandom({ …, dateYmd, now })`에 동일 주입(자정 걸침 시 검증 기준일·판정 기준일 어긋남 방지).
- **날짜 파생 규칙** — `DrawParams.dateYmd?: string` 신설. **우선순위 명문화: 명시 주입(`month`·`today`) > `dateYmd` 파생 > 현재시각**(테스트·호출부가 동시 주입해도 결정적):
  - 🦀 제철: `month = params.month ?? (dateYmd ? monthOf(dateYmd) : currentMonth())` — 월말에 다음 달 주말 케이스 포함.
  - 🎪 축제: `getFestivalMap(params.today ?? params.dateYmd ?? todayKST())` — `eventStartDate`가 URL에 들어가 캐시 자동 분리, `isInProgress` 판정 그대로.
  - 🍃 한적: `getCongestionDay(params.dateYmd ?? kstYmd(params.now))` — §6.7 확장 훅 그대로. stale 판정 `nowMs`는 실제 현재 시각 유지.
  - 상충 주입(예: `month`와 `dateYmd` 동시) 케이스 테스트 1개 추가.
  - ☔ 날씨: **미래 불가(구조적 — 초단기실황은 현재 관측만)**. **전이 규칙(🌊-types 패턴 그대로)**: 오늘 아닌 칩 선택 시 ☔ 토글 **잠금 표시**(잠긴 토글은 눌리지 않은 것으로 취급 — 🌊 관례) + **`buildRandomQuery`가 date≠오늘이면 `noRain` 미방출**(표시·전송·서버 일치, 선택 state 보존 — 오늘 복귀 시 복원; count 경로도 noRain 미방출이라 dynamic 강등 없음). **서버 무시는 `dateYmd > 오늘(KST)`일 때만**(직접 URL `date=오늘&noRain=1`은 범위상 유효 — 죽이지 않음) + notice "오늘이 아닌 날짜라 ☔ 조건은 건너뛰었어요."(직접 URL 방어 전용 — 정상 UI 경로에선 안 뜸). 잠금 안내 문구: "☔ '비 안 오는 곳'은 지금 날씨만 알 수 있어요 — 그래서 오늘 뽑기에서만 켤 수 있어요."(🌊 이유 설명형 문형). 단기예보(`getVilageFcst`, +3일 POP) 확장은 §11.1 백로그.
- **count 경로**: `countCandidates`를 **옵션 객체로 개편** — `countCandidates(params, opts?: { month?: number; now?: Date; dateYmd?: string })`(현행 2번째 위치 인자가 `month: number`라 dateYmd를 그 자리에 못 넣음; 직접 단위테스트가 없어 시그니처 변경 부담 낮음). 내부 파생은 위와 동일 규칙 — `month = opts.month ?? (dateYmd ? monthOf(dateYmd) : currentMonth())`, quietSet은 `getCongestionDay(dateYmd ?? kstYmd(now))`. 🍃 정적 편입·empty·dynamic 분기는 §6.7 그대로.
- **주말 계산**(신규 순수부 `lib/tripDate.ts`, TDD): `dateChips(now)` → `[{key, label, ymd}]`. **정의(요일 기준 명시): 오늘이 토·일이면 '이번 주말' = 오늘**(→ ymd 중복으로 자연 생략), **그 외엔 다가오는 토요일. '다음 주말' = 그다음 토요일**(일요일에 '다가오는 토요일'=6일 뒤가 '다음 주말'과 충돌하는 파탄 방지). 중복 ymd 칩 생략(개수 가변 3~4개). **칩 라벨 "이번 주말(토)"·"다음 주말(토)"** — '주말'(토·일 이틀)을 약속하면 일요일 진행 축제 누락 반례에 무방비라 토요일 기준을 정직 표기. 토·일 합집합 판정은 §11.1 백로그로 명시 분리. KST 헬퍼는 **신설 `lib/kst.ts`**로 이동(`kstDateParts`·`ymdOffset`·`kstYmd` — `lib/congestion.ts`는 re-export로 기존 소비처 무변): congestion을 직접 import하면 클라 번들에 congestionCodes 대형 테이블 유입 + 도메인 모듈에 범용 헬퍼가 사는 계층 역전. 테스트: 요일별 7케이스(**일요일 기대값 명시**) + 월말·연말 경계 + 중복 생략.
- **UI(FilterPanel)**: 지역·테마와 같은 **가로 pill 행**(`chip()` 재사용, 단일 선택 라디오형) — 테마 아래·추가 조건 위 "📅 언제 가요?" 섹션, 기본 '오늘' 상시 노출(숨김 확장이면 데모에서 기능이 안 보임 — 노출형 유지). **선택 상태는 ymd로 저장** — 렌더마다 `dateChips(now)`와 조인해 일치 칩 하이라이트, **일치 칩이 없으면(자정 통과·과거화) '오늘' 자동 복귀**(state 초기화 — key 저장 시 고른 날짜가 소리 없이 바뀌는 정직성 위반 방지). 자정 경계 테스트: 토 00:00 직후 금요일 선택 ymd가 어느 칩에도 안 붙는 케이스.
- **결과 카드 — 배지 문구 개정 포함**(기존 "(M/D 기준)"의 '기준'이 '데이터 기준일'로 오독되는 양의성 + 배치 지연 시 `baseYmd < 선택일`이면 고른 날짜와 다른 날짜가 떠 버그처럼 보임): 🍃 배지 = **"🍃 {M/D} 한적 예측 · 집중률 하위 N%"**(예측 대상일 선두 — 정상 경로에선 선택일과 일치), **`baseYmd < 선택일` 예외에만 "({M/D} 데이터)" 병기**. 🎪 축제 배지 = **오늘 아닌 기준일이면 "(M/D 기준)" 부가**(시작 전 축제가 '진행 중'처럼 보이는 오해 방지 — §7.9 원칙 2 기준일 명시).
- **테스트**: 신규 — `kst.test.ts`(이동분) · `tripDate.test.ts`(위) · `query.test.ts`에 `parseDateYmd`(형식·실존·범위 오늘/+29 경계) + `buildRandomQuery`(date 방출/오늘 생략/**date≠오늘 시 noRain 미방출**) · "date 단독 = 계획 무변"(`candidateCount.test.ts`) · 날짜 파생 우선순위·상충 주입. 갱신 — `candidateCount.test.ts`(month 파생). 축제·한적 판정 자체는 기존 테스트 커버(날짜는 통과 인자).
- **손대는 파일**: 신규 — `lib/kst.ts` · `lib/tripDate.ts`(+각 테스트). 기존 — `lib/query.ts` · `lib/congestion.ts`(re-export) · `lib/tourapi.ts`(DrawParams.dateYmd + 파생 3곳 + countCandidates 옵션화) · `lib/candidateCount.ts` · `app/api/random/route.ts` · `app/api/random/count/route.ts` · `app/page.tsx` · `components/FilterPanel.tsx` · `components/ResultCard.tsx`(배지 문구).
- **검증 기준**: 검증 세트 + ① 내일+🍃 — 배지 "🍃 {내일 M/D} 한적 예측" ② 주말(토)+🎪 — 그 날짜 진행 축제만 + "(M/D 기준)" ③ 날짜만 선택 — 후보 수·동작 완전 랜덤과 동일 ④ 오늘 아닌 칩에서 ☔ 잠금 전이 + 직접 URL notice ⑤ **데모는 날짜+🍃(또는 🎪) 페어로 후보 수 배지 숫자가 변하는 장면으로**(날짜 단독은 무변이라 날짜만 바꾸는 시연은 아무 변화가 없음 — 함정). 데이트피커 부재는 의도적 단순화(여행 의사결정의 실제 단위 = 오늘/내일/주말 칩 4개 — 임의 날짜는 백로그).

### 6.9 ⚖️ 분산 모드 — 시·도 균등 승격 + 방문자 역가중 (M22)

> 두 부분: **(A) 시·도 균등 승격**(전 모드 공통 — §5.5 개정·§13 미결 해소) + **(B) ⚖️ 분산 모드 토글**(조건 모드 전용,
> **기본 OFF**). ⚖️는 필터가 아니라 **분포 축**(§6.8 날짜=판정 축과 대구) — 후보 풀은 그대로, 뽑힐 확률만 기운다.
> **후보 수 무변이 불변식**(A·B 공통 — 풀 자체는 안 바뀐다).

#### (A) 시·도 균등 승격 — 무지역 뽑기를 (타입×17시·도) 조합 균등으로

- **배경**: 현행 무지역 뽑기는 `areaCode` 없는 전국 쿼리(타입 균등 + 항목 균등)라 **관광지 수에 비례해 서울·경기 쏠림** —
  그런데 배포된 M18 카피(슬롯 "17개 시·도에 같은 주사위"·배너 "전국 모든 시·도에 같은 주사위")는 시·도 균등을 주장(과장 상태).
  §13 "균등 정의"가 M1부터 미결이었고, ⚖️도 시·도 선택 축이 있어야 성립 → 셋을 한 번에 해소.
- **변경**: `drawByType`에서 `areaPool = null`(지역 미선택)이면 **17개 시·도 버킷으로 실체화** — 조합이 (타입×17시·도) 셔플 균등이
  된다(✅ 2026-07-10 사용자 결정). 순수·조건 모드 공통(같은 파이프라인 — §2.1 "자동 보장" 유지).
- ⚠️ **버킷은 `areaCode` 파라미터가 아니라 법정동 `lDongRegnCd` 쿼리로** (✅ 2026-07-27 사용자 결정 — 리뷰 실측 반영):
  - **왜**: TourAPI 항목의 **~40%가 `areacode` 필드 공백**(실측 2026-07-27: type 12 전국 12,682 vs 17시·도 areaCode 쿼리 합
    7,471 — 전 타입 합 21,240 vs 12,723). 공백 항목은 정상 관광지(가덕도·가리산자연휴양림 류 — 좌표·주소 보유, **지방 한적
    명소 성격**)라 areaCode 버킷이면 무지역 뽑기에서 영구 탈락 — 오버투어리즘 주제와 정면 충돌 + 후보 수 배지 67% 과대.
    같은 항목들이 `lDongRegnCd`는 보유(43/43 실측) → **lDong 버킷이면 99.99% 도달**(type 12 기준 12,681/12,682 — 세종
    36110 특례 포함 구현 실측). 🎪 축제가
    같은 함정을 lDong으로 해결한 전례(`lib/constants.ts` `LDONG_TO_AREA` 주석)·구코드 폐기 예고(§14.3)의 진행 징후와도 정합.
  - **매핑**: `LDONG_TO_AREA` 역방향 `AREA_TO_LDONG`(신코드만 — 51 강원·52 전북, 폐지 29·42·45·46 제외). **특례 2건**:
    ① **세종** — `lDongRegnCd=36`은 0건, **`36110`이 54건으로 확정**(✅ 구현 실측 — areaCode=8은 23건뿐이라 폴백 불요,
    세종 항목 ~57%가 areaCode 쿼리 탈락이었음. §7.11 "두 필드 모두 5자리" 관찰과 정합). ② **전남광주 신코드 12** — 광주(5)·전남(38) 두 슬롯이
    **같은 병합 쿼리 `lDongRegnCd=12`를 중복 보유**(의도적 중복 — 병합권 합산 확률 2/17 유지, 풀 내 항목 균등). 광주/전남
    1/17 정밀 분리는 `lDongSignguCd` 하위 쿼리 확장이 필요해 1단계 기각(§11.1 백로그).
  - **적용 범위**: **실체화로 생긴 ALL 풀만** lDong 버킷. 사용자 지역 선택·🍃 등 필터가 만든 풀은 기존 `areaCode` 쿼리 유지
    (기존 동작 무변 — 그 경로의 areacode 공백 손실은 M22 이전부터로 §11.1 전면 마이그레이션 몫).
  - **배지 귀속**: lDong 쿼리 응답은 `item.areacode`가 빌 수 있음 — lDong 필드로 앱 areaCode 판별(축제 전례:
    `lDongRegnCd` + 광주권 5개 구 `lDongSignguCd` {210,240,270,300,330} → 5, 그 외 12 → 38).
  - **순수부**: 조합 생성을 순수 함수로 추출(예: `buildDrawCombos`) — 버킷 수·병합 중복·lDong/areaCode 분기·조합 수를 단위
    테스트로 잠금(리뷰 권고 — 기존 스위트가 이 변경을 전혀 잠그지 않음이 돌연변이로 실측됨).
  - **console.warn 이동**: 예산 잘림 시점 경고는 오경보(85조합 전부 비어있지 않음 실측 — 상한이 실제로 자른 적 없음) →
    **루프 소진 후 EMPTY_POOL 직전**으로 이동(진짜 예산 부족만 잡음).
- **효과·비용**: 카피가 전 경로에서 참이 됨(카피 무변·동작 수리). 후보 수 집계는 무변(count 경로는 전국 쿼리 그대로 — lDong
  도달률 99.99%라 배지 과대 실질 해소, 잔차 0.01%). 뽑기 1회 콜 수 무변(셔플 후 **첫 비어있지 않은 조합만** count — 현행과
  동일 2~3콜, `COMBO_BUDGET` 34 상한 기존 그대로). 뽑기 URL이 (타입×지역) 단위로 분화 — 24h `getTotalCount` 캐시가 조합
  단위로 웜업(콜드 미스는 1회성).

#### (B) ⚖️ 분산 모드 토글 — 외지인 방문자수 역가중 (기본 OFF)

- **정의**: 조건 모드 전용 토글. ON이면 시·도 선택을 균등 → **외지인 방문자수 역가중**으로. **기본 OFF**(✅ 2026-07-10 사용자
  결정 — 로드맵의 '기본 ON'은 기각: M18 카피·"조건 0개=완전 랜덤" 불변식·기존 안내 문구 전부 무손상, 심사 시연은 켜서).
- **데이터 — `visitor_daily` 활용 개시**(M17 "적재만"의 활용처): 실측(2026-07-10) `tou_div_cd` 실값 = **"1"현지인/"2"외지인/"3"외국인**
  (스키마 주석 a/b/c는 오기 — 정정 포함). 최신 base_ymd는 약 1개월 지연(정상), 268 시군구.
  - `getVisitorRecent()`(db/congestion.ts 동거, `unstable_cache` ["visitor-recent"] 1h) — **최근 가용 ≤7일**의 외지인("2") 행을
    시·도 합산 **평균**(단일일의 요일·이벤트 노이즈 완충. 현재 1일치뿐이라 초기엔 1일 — 배치가 매일 쌓아 자연 확장).
  - 시·도 귀속은 **2계층**(✅ 구현 실측으로 확정 — 2026-07-27): ① `LDONG_TO_APP_AREA` 정밀 매핑 우선(군위 27720→area 35
    특례는 여기서 흡수) → ② 미스만 시도 2자리 `ldongToAreaCode` 폴백. 순서 고정(반전 시 군위 유출 — 테스트 잠금). 미매핑
    16개(통합시 상위 코드 41110 수원 등 + 신코드 52110, 외지인 ~15%)는 처음엔 "유실 구제" 대상으로 봤으나 **실은 전부
    아래 입도 중복**이라 dedupe가 걷어낸다 — 실측상 dedupe 후 폴백 통과 행은 0건. 폴백의 실제 가치는 **방어선**(정밀
    매핑이 아직 못 따라간 개편 신코드가 *진짜 별개 시·군*으로 들어오는 미래 케이스 — 규칙 ③·단일행 그룹이 받는다).
  - ⚠️ **입도 중복 제거 필수**: 방문자 데이터에 **시 상위 코드(41110 수원시)와 구 하위 코드(41111 장안구…)가 동시 존재**
    (같은 인구 이중 계상 — 경기 ×1.544·충북 ×1.455·전북 ×1.331·경남 ×1.330·충남 ×1.294·경북 ×1.164 부풀림 → 충북처럼
    ⚖️가 의도와 반대로 눌리는 시·도 발생). `areaWeights` 합산 **전에** 법정동 **앞 4자리 그룹** 단위로 제거한다.
    - **채택 규칙 = 정밀 매핑(`LDONG_TO_APP_AREA`) 보유 여부**(✅ 구현 실측으로 확정 — 2026-07-27). 정밀 매핑이 곧 "앱이
      인정하는 시·군·구 정본"이라, ① 그룹에 **정본과 미매핑이 섞이면 정본만** 채택 ② 그룹이 **전부 정본이면 손대지 않음**
      ③ 그룹이 **전부 미매핑일 때만** 행 수 많은 쪽(동수면 세분) 채택. ①이 수원형(정본=구 4개, 중복=상위 41110)과
      화성형(정본=상위 41590, 중복=신설 구 41591~97)의 **반대 방향을 한 규칙으로** 흡수한다.
    - ⚠️ **"행 수 많은 쪽"만으로 판정하면 안 된다**(초안 기각 — 실측 반례): `43740 영동군` + `43745 증평군`은 앞 4자리가
      같지만 **별개 군**(둘 다 정본)이라 1:1 동수가 돼 **한 군의 방문자가 통째로 삭제**된다. 코드 구조·값 비율(0.96~1.16)
      로는 통합시 중복과 구분되지 않고 **정본 보유 여부만** 갈라낸다.
    - 실측 분류(최신일 268행): **입도 혼재 13그룹 · 별개 시군구 1그룹 · 전부 미매핑 0그룹**. 순수부라 단위 테스트로 잠금.
    - **잔여 리스크(기록 — 수정 불요)**: 규칙 ①은 혼재 그룹의 미매핑 행을 전부 버리므로, 미매핑이 중복이 아니라 **정밀
      매핑이 못 따라간 진짜 별개 시·군**이면 그 방문자가 사라진다(초안 규칙이 4374에서 저지른 실패의 거울상). 현재
      데이터에선 0건이고 영향도 가중치 소폭 이동에 그침 — 이 규칙은 무오류가 아니라 **현 데이터에서 우월한 선택**이다.
      `congestionCodes.ts` 재생성 시 분류(13/1/0) 재실측 필요.
- **가중 — 신규 순수부 `lib/scatter.ts`**: `areaWeights(rows, ldongToArea)` → `Map<areaCode, weight>`, **weight = 1/√(시·도 외지인 합)**.
  시·도 극단 실측 **71.1배**(✅ 구현 실측 2026-07-27 — 최대 서울 5,916,964 vs 최소 세종 83,164, 입도 중복 제거 후. 제거 전
  91.1배(경기 7,572,628)는 이중 계상 오염값이었고, 제거 후 경기는 4,903,514 로 서울에 이어 2위가 된다) → √ 완충으로
  확률비 **8.43배**(뽑힐 확률 최상 세종 15.68% ↔ 최하 서울 1.86%). 결측 시·도는 **최근 가용일
  폴백 → 그래도 없으면 분포 중앙값 가중**(과대·탈락 방지 — 결측 지점에선 "적을수록 자주" 단조성이 안 깨지게 계층화). 원시
  역비례·상한 클램프는 시뮬레이션 분포 보고 조정 여지.
- **적용 지점 — drawByType 조합 선택 재구성(scatter=1일 때만)**: 지역 풀(선택∩🍃 좁힘, 무선택=ALL) 위에서. **무선택 ALL 풀의
  지역별 쿼리는 (A)의 lDong 버킷 디스크립터를 그대로 공유**(안 그러면 scatter=1에서 40% 손실 재발 — 같은 헬퍼 사용).
  **`drawSeaside` 패턴** — 후보 배열 복사본에서 `weightedIndex`로 지역 선택 → 그 지역 안 타입 셔플 순회 count → 전 타입 0이면
  그 지역을 **splice 제거** → `while (pool.length > 0)` 반복, 소진 시 EMPTY_POOL. ⚠️ `weightedIndex`는 가중 합≤0이면 **인덱스 0을
  반환**(에러 아님) — "가중치 0으로 재가중" 방식은 소진 감지 실패로 무한루프·0번 쏠림, splice+길이 종료가 정답.
  **순수부 추출**: 선택·소진 순서 로직을 scatter.ts 순수 함수로(결정적 rand 주입) — drawByType은 소비만("순수 함수만 단위 테스트"
  정책 준수, tourapi 통합 테스트 없음). scatter=0이면 (A)의 균등 경로 그대로.
  - **미적용 경로**: 🌊 바다(이미 count 가중 — 의미 충돌) · 📍 near · 🔭 emptySpot(§7.11 — 셀 균등). 1단계 범위 명기.
- **파라미터·방출 계약**: `scatter=1`(`DrawParams.scatter`). `buildRandomQuery`에 opts.scatter 추가 — 조건 모드 + ON일 때 방출,
  **seaside ON이면 미방출**(서버 무시 파라미터를 URL에서 빼는 types 관례 동형). **count 쿼리 미포함**: FilterPanel의 countQuery는
  scatter 옵션을 **전달하지 않는다**(호출부 계약 명문화 — "뽑기·count 쿼리 일치" 관례의 첫 의도적 예외, lib/query.ts 주석 갱신).
  테스트: **같은 상태에서 draw 쿼리 ⊇ count 쿼리, 차집합 ⊆ {scatter}**.
- **폴백·notice**: `getVisitorRecent` 실패·빈 테이블 → **균등 폴백 + notice**(문구 ② — 조용한 저하 금지). 판정 위치는
  **drawRandom의 🍃 단계 다음**(notices 접근 가능 지점), weights를 drawByType **인자로** 전달.
- **UI**: FilterPanel "추가 조건" 그리드와 **분리된 줄**(필터 아님을 시각 분리), `ExtraToggle` 재사용. **"순수 랜덤" 문구 금지**
  (🎰 ModeToggle과 충돌) — 문구 확정 ①. `clearFilters`는 scatter도 기본(OFF)으로 리셋 + `hasAny`에 scatter ON 포함(📅 날짜 칩과
  동일 취급 — 기본 OFF라 리셋=끄기로 자연스러움).
- **결과 카드**: ON으로 뽑힌 결과에 캡션 "⚖️ 방문자 적은 시·도 가중으로 뽑음"(`PickedInfo` 표식 필드 — types/tour.ts + shape
  테스트 갱신) — 근거 없는 분포 변경이 안 보이는 것 방지.
- **시뮬레이션(심사 데모 — 산출물은 테스트)**: 시드 고정 LCG로 areaWeights×선택 순수부 **1,000회 분포 스냅샷**(vitest) — 차트
  재활용 시 라벨 **"가중 확률(설계치)"** 필수(실측 사칭 금지). 실측은 검증 기준 ③(로컬 scatter=1 연타 분포)으로 별도.
- **문구 확정**:

| # | 표면 | 문구 |
|---|---|---|
| ① | 토글 | **⚖️ 분산 모드** — "방문자 적은 시·도가 더 자주 나와요 (약 1~2개월 전 공공 방문자 집계 기준)" / OFF 설명 "끄면 17개 시·도 같은 확률" *(정정 2026-07-27: 상류 지연 실측 ~47일이라 "약 1개월 전"은 낙관적)* |
| ② | notice(폴백) | 방문자 데이터를 못 불러와 이번엔 모든 시·도 같은 확률로 뽑았어요. |
| ③ | 결과 카드 캡션 | ⚖️ 방문자 적은 시·도 가중으로 뽑음 |

- **테스트 (TDD)**: `scatter.test.ts`(√ 완충 수치·91배→~9.5배 경계·군위 특례·결측 계층 폴백·전결측·재정규화(부분 풀)·**선택-소진
  순열(결정적 rand)·전 지역 소진 판정**) · `query.test.ts`(scatter 방출/미방출·순수 모드 억제·seaside 억제·**draw⊇count 차집합
  ⊆{scatter}**) · 1,000회 분포 스냅샷 · 입도 중복 제거. (A)는 순수부 변화 없음 — 기존 불변식 테스트 무변으로 회귀 확인.
  *(구현 중 확인: `PickedInfo`를 `toEqual`로 잠그는 shape 테스트는 레포에 부재 — "shape 테스트 갱신" 요구는 대상 없음으로 삭제.)*
- **파일**: `lib/scatter.ts`(+test) · `db/congestion.ts`(getVisitorRecent) · `db/schema.ts`(주석 정정) · `lib/tourapi.ts`(drawByType
  승격·가중 분기) · `lib/query.ts`(+test) · `app/api/random/route.ts` · `components/FilterPanel.tsx` · `app/page.tsx` ·
  `types/tour.ts`
- **호출 예산**: 가중 재료 TourAPI **0콜**(DB 1쿼리/1h 캐시). 뽑기 전형 콜 수 무변(3콜). 워스트(연쇄 빈 지역 소진)의 count 콜은
  **COMBO_BUDGET(34) 준용** 상한.
- **검증 기준**: ① ⚖️ ON/OFF에서 후보 수 배지 동일(무변 불변식) ② (A) 무지역 뽑기 연타에서 시·도 쏠림 완화 확인(로컬 실측)
  ③ 시드 스냅샷("설계치" 라벨) + 로컬 scatter=1 ~200회 실측에서 세종·제주 비율 상승 ④ visitor_daily 비운 상태 → 균등 폴백 +
  notice ⑤ 순수 모드 쿼리 `""` 회귀 없음.

### 6.10 🚫 비여행지 거부 필터

TourAPI 풀을 정제 없이 쓰면 "청소년교육센터"·"○○시립도서관" 같은 **생활·행정 시설**이 여행지로 뽑힌다(실측 2026-07-28: 수련시설 A03020200 70건·도서관 123건 등 **~250건 = 풀 21,250건의 1.2%**). 사용자 조건이 아니라 **풀 위생**이라 토글 없이 항상 적용.

- **2겹 거부** (`lib/placeFilter.ts` 순수부 — `isDrawablePlace`): ① **cat3 6종**(수련시설·도서관·학교·컨벤션센터·영화관·대형서점) ② **제목 키워드**(청소년수련/센터/회관/문화의집/야영장·수련원·교육센터·도서관·체육센터·복지회관·주민센터·평생학습 등). ②가 필요한 이유는 **cat3가 빈 채로 오는 항목이 실재**하기 때문("강남청소년센터"·"곡성군청소년야영장" 실측). 키워드는 **그 단어면 사실상 100% 생활시설인 조합만** — 넓은 패턴은 오폭("서울월드컵경기장"은 통과해야 한다).
- **적용 지점 = `tourapi.pickItemFrom` 한 곳**: 일반·🌊바다·📍주변·🔭빈곳·🧭코스·🦀제철맛집이 전부 지나는 초크포인트라 호출부 수정 0. 기존 `exclude`·`accept`와 **별개 조건**(accept에 합성하면 호출부 전부를 고쳐야 한다). 거부 시 같은 루프에서 재추첨 — 거부율 1.2%라 `MAX_INDEX_TRIES` 3 그대로.
- **미보정**: `countCandidates`는 상류 totalCount 합산이라 **~1% 과다 집계**를 유지한다(배지 근사값 성격 + 보정하려면 전 항목 스캔 필요 — 비용 대비 무의미).

### 6.11 🐕 반려동물 동반 + ♿ 무장애 여행지 (M25)

§6.6 백로그 "🐕 반려동물 동반"·"♿ 무장애 여행지"의 정식 채택(✅ 2026-07-28 사용자 결정 — 묶음). **묶음 근거**: 둘 다 같은 축(대상 한정)이라 설계·테스트가 공유되고, 분산 서사가 "누구에게나 같은 주사위 — 반려인·관광 약자 포함"으로 확장된다(공모전 §14 심사 서사). 요청 파라미터 **`pet=1`** · **`barrierFree=1`**(`parseBool`).

- **소스 (실호출 스파이크 2026-07-28 · 2차까지 완료)**: 둘 다 기존 `TOUR_API_KEY` 그대로.
  - 🐕 **`KorService2/detailPetTour2` — 추가 활용신청 불필요, 즉시 사용 가능**: TourAPI 4.0 GW 개편으로 반려동물 동반여행 정보가 기존 국문 서비스에 **통합**됐다(포털 15101578 오퍼레이션 목록 + 실호출 확인). 실측: 목록 모드(contentId 생략) totalCount **9,972** · §5.5 랜덤 인덱스 트릭 동작(pageNo=5000 → 1건) · 행에 동반정보 필드 동봉 · contentId 단건 조회 가능(동반 불가 항목은 total 0 = **존재 여부가 곧 판정**). ⚠️ **areaCode·contentTypeId·lDongRegnCd 필터 미지원**(INVALID_REQUEST_PARAMETER_ERROR 실측 — 에러 봉투가 `response.header`가 아니라 평평한 `{resultCode:"10"}`인 것도 주의). 독립 `KorPetTourService2`는 게이트웨이에 실존(403)하나 포털에 활용신청 항목이 없다(검색 0건) — 경로 아님.
  - ♿ **`KorWithService2` — 활용신청 완료·승인(2026-07-28)**: 포털 15101897 "한국관광공사_무장애 여행 정보"(약 6만 건, 13 오퍼레이션 — areaBasedList2·locationBasedList2·detailWithTour2·areaBasedSyncList2·ldongCode2 포함). 개발계정 자동승인 · 일 1,000회 · 만료 2028-07-28. **승인 직후에도 게이트웨이 403 잔존 = 반영 지연**(§6.7 전례) — 게이트 ①에서 해소 확인 후 착수.
  - 쿼터: ♿는 활용신청별 독립(뽑기 쿼터 잠식 0). 🐕는 **KorService2 쿼터 공유**(뽑기 1회 2~3콜 — 기존 뽑기와 같은 예산 축, §5.6).
- **축 의미론 — 🌊 동형 '대상 한정 축', 단 🐕는 1단계 전국 전용**:
  - ♿: 지역·테마·☔ 🎪 🦀 🍃(지역 풀 필터)·📅 기준일과 그대로 AND 조합 — 좁혀진 areaPool·contentTypeId가 KorWithService2 쿼리에 그대로 얹힌다.
  - 🐕: 상류가 지역·타입 필터를 안 받으므로 **1단계는 전국 전용** — pet 토글을 켜면 지역·테마 선택 잠금(☔ 📅 잠금 패턴 + 이유 문구: "반려동물 동반 목록은 전국에서 통째로 뽑아요"). 지역 조합은 백로그(후보 경로: contentid 전량 일 1회 배치 적재 후 지역 조인 — 100콜/일 규모. 뽑은 항목 사후 판정 재추첨은 지역 히트율이 낮아 기각 우선). **구현 보강(2026-07-28)**: 🐕 는 지역 풀 필터(☔ 🎪 🦀 🍃)도 적용하지 않는다 — UI 잠금 대상은 지역·테마뿐이라 그 토글들은 켜진 채로 도착할 수 있고, 그냥 무시하면 조용한 의미 변경이 된다. 그래서 `drawPet` 이 **켜진 채 무시된 조건을 notice 로 명시**한다(§6.5 soft-skip 안내 관례 재사용, 토글을 더 잠그지 않는 최소 대응).
  - ⚖️ 분산은 둘 다 1단계 미적용(🌊·📍·🔭 관례 — §6.9B).
- **상호배타 — 대상 축(🌊·🐕·♿)은 동시 1개**: 교집합(동반 가능 ∩ 무장애 등)은 contentId 크로스 조인(양쪽 전량 페이지네이션)이 필요해 1단계 기각(§11.1 백로그로 명시 분리). 전이 규칙은 ☔ 📅 패턴(§6.8) 재사용 — 하나 켜면 나머지 두 토글 **잠금**(잠긴 토글은 눌리지 않은 것으로 취급) + 이유 설명형 문구, `buildRandomQuery`는 켜진 하나만 방출(표시·전송·서버 일치). **직접 URL로 둘 이상 동시 도착 시 `BAD_REQUEST`**(무시+notice가 아닌 400 — 어느 쪽을 무시해도 의미가 조용히 바뀌므로 명시 거부).
- **뽑기 경로 — 두 갈래**:
  - ♿ **`drawByType` 재사용(서비스 base 파라미터화)**: `tourFetch`에 서비스 base 인자(기본 `KorService2`)를 추가하고 조합 생성(`buildDrawCombos`)·순회·`pickItemFrom`(중복 방지·비여행지 거부 §6.10 초크포인트)·타임아웃(§6.5)·서버 1회 재추첨을 전부 재사용. 뽑기 1회 콜 수 현행 동일(count 24h 캐시 + item 1콜 · URL이 서비스별로 갈려 캐시 자동 분리). **무지역 버킷 확정(게이트 ③ 해소 2026-07-28)**: `lDongRegnCd` **지원 실측**(51→1,602 · 세종 36110→60) → 기존 lDong 버킷·`buildDrawCombos` **그대로 재사용**(§6.9A 균등 유지, 폴백 불필요). 전국 단일 쿼리는 쏠림이라 기각.
  - 🐕 **신규 소경로(2~3콜)**: detailPetTour2 totalCount(24h 캐시) → 랜덤 pageNo 1건(**동반 칩 필드 동봉**) → 그 contentid로 `detailCommon2` 1건 — 카드 기본정보(제목·주소·이미지·좌표·타입) + **개요 동봉**. **§5.6 개요 지연 로드의 예외**: 카드 구성에 detailCommon2가 어차피 필수라 개요가 공짜로 온다 — `/api/overview` 왕복 불필요. 귀속은 areacode 공백 실측(표본 "마더피아": areacode="" + lDongRegnCd=11) → `itemAreaCode` lDong 폴백 재사용. §6.10 비여행지 거부·중복 방지는 같은 재추첨 루프에 태운다(풀에 용품점 등 type 38·39 상업시설 실재 — 거부 필터가 실질 방어선).
- **장애 의미론 — soft-skip 불가**: ☔ 🎪는 필터라 건너뛰고 결과를 살리지만(§6.5), 대상 축은 **풀 자체가 그 서비스**라 소스 장애 = 뽑기 실패(`UPSTREAM_ERROR` → 기존 서버 1회 재추첨·"다시 뽑기" 유도). **일반 풀로의 조용한 대체는 금지** — 근거 없이 "동반 가능/무장애"를 주장하게 된다(§7.9 허위 서사 금지와 동일 원칙).
- **배지**:
  - 🐕 **뽑기 응답 동봉(지연 로드 불필요)** — 칩 필드 실측 확정: `acmpyTypeCd`(동반 유형 — "전구역 동반가능" 등) · `acmpyPsblCpam`(동반 가능 동물) · `acmpyNeedMtr`(동반 시 필요사항) · `etcAcmpyInfo`(기타 안내). `PickedInfo.pet`에 정규화해 싣는다(순수부 TDD).
  - ♿ **정적 + 지연 2단**: 정적 배지("무장애 정보 보유", 추가 콜 0 — 소스 자체가 근거) + `/api/overview`를 `detail=with`로 확장해 `detailWithTour2` 편의시설 칩 지연 로드(contentId별 24h 캐시·best-effort — `getOverview` 동형). **필드 매핑 확정(실측 2026-07-28)**: contentid 제외 **28종**을 `lib/barrierFree.ts`의 `WITH_FACILITY_LABELS`에 한글 라벨로 고정(키 순서 = 칩 표시 순서 — 이동·접근 우선). 항목마다 **채워진 필드만** 오므로 없는 시설은 "없음"이 아니라 **정보 없음**이다(있는 것만 노출). 값 꼬리의 분류 꼬리표(`…_무장애 편의시설`)는 라벨과 중복이라 잘라낸다.
- **후보 수 배지 — 정확 집계**: 🐕 = detailPetTour2 totalCount 1콜(**9,972 실측** · 24h 캐시 — 전국 전용이라 조건 무관 단일 값). ♿ = `planCandidateCount`에 대상 축(source) 추가 — 조합 계획 재사용, `countCandidates`가 endpoint만 갈아 낀다. ☔ 🎪 동시엔 기존대로 dynamic.
- **UI(FilterPanel)**: 추가 조건 토글 5→7종. 아이콘·문구는 designGuide.md 관례(선 아이콘 — 이모지 미사용). 잠금 문구는 이유 설명형(🌊-types 문형), 예: "바다·반려동물·무장애는 서로 다른 목록에서 뽑아요 — 한 번에 하나만 골라요."
- **테스트(TDD)**: `query.test.ts`(pet·barrierFree 방출/상호배타/pet 시 지역·테마 미방출) · `candidateCount.test.ts`(source 축·dynamic 분기) · pet 응답 정규화 순수부 · with 칩 정규화(게이트 후) · 기존 `PickedInfo` shape 테스트 갱신 · 라우트 400(동시 도착) 케이스.
- **손대는 파일**: 기존 — `lib/tourapi.ts`(tourFetch base·pet 소경로·PickedInfo) · `lib/query.ts` · `lib/candidateCount.ts` · `types/tour.ts` · `app/api/random/route.ts` · `app/api/random/count/route.ts` · `app/api/overview/route.ts` · `hooks/useOverview.ts` · `components/FilterPanel.tsx` · `components/ResultCard.tsx`. 신규 — pet/with 응답 정규화 순수부(+테스트).
- **1단계 범위 밖(백로그)**: 🐕 지역·테마 조합(배치 적재 경로) · 📍 주변·🧭 코스·🔭 빈 곳과의 조합(with에도 `locationBasedList2`가 있어 확장 여지) · 대상 축 교집합 · ⚖️ 적용.
- **게이트 — 전부 해소(2026-07-28 실측)**: ① `KorWithService2` 403 해소(승인 반영 확인) ② 전국 totalCount **10,188** · 타입별 12=3,148 / 14=1,642 / 15=5 / 25=0 / 28=279 / 32=1,062 / 38=1,567 / 39=2,485 — 0건 타입은 기존 조합 루프가 자연 스킵하므로 **테마 칩 조정 불필요** ③ `lDongRegnCd` 지원 → lDong 버킷 그대로(위 참조) ④ `TourApiItem` 필드 호환(표본 50: 좌표 100%·이미지 62%·areacode 공백 44% → `itemAreaCode` lDong 폴백이 전제) ⑤ `detailWithTour2` 필드 28종 → 칩 매핑 확정(위 참조) ⑥ contentId가 KorService2와 **공유** → 기존 `/api/overview` 개요 경로 무변 ⑦ 표본에 §6.10 거부 대상 없음.
- **🐕 풀의 타입 편중 → 관광지성 우선 + 카피 (✅ 2026-07-28 사용자 결정)**: 상류 `detailPetTour2` 풀 자체가 "반려동물 동반 가능한 **장소**"(매장·카페·숙소 포함) 목록이라, 설계가 방어선으로 지목한 §6.10 `isDrawablePlace`(생활·행정 시설 거부)는 **일반 소매 체인을 걸러내지 못했다**(구현 후 표본 12건: 쇼핑 9·음식점 2·숙박 1, **관광지성 0**. 올리브영·지오다노·안경점 실측). 대응은 **소프트 편향 + 정직한 카피**:
  - **관광지성 우선 수용(하드 제외 아님)**: `detailCommon2` 조인 뒤 `contenttypeid`가 12·14·25·28이면 즉시 수용, 32·38·39면 재추첨하되 **마지막 후보를 보관했다가 예산 소진 시 그대로 수용**한다(`pickMealItem`의 "완벽주의 금지" 전례 — 빈 결과보다 상업 항목이 낫다). 순수부 `pickPreferred`(TDD).
  - **예산 `PET_DRAW_TRIES = 4`**: 시도 1회당 상류 2콜(목록+조인)이라 워스트 8콜. 관광지성 비율이 낮아 3회로는 편향이 거의 안 걸리고 5회 이상은 지연이 눈에 띈다. **실측(2026-07-28, dev)**: 관광지성 **0/12 → 3/12**, 응답 중앙값 ≈ 3.8s(웜업 후 소진 케이스 2.7~4.1s. 첫 3건 7~22s는 dev 컴파일 웜업).
  - **기각**: 타입 하드 제외 — 상류가 `contentTypeId` 필터를 안 받아 사후 재추첨만 가능한데 관광지성 비율이 낮아 빈 결과가 잦아진다. 조용한 카피 유지 — 풀 성격을 숨기면 §7.9 허위 서사 금지에 걸린다.
  - **카피**: 토글 "반려동물과 함께 / 함께 갈 수 있는 명소·카페·숙소·매장 (전국)", 잠금 문구에 "명소를 먼저 보여드리지만 카페·숙소·매장도 함께 나와요", 결과 배지 "반려동물 동반 **가능**"(여행지라는 주장이 아니라 함께 갈 수 있다는 사실만).
- **검증 기준**: 검증 세트(tsc·eslint·vitest·build) + ① ♿ 단독 뽑기 — 정적 배지·지연 칩·후보 수 숫자 ② ♿+지역+🍃 조합 — AND 동작·배지 동반 ③ 🐕 — 전국 랜덤·동반 칩·지역/테마 잠금 전이 ④ 상호배타 전이(잠금·복원) + 직접 URL 동시 도착 400 ⑤ 소스 장애 시 실패 안내(조용한 대체 없음 확인).

## 7. 내 여행 지도 & 경험 계층 (방문 마크·공유·서사)

> **배경 조사(2026-07-03)**: 카카오맵·네이버지도·구글맵 **3사 모두 "사용자 저장목록(즐겨찾기)에 쓰기" 공개 API가 없다**
> (카카오 공식 답변 "즐겨찾기는 API 미제공" / 네이버 NCP Maps에 저장 API 부재 / 구글 2017년부터 기능 요청 미해결).
> → "지도 앱에 자동 마크"는 불가능. 아래 3단 구성으로 같은 목적을 이룬다.

### 7.1 ⭐ 내 여행 지도 — 자체 임베드 (정식 기능)

- **동작**: 결과 카드·기록에서 **"다녀왔어요 ✔"** 체크 → 방문 목록(localStorage, 좌표 포함) → "내 여행 지도" 화면에서 **카카오맵 JS SDK** 임베드에 마커로 누적 표시.
- **SDK 선택**: 카카오맵 JavaScript API — **일 30만 건 무료**로 3사 중 여유 최대(네이버 NCP는 결제수단 등록 필요, 구글은 월 1만 로드), 국내 POI 품질 우수. 카카오디벨로퍼스에서 JavaScript 키 발급 + 사이트 도메인 등록.
  - ⚠️ **2026-07-21 무료 쿼터 정책 변경 예고**(계정당 첫 활성화 앱 1개 한정 등) — 구현 시점에 최신 공지 확인.
- **키 취급**: JS SDK 키는 브라우저 노출이 전제인 키(도메인 제한으로 보호) — TourAPI 서비스키와 달리 `NEXT_PUBLIC_` 허용.
- **시너지**: §12 수익 모델의 방문 인증(`visited`) 이벤트와 정확히 맞물림 — 지도에 핀이 쌓이는 재미가 방문 체크의 동기가 되어, P1 로깅 도입 시 이 체크가 곧 `visited` 이벤트가 된다.

### 7.2 딥링크 저장 유도 (반자동)

결과 카드의 "지도 앱에서 열기" 버튼 → 해당 앱에서 열리면 사용자가 직접 ★저장 한 번 탭. 3사 모두 공식 경로 확인됨:

| 앱 | 링크 | 비고 |
|---|---|---|
| 카카오맵 | `kakaomap://place?id={장소ID}` / 웹 `map.kakao.com/link/map/{id}` | 공식 URL 스킴 문서 존재 |
| 네이버지도 | `nmap://place?lat=..&lng=..&name=..&appname={식별자}` | `appname` 필수 · 미설치 시 스토어 유도 |
| 구글맵 | `https://www.google.com/maps/search/?api=1&query=위도,경도` | API 키 불필요 · 앱/웹 자동 분기 |

- TourAPI 응답의 좌표(`mapx`/`mapy`)·장소명으로 링크 생성. 카카오 장소ID는 TourAPI와 체계가 달라 **좌표 기반 링크가 기본**.
- **PC/모바일 분기**: `kakaomap://`·`nmap://` 앱 스킴은 PC 브라우저에서 무반응 → 앱 스킴은 **모바일에서만**, PC에선 웹 URL(카카오 `map.kakao.com/link/…`, 네이버 지도 웹 검색 URL)로 폴백. 구글맵 URLs는 자체 자동 분기라 그대로 사용.

### 7.3 KML 내보내기 (백로그)

방문 목록 → KML 파일 다운로드 → 사용자가 **구글 My Maps에 수동 임포트**(공식 지원, 레이어당 2,000행 제한). 임포트 자동화 API는 없음 — 파워유저용이라 수요 확인 후 구현.

### 7.4 🧩 전국 정복 지도 (M12, 정식 기능)

- **동작**: `/map`에 **[📍 핀 지도 ↔ 🧩 정복 지도]** 토글 추가. 정복 지도는 대한민국 **250개 시·군·구**를 실제 윤곽 SVG 조각으로 그리고, 다녀온 곳을 **1곳 이상 방문한 시·군·구를 색(indigo)으로 채운다**. 안 가본 곳은 회색 빈 조각. 방향 잡기용 **시·도 굵은 외곽선** 오버레이. 상단 **"N / 250 · xx%" 게이지**, 하단 **시·도별 진행 칩**("서울 3/25" 식).
  - **단위 = 시·군·구 이유**: 시·도(17)는 너무 굵어 17번이면 100%라 "완주"의 무게가 안 산다. 시·군·구(약 250)면 완주 바가 유의미해진다(2026-07-05 사용자 피드백).
- **정복 판정 = 좌표 점-다각형**: 방문 데이터엔 시·군·구 코드가 없다(시·도 `areaCode`만). 그래서 방문 **좌표(lat/lng)를 지도 평면으로 투영**한 뒤 **어느 시·군·구 다각형 안에 있는지 even-odd ray casting**으로 판정한다. → **스키마 변경 0, 기존 방문 기록도 소급 반영**. 좌표 없는 방문만 제외(대부분 TourAPI 좌표 있음). **추가 API·키 불필요**(카카오 키 없이도 동작, 순수 SVG).
- **핵심 설계**: 윤곽은 손으로 못 옮기는 산출물이라 M11과 같은 원칙 — **생성기로 재현**(`scripts/genKoreaMap.mjs` → `lib/koreaMap.ts`). 통계청 파생 시·군·구/시·도 경계 GeoJSON(southkorea-maps, public domain)을 받아 작은 섬 제거 + Douglas-Peucker 단순화 + 위도 보정 등거리 투영. 시·군·구·시·도를 **같은 투영**으로 겹치게 방출하고, 판정용 **투영 파라미터(`KOREA_PROJECTION`)**도 함께 내보내 런타임 판정과 렌더가 같은 평면을 쓰게 한다. ⚠️ 통계청 code(앞2자리)와 **TourAPI areaCode는 35/36/37/38이 뒤바뀌어** 있어 `PREFIX_TO_AREA`로 매핑.
- **순수 로직**: `lib/conquer.ts`(TDD) — `projectLatLng`·`pointInRings`(even-odd)·`sigunguAt`(bbox 프리필터)·`conqueredSigunguCodes`·`conquerStats`(개수/250/퍼센트)·`conquerByArea`(시·도별 진행). 렌더는 `components/ConquerMap.tsx` — 시·군·구 데이터(약 200KB)라 **정복 뷰 전환 시에만 `next/dynamic` 지연 로드**(핀 뷰 기본 로드는 가볍게).
- **경계 갭·해안 스냅 폴백(2026-07-05)**: 두 가지 무성 미스를 함께 구제한다. ① 인접 시·군·구 경계를 각자 독립 단순화(DP)해 생기는 서브픽셀 갭. ② 해안선 단순화(EPS ~450m)·소도서 제거가 **해안·반도 관광지 좌표를 폴리곤 밖으로** 밀어냄(실측: 인천 월미도 관광지가 중구 경계 밖 0.04px → 첫 릴리스에서 정복이 0으로 샜음, 사용자 제보). 관광지 좌표는 늘 육지·해안이라, 정확 판정 실패 시 **가장 가까운 시·군·구 경계까지 거리 ≤ 12px(~7.5km)면 그 조각으로 스냅**한다(초기 8방향 nudge는 뾰족한 돌출부를 못 잡아 교체). 실측상 해안 미스 ~0.04px, 먼바다(동해 한가운데) ~103px라 그 사이 임계로 해안·소도서는 구제하되 먼바다는 배제. 회귀 테스트로 월미도 좌표를 못 박음.
- **알려진 한계**: `KEEP_RATIO`로 잘린 부속 도서(예: **독도**)는 투영 bbox 밖이라 판정 불가 — 넣으면 지도가 동쪽으로 크게 늘어나 구성이 망가져 **의도적으로 감수**(독도는 사실상 랜덤여행 POI 아님). 근본 해결(topojson 위상 보존)은 현 범위 밖.
- **시너지**: 백로그 "🏴 전국 정복률"의 시각화 승격. 조각이 채워지는 재미가 **"다녀왔어요 ✔" 체크 동기**를 강화 — §12 `visited` 이벤트와 맞물림(§7.1과 동일 취지).

### 7.5 💬 뽑기 결과 카톡 공유 (M13)

- **동작**: 결과 카드에 "💬 카카오톡 공유" 버튼. 누르면 카카오 JS SDK `Kakao.Share.sendDefault`(feed 기본 템플릿)로 **채팅에 사진+제목+주소 카드**가 꽂힌다. 카드/"지도에서 보기" 버튼은 그 장소의 **카카오맵 링크**(§7.2 `lib/mapLink` 재사용), "나도 뽑기" 버튼은 앱 홈. **새 라우트 없이** 구현(사용자 결정).
- **왜 시·도 SDK와 별개**: 지도 SDK(`dapi.kakao.com`, `window.kakao.maps`)와 카카오 JS SDK(`t1.kakaocdn.net/.../kakao.min.js`, `window.Kakao`)는 **별개 스크립트·별개 전역**이라 공존한다. 공유는 후자 필요 → 첫 클릭에 1회 지연 로드(SRI integrity 검증) + `Kakao.init(NEXT_PUBLIC_KAKAO_MAP_KEY)`. **같은 JavaScript 키 재사용**, 콘솔 [제품 설정 > 카카오톡 공유]만 활성이면 됨(sendDefault 기본 템플릿이라 메시지 템플릿 등록 불필요).
- **폴백**: SDK 미로드·비카톡 환경 → `navigator.share`(모바일 공유시트, 카톡 포함) → 실패 시 클립보드 복사("링크 복사됐어요" 인라인 안내). 토스트 시스템은 안 만듦.
- **이미지 https**: 카톡 피드 `imageUrl`은 https 필수 → TourAPI 이미지를 http→https 업그레이드(`httpsImage`), 이미지 없으면 앱 아이콘(`/icon-512.png`)로 대체.
- **순수 로직**: `lib/kakaoShare.ts`(TDD) — `buildShareFeed`·`httpsImage`·`shareText`. SDK 배선은 `hooks/useKakaoShare`(useKakaoLoader 싱글턴 패턴 재사용), 렌더는 `components/ResultCard`.
- **시너지**: §11.1 백로그 "결과 공유 URL"의 **선행 구현**(무료 유입 경로). 붙여넣기 링크 언펄링용 `/place/[contentId]`+OG 이미지는 잔여 백로그.

### 7.6 📍 첫 여행지 주변에서 뽑기 (M14)

- **동작**: 순수 랜덤으로 첫 여행지를 받으면 그 결과가 **앵커(여행 거점)** 가 된다. 결과 카드에 **"📍 {첫 여행지} 주변에서 뽑기"** 버튼이 뜨고, 누르면 앵커 좌표 **반경 20km** 안에서 랜덤 1건을 추천한다. "다시 뽑기"(전국 랜덤)는 그대로 — 주변 뽑기는 **별도 버튼**(사용자 결정). 주변 결과 카드엔 **"📍 {앵커}에서 N km"** 거리 배지.
- **앵커 규칙**: 앵커 = 가장 최근 **전국 랜덤 결과**(뽑기/다시뽑기 성공 시 갱신). 주변 뽑기는 앵커를 **바꾸지 않아**, 반복해 눌러도 같은 거점 주변을 계속 탐색한다.
- **범위**: **순수 랜덤 모드 전용**(사용자 결정) — 조건 랜덤(지역·테마·바다·제철·축제·날씨)은 기존 동작 유지, 주변 버튼 미표시. 첫 여행지에 좌표가 없으면 버튼 숨김(폴백 = 그냥 다시 뽑기).
- **서버**: TourAPI **`locationBasedList2`**(요청 `mapX`=경도·`mapY`=위도·`radius`·`contentTypeId`·`arrange="E"`, 응답 `dist`(m)). 기존 `tourFetch`·`pickItemFrom` 재사용, `getTotalCount`/`getItemAt`를 `(endpoint, params)`로 일반화. `RANDOM_DEFAULT_TYPES` 셔플→첫 비어있지 않은 타입에서 랜덤 인덱스 1건(타입 균등). 반경 내 0건이면 `EMPTY_POOL` 안내. **추가 API·키 불필요**(같은 `TOUR_API_KEY`).
- **순수 로직**: `lib/query.ts`(`parseLatLng`·`buildNearbyQuery`, TDD)·`lib/geo.ts`(`formatKm`, TDD). API `?near=위도,경도` → `drawNearby`. 클라이언트 `app/page.tsx`(anchor state), 렌더 `components/ResultCard.tsx`.
- **기록 서랍에서도 진입(후속)**: 결과 카드는 새로고침·시간 경과로 사라지므로, 서랍(찜·최근·다녀옴)의 **좌표 있는** 각 기록 행에도 **📍 버튼**을 달아 그 장소를 앵커로 삼아 주변 뽑기를 걸 수 있게 했다(`components/RecordPanel.tsx`). 이때 앵커는 그 기록으로 바뀐다. 기록에서 주변 뽑기를 하면 조건 모드여도 **거리 결과 카드**엔 이어서 탐색하도록 주변 버튼을 노출(카드 기본 노출은 여전히 순수 모드 전용).

### 7.7 📊 재방문 의향 평가 (M15)

- **동작**: **다녀옴** 탭의 각 기록 행 둘째 줄에 **3단계 재방문 의향** 세그먼트(가로 3분할)를 단다 — **👎 또 안 감**(또 갈 생각 없음) · **😐 그럭저럭**(나쁘지 않았음) · **👍 또 갈래**(또 갈 의향이 있음). 별점의 애매함 대신 "또 갈까?"라는 실제 의사결정 축. 선택된 칸만 색(rose/amber/emerald)으로 채우고, 같은 칸을 재클릭하면 **해제**(미평가). 전체 문구는 `aria-label`.
- **범위·위치(사용자 결정)**: **다녀옴 탭 전용**(찜·최근엔 미표시, 결과 카드도 미표시 — 카드는 깔끔 유지). 좌표 유무와 무관(평가는 반경 검색과 별개).
- **저장·동기화**: 평가는 **방문 장소의 속성**(`SavedPlace.rating?: 1|2|3|null`). localStorage `rt.visited.v1` 항목에 함께 실리고(직렬화 왕복 보존), 로그인 시 **write-through**(POST 업서트가 `rating`까지 갱신)로 기기 간 동기화. DB `user_place`에 nullable `rating` 컬럼 추가(additive — 기존 코드 호환). 병합 충돌 시 `rating`은 **null 아닌 쪽 보존**(coalesce)해 평가 유실 방지.
- **순수 로직(TDD)**: `lib/travelStore.ts`(`setRatingInList`)·`lib/placesApi.ts`(`sanitizePlace`가 1\|2\|3만 통과)·`lib/syncMerge.ts`(rating coalesce). 라벨 상수 `REVISIT_OPTIONS`(`lib/constants.ts`). 클라이언트 `hooks/useTravelStore.ts`(`setRating` write-through), 렌더 `components/RecordPanel.tsx`.
- **v1 한계**: 비로그인 상태로 평가 후 로그인 시, 서버에 이미 있는 장소면 sync 업로드(`onConflictDoNothing`)가 그 평가를 안 올릴 수 있음(기존 "삭제 미전파"와 동류). write-through가 주 경로라 실사용 영향 작음. **후속 후보**: 평가 기반 필터·주변 뽑기 제외·지도 마커 색·rating 이벤트 로깅(§12).

### 7.8 🎨 UI 리디자인 — 탐험 로그 (1C) (M16)

- **동기·방향**: 외부 디자인 핸드오프(`claude-design/`, "탐험 로그/1C")를 **기존 코드 패턴으로 재구현**. **지도를 주인공**으로 삼아 뽑기·결과·정복 현황을 한 흐름에 묶고, "다녀왔어요 → 지도 즉시 반영"의 성취감을 상시 노출한다. **표현(뷰) 계층만** 바꾸고 데이터 계층(`useTravelStore`·`/api/random`·정복 판정·`lib/*`)은 그대로 재사용.
- **비주얼 토큰**: 액센트 **indigo → emerald(#059669)** 전면 전환(`globals.css` focus-visible·`layout.tsx` themeColor 포함). 폰트는 현행 유지(사용자 결정 — PWA 오프라인·번들 단순). 다크모드 지원 유지.
- **홈(`app/page.tsx`)**: 헤더에 **정복 pill(🧩 정복 N/17·P%)** + 상단 **지도 히어로**(새 `components/MapHero` — 정복/핀 토글, 17개 시·도 타일 정복 뷰, 경량 좌표 핀 뷰, **새 시·도 정복 시 🎉 토스트**). 하단 2단: **뽑기 덱**(raised sheet — 모드 토글→조건 패널→큰 뽑기 버튼→결과) + **기록 패널**. → **§7.13에서 재배치**(2026-07-28): 지도 히어로와 뽑기 덱이 좌·우 2열, 기록 패널이 그 아래 전폭.
- **결과 카드(`ResultCard`)**: 사진 위 **오버레이 찜·다녀옴 토글** + **주요 2(다시뽑기·주변) / 보조 4아이콘(지도·길찾기·공유·복사)** 2단 계층.
- **실시간 후보 수 배지(핵심 신규)**: 조건 패널에 **"≈ N곳 후보"**. 현재 앱은 뽑기 전 후보 총합을 몰라 **새 `/api/random/count`** 추가 — 순수 계획 `lib/candidateCount.planCandidateCount`(지역×타입/cat3 조합 계획, TDD) + `lib/tourapi.countCandidates`(조합별 `getTotalCount` **24h 캐시 재사용** 합산). **동적 조건(🎪 축제·☔ 비안옴)은 그날그날 원격 소스라 정확 집계 불가 → `dynamic` 라벨**("조건에 맞는 곳에서" — **예외: 🍃 한적(M17)은 배치 DB 조회라 정확 집계 가능, §6.7**). `COMBO_BUDGET 34` 상한(capped 시 하한 `≈ N곳+`). 상류 장애·키 미설정 시 `dynamic` 폴백(배지가 앱을 막지 않음). 클라이언트 `hooks/useCandidateCount`(400ms 디바운스 + seq 경합 무시).
- **지도(`app/map/page.tsx`)**: 정복 뷰 기본. `ConquerMap`에 **히어로**(정복률 링 + 3통계[시·군·구 N/250·다녀온 곳 M·**발 들인 시·도** R/17] + **탐험가 레벨** 진행바) + 기존 250 시·군·구 SVG + **시·도별 진행 보드**(점 채움). `VisitedMap`은 **실제 카카오맵 유지** + 하단 **다녀온 곳 리스트**(썸네일·재방문 배지·상대날짜·**카카오/네이버 딥링크**). `koreaMap` **지연 로드 유지**(핀 뷰만 볼 땐 200KB 미수신).
- **새 순수 로직(TDD)**: `lib/level.explorerLevel`(정복 시·군·구 수→5단계 🌱→👑) · `lib/relativeDate.relativeDay`(오늘/어제/N일 전/N달 전, now 주입) · `lib/conquer.visitedAreaCodes`(areaCode 기준 **발 들인 시·도** — 홈 pill·히어로·지도 히어로의 **단일 출처**) · `lib/mapLink.naverMapLink`.
- **범위 밖(후속)**: Pretendard 폰트 도입, capped 배지의 스케일 추정, 후보 배지의 동적 조건 정밀 집계.

### 7.9 📣 분산 서사 카피 (M18)

앱의 부수 가치(§1 — 유명지 쏠림을 깨는 시·도 균등 랜덤)가 **UI 문구엔 전혀 드러나지 않는다**("운명에 맡겨"는 재미만 전달). M17로 🍃 근거 배지·데이터가 생겨 **근거 있는 서사**가 가능해진 지금 카피를 일괄 리라이트한다. **뽑기 분포·필터 판정·저장 데이터 무변** — 단 §6.5 설계분(슬롯 최소 노출시간)의 구현과 공유 페이로드 확장(`ShareContext`·`shareText`)을 포함하므로 §7.8류 "표현 계층만"은 아니다.

- **카피 원칙 5** (전 문구 공통):
  1. **알고리즘이 보장하지 않는 주장 금지** — 균등은 **시·도 단위**(§5.5). 금지 예: "전국 250곳 같은 확률"·"1/250"(시·군·구 균등 오주장), 🍃 꺼진 기본 뽑기에 혼잡 회피 약속("붐비지 않는 곳으로" — 명동도 뽑힌다). 허용: "17개 시·도에 같은 주사위"·"같은 출발선"·"유명세는 안 봐요"(인기 무가중은 항상 참). *(정합 주석: 무지역 뽑기는 M22 균등 승격(§6.9A) 전까지 항목 균등이라 "같은 주사위"가 과장이었음 — 승격으로 전 경로 참. ⚖️ ON 뽑기의 주장은 "방문자 적은 시·도 가중"만.)*
  2. 혼잡도 언급은 **'예측' + 출처**(§6.7)와 함께. **외부 노출면(공유 카드)은 기준일 필수 + 이해 가능한 단어 선행** — "🍃 한적 예측 · 집중률 하위 N% (M/D 기준)" 어순('집중률 하위'가 선두면 무맥락 수신자가 '순위 낮은 별로인 곳'으로 오독).
  3. 기존 카피는 **교체보다 확장**(검증된 문구는 뒤에 덧붙임).
  4. 재미 톤 유지 — 캠페인 구호가 아니라 앱 정체성의 자연스러운 표현.
  5. **시점 중립** — 히어로·슬롯 로테이션에 '오늘' 류 특정 시점 표현 금지(§6.8 기준일 축 도입 시 즉시 거짓이 됨).
- **변경 목록**:
  1. **히어로 서브카피**(`app/page.tsx` h1 아래 + README 소개 블록 동기화 — 두 곳 중복): ✅ 문안 확정(2026-07-10 사용자 선택) — **"어디 갈지 고민될 때, 운명에 맡겨 — 전국 어디든 같은 출발선."**(기존 보존+확장, 원칙 3). README 핵심 기능 '확장 조건' 행에 **🍃 추가**(M17 때 놓친 동반 갱신 수습).
  2. **온보딩 배너(신규 1회성)** `components/StoryBanner`: **정체성 1문장만** + 닫기 ×("어디든은 유명한 곳에 쏠리지 않아요 — 전국 모든 시·도에 같은 주사위를 굴려요." 류 — '시·도' 단어 고정). 출처 문장은 배너가 아니라 **FilterPanel 🍃 desc 확장**("한국관광공사 집중률 예측 기반")으로 — 기능을 만난 순간에 출처 제공. 닫으면 `rt.storySeen.v1` 기록 — **기기 단위 플래그: owner 게이트·로그아웃 정리(키 화이트리스트) 비대상**(공용 PC 다음 사용자 미노출은 수용). try/catch 래퍼 + **mounted 게이트**(hydration — M16 관례). 렌더 테스트 부재 영역 — 수동 확인.
  3. **슬롯 최소 노출시간 + 메시지 로테이션**: `runDraw`에 `Promise.all([wrappedFetch, delay(MIN_SPIN≈1.2s)])` — §6.5 설계분의 구현. **에러 포함 모든 결과가 최소 노출을 따른다**(fetch를 `.catch`로 결과값화해 reject 없는 형태 — HTTP 오류/네트워크 거부가 두 속도로 보이는 비대칭 방지). 로테이션은 **2개**(1.2s에 간격 0.9s면 3~4개는 안 읽힘): 후보 "🎰 17개 시·도에 같은 주사위를…" + "🎰 운명을 굴리는 중…"(기존 문구에서 '오늘의' 제거 — 원칙 5), 최종 문안은 §13. 린트: 인터벌 콜백 setState는 set-state-in-effect **비대상** + SlotMachine은 loading 조건 렌더라 재마운트로 인덱스 자연 리셋 — disable 주석 불요(M16 선례 복붙 금지). 순수부 `rotatingIndex(elapsedMs, count)`만 TDD.
  4. **정복 토스트 확장**: "🎉 {시·도} 정복!" → "🎉 {시·도} 정복! · **내 지도 +1**"(1.7s 타이머 무변, 덧붙임 6자 내외). ~~"전국 분산 +1"~~은 기각 — 자가 신고 방문 체크가 전국 단위 분산에 기여했다는 반증 가능한 인과 주장(원칙 1). 참인 수치는 사용자 자신의 지표뿐.
  5. **카톡 공유 서사화**: ① `DEFAULT_DESC` 리라이트 ② **`ShareContext`에 `congestion?: CongestionBadge | null` 추가** — `buildShareFeed` 인자 추가만으론 배선 불가(실호출부는 `useKakaoShare.share` 내부 — ctx 통과라 훅 무변), `ResultCard` onShare에서 `data.picked.congestion` 주입 ③ **폴백 평문 `shareText`에도 동일 1줄**(카카오/Web Share·클립보드/복사 3경로 서사 일치 — 공유는 분산 근거를 실어 나르는 유일한 외부 노출면). 문구는 원칙 2 어순 + **주소 선행·congestion 줄 후행**(카톡 피드 desc 2줄 잘림 시 수신자 실용 정보(주소) 우선 — 실기기 렌더 확인). `kakaoShare.test.ts`: 기존 부분 문자열 assert 무변 — feed·평문 congestion 케이스만 추가.
  6. **manifest·layout description**(두 파일 동일 문자열): "버튼 하나로 오늘 떠날 국내 여행지를 무작위로 뽑아주는 웹앱 — 유명세 대신 전국 어디든 같은 출발선."(기능 선행 + 서사 후행 — 스니펫·설치 다이얼로그에서 잘려도 앞부분 단독 성립). manifest.test는 truthy 검사라 무변.
  7. 무변 확인: FilterPanel 🍃 라벨(desc는 ②에서 출처 확장) · 후보 수 배지 문구 · 에러/폴백 문구.
- **테스트·검증**: 갱신 `kakaoShare.test.ts`(feed·평문 congestion 케이스) · 신규 `rotatingIndex` 순수 테스트. 검증 세트 + 프로덕션: 배너 1회성(닫힘 유지) · 슬롯 최소 노출(성공·에러 경로 모두) · 토스트 · 공유 실기기(카톡 2줄 렌더).
- **진행 순서: M18 → M19 순차**(§6.8 참조 — 둘 다 `app/page.tsx`·FilterPanel 접촉, M19의 신규 문구가 이 카피 원칙의 적용 대상).

### 7.10 🧭 반나절 코스 1단계 (M20)

§11.1 백로그 "🧭 반나절 코스"의 정식 채택. 뽑힌 여행지(현재 결과 카드의 장소)를 **앵커**로, 반경 내에서 **볼거리 → 식사 → 카페** 3스텝을 랜덤으로 채워 반나절 코스 타임라인을 만든다 — 뽑기의 분산 위에 **그 자리에서의 체류를 완성**하는 한 흐름. 코스는 풀을 좁히는 조건(§6)이 아니라 **뽑힌 결과 위에 얹는 경험 계층**(§7.6 주변 뽑기 동형·목적지 앵커형)이라 §7에 둔다 — 코스 뽑기는 §2 조건 파이프라인 밖(M14 `locationBasedList2` 경로와 같은 축). 진행 순서는 M19 뒤(같은 결과 카드·page.tsx 접촉). 스텝별 재뽑기·전체 재생성 지원.

- **스파이크 확정(2026-07-10 실호출)**: ① 카페/전통찻집 cat3 = **`A05020900`**(categoryCode2 실조회 — 음식점(39) 하위 7종: 한식 A05020100 / 서양식 A05020200 / 일식 A05020300 / 중식 A05020400 / 이색음식점 A05020700 / 카페·전통찻집 A05020900 / 클럽 A05021000) ② **locationBasedList2가 cat3 필터를 존중**(서울시청 3km: 39 전체 156 vs cat3 카페 30, 응답 전 항목 일치) ③ **radius 30km도 동작**(20km 상한 아님 — `NEARBY_RADIUS_M` 주석 "최대치"는 낡은 정보, 구현 시 주석만 정정·M14 동작 무변. 30km+ 확대는 §11.1 백로그) ④ 시골 커버리지: 완도 20km 카페 2건 — 0건 슬롯 생략 처리가 실제로 필요.
- **슬롯 정의(상수 `COURSE_SLOTS` — 순수부)**: ① **볼거리** `sight`: 타입 [12 관광지·14 문화시설·28 레포츠](15 축제는 시간성·25 여행코스는 코스 중첩이라 제외) ② **식사** `meal`: 39 전체 ③ **카페** `cafe`: 39 + `cat3=A05020900` 직접 필터. cat3 상수는 `lib/constants.ts`(`CAFE_CAT3`·`MEAL_REJECT_CAT3`=[카페, 클럽] — SEA_CAT3 관례).
- **뽑기 절차 — 부분 병렬 + 반경 바깥 루프**:
  - **`Promise.all([ 볼거리, (식사 → 카페 순차) ])`** — 볼거리(12·14·28)는 39와 타입이 안 겹쳐 병렬 안전, **카페는 식사 확정 후 exclude에 식사 contentId를 넣고 뽑는다**(유일한 충돌쌍 식사↔카페를 구조적으로 차단 — 완전 병렬이면 "이미 확정된 슬롯 제외"가 성립 불가).
  - **반경이 바깥 루프**: `COURSE_RADII = [5000, 10000, 20000]`(m) 각 반경에서 슬롯 시도 — 볼거리는 셔플된 3타입 순회 후 전부 0건일 때만 다음 반경(가까운 볼거리 우선 = 반나절 감성). 슬롯별 **독립 확대**(식당은 5km에 있는데 카페만 없으면 카페만 확대).
  - **식사 슬롯의 카페·클럽 거부**: `drawCourseSlot` 래퍼가 `pickItemFrom(exclude)`을 최대 3회 재호출하되 거부된 카페·클럽 contentId를 **로컬 exclude 사본에 누적**(같은 카페 재뽑힘 방지), 3회째도 거부 cat3면 그 항목 수용(완벽주의 금지 — cat3별 다중 count 콜 없이 1콜 유지).
  - **실패 = 반경 확대**: `pickItemFrom`이 3회 모두 exclude·스테일로 null이면 **totalCount 0과 동일하게 다음 반경으로**, 최대 반경에서도 실패면 — 초기 생성은 **슬롯 생략 + notice**(1스텝 이상이면 코스 성립), 전 슬롯 실패는 `EMPTY_POOL`(404, 문구 아래). 재뽑기는 스텝 유지 + 안내. ~~"totalCount ≤ exclude면 해제"~~ 류 규칙은 두지 않는다 — 코스 exclude는 §5.6 '최근 본 곳'과 달리 **절대 나오면 안 되는 항목**(앵커 자신·교체하려는 현재 스텝)이라 해제가 항상 역효과.
- **방문 순서 = 의미 고정**(앵커 → 볼거리 → 식사 → 카페). 로드맵의 nearest-neighbor 정렬은 **기각** — 슬롯이 3개뿐이고 구경→밥→커피의 시간 서사가 거리 최적화보다 우선(거리순 재배열은 "카페부터 가라"는 코스를 만든다). 다슬롯 확장 시 재검토(§11.1).
- **중복 제외**: `pickItemFrom`에 `exclude?: Set<string>`(contentId) 확장 — 뽑힌 항목이 exclude에 있으면 기존 `MAX_INDEX_TRIES`(3회) 루프 안에서 인덱스 재추첨(기존 호출부 4곳 무변). 초기 생성 exclude = {앵커}(위치 기반 조회는 기준점 자신도 반환 — 앵커 재등장 방지), 카페는 +{식사}. 재뽑기 exclude = 클라가 보낸 {앵커 ∪ 현재 전 스텝}(교체 대상 포함 — 같은 곳 재등장 방지).
- **API — 새 라우트 `GET /api/course`** (`app/api/course/route.ts`):
  - 파라미터: `near=위도,경도`(필수 — `parseLatLng` 재사용, 400) · `exclude=id1,…`(신규 `parseContentIds(raw, 상한 12)` — 숫자 문자열만·중복 제거. 실요구 최대 4(앵커+3스텝)에 다슬롯 확장(§11.1) 여유) · `slot=sight|meal|cafe`(있으면 그 슬롯 1개만 = 재뽑기 경로) · `date=YYYYMMDD`(`parseDateYmd` + 요청당 단일 `now`).
  - 응답: 전체 = `CourseResponse { steps: CourseStep[]; congestion: CongestionBadge | null; notice: string | null }` / 재뽑기 = `{ step: CourseStep; notice: string | null }`. `CourseStep { slot: "sight" | "meal" | "cafe"; place: Place }` — types/tour.ts(관례). 개요(detailCommon2)는 **생략**(타임라인 압축 행에 불필요 — §5.6 예산, `normalizePlace(item, null)`). notice는 기존 관례대로 응답당 단일 문자열(join). 오류 매핑은 `errorResponse` 패턴 복제(404/400/502).
  - 서버 뽑기 로직은 `lib/tourapi.ts`에 `drawCourse`·`drawCourseSlot` 추가(프라이빗 헬퍼 `getTotalCount`·`pickItemFrom` 재사용 — export 확장 없이 동거 관례). **`drawCourse` 시그니처에 날짜 계열 파라미터를 두지 않는다** — "date는 코스 구성 무변" 불변식이 내부 규율이 아니라 **구조로 보장**되게.
- **🍃 코스 헤더 배지 — 라우트 핸들러에서 재조립**: 앵커 좌표 → `sigunguAt` → `getCongestionDay(dateYmd ?? kstYmd(now))` → `congestionStale` → `rankDaily` → `congestionBadge(…, targetYmd)` — 전부 export된 조각(tourapi의 `buildBadges`는 프라이빗이라 재사용 불가, `countCandidates`가 drawRandom 밖에서 같은 호출을 하는 선례). **체인 전체를 try/catch로 감싸 실패·stale·매핑 없음·pctRank>0.5 전부 `congestion: null`로 수렴**(코스는 산다 — 배지 생략이 정상 경로, 허위 서사 금지). 문구는 **주장 범위를 앵커 시·군·구로 고정**: **"🍃 {sigunguName} · {M/D} 한적 예측 · 집중률 하위 N%"**(+ `baseYmd < targetYmd`면 "({M/D} 데이터)" 병기) — 결과 카드 문구에서 sigunguName만 추가: 반경 확대 시 식사·카페가 이웃 시·군·구로 넘어가는 게 일상이라, 지역명 없는 배지는 "코스 전체가 한적"이라는 미보장 주장(§7.9 원칙 1)이 된다. ⚠️ §6.7 노출 조건 중 **"🍃 켠 뽑기" 게이트는 코스에 부적용**(토글 없는 경로) — pctRank≤0.5·비stale·매핑 존재 조건만 유지. 날짜 포맷터는 `fmtYmd`(ResultCard 프라이빗)를 **`lib/kst.ts`로 승격**해 ResultCard·CoursePanel 공용(kakaoShare `mdOf`도 통합 — M/D 포맷터 3중화 방지. 두 구현은 문자 단위 동일이라 기존 테스트 무변).
- **거리 표시 — 스텝 간 다리(leg) + 교통 힌트**: 신규 순수부 `haversineM(lat1, lng1, lat2, lng2)`(`lib/geo.ts` 확장 — 레포에 하버사인 없음) + `courseLegs(points): (number | null)[]`(`lib/course.ts`) — 앵커 포함 인접 스텝 간 직선거리, 좌표 null인 다리만 생략. 표기 `formatKm` 재사용(행 사이 "↓ 1.2km"). 헤더에 총 이동거리 합계 + **합계가 `COURSE_DRIVE_HINT_M`(10km) 초과면 "🚗 차로 이동 기준" 병기**(순수 판정, 경계 테스트) — 반경을 20km로 눌러도 다리 거리는 이론상 40km까지 가능하므로, "반나절"의 정직성은 반경 상한이 아니라 **다리·총거리 표기 + 교통 힌트**로 확보한다. locationBased 응답 `dist`(앵커 기준)는 쓰지 않는다 — 다리 축(이전 스텝 기준)과 달라 혼동 방지, 하버사인으로 축 통일.
- **클라이언트(UI)**:
  - **진입 버튼**: ResultCard에 📍와 같은 패턴의 전폭 버튼 **"🕒 반나절 코스 만들기"**(`onOpenCourse: (() => void) | null` — null이면 미렌더). 이모지 🕒 확정 — 🧭는 카드 보조 타일 '길찾기'와 충돌, 🚶는 차량 코스(시골)와 모순(문서·마일스톤 표기는 🧭 유지). 게이트 = **현재 결과 place의 좌표 유무만**(RecordPanel 관례 — ⚠️ M14 `canDrawNearby`의 mode 게이트를 복사하지 않는다: 코스는 필터와 독립, 순수/조건 모드 무관). 앵커 = **`status.data.place`**(M14 anchor state 아님 — 주변 뽑기 후 anchor는 옛 장소라 축이 다름). **버튼은 코스 생성 후에도 잔존, 재클릭 = 전체 재생성**(exclude는 {앵커}만으로 재시작, 로딩 중 disabled — 연타 방지).
  - **CoursePanel(신규, 표시 전용)**: 결과 aria-live 컨테이너 **밖** 바로 뒤(section 안)에 렌더, 게이트 `status.kind === "ok" && course.kind !== "idle"` — 로딩(SlotMachine) 동안 옛 코스가 보이는 것·통째 낭독을 함께 방지. **패널 안에 sr-only polite live region 1개**(컨테이너 밖이라 중첩 아님): "반나절 코스가 준비됐어요" / "{슬롯 라벨}을 다시 뽑았어요" / 에러 문구만 통지. 타임라인: 헤더 **"{앵커명}에서의 반나절 코스"**(시점 표현 금지 — §7.9 원칙 5) + 🍃 배지·총거리 → 앵커 행 **"출발 · {앵커명}"**(고정) → 스텝 행(슬롯 라벨 🏞️ 볼거리/🍚 식사/☕ 카페 + 장소명·주소(line-clamp-1) + 카카오맵 링크(`lib/mapLink` 재사용) + **↻ 재뽑기 버튼**(🎲는 덱 '다시 뽑기'와 이중 의미라 회피, `aria-label="{슬롯 라벨} 다시 뽑기"`, 터치 타깃 44px)) + 행 사이 "↓ N.Nkm". 재뽑기 중 그 행만 스피너·버튼 disabled. 에러 상태 = amber 톤 행(ErrorPanel 관례 축소판) + "다시 시도" 버튼.
  - **상태 소유 = page.tsx**(관례 — 패널은 콜백 주입): `courseStatus`(idle/loading/ok/error) + **`courseTokenRef` 경합 가드**(drawTokenRef 동형 — 코스 fetch는 별도 흐름). **리셋은 runDraw 시작부**(setStatus(loading) 직후) 2줄: `courseTokenRef.current++`(진행 중 코스 fetch 무효화 — 코스↔뽑기 교차 경합 차단) + `setCourse(idle)` — 커밋 클로저(성공·HTTP 에러·네트워크 3종)에 각각 넣는 방식은 로딩 중 stale 코스 노출·에러 경로 누락이 생겨 기각.
  - **date 스레딩**: 신규 `buildCourseQuery(lat, lng, opts: { slot?; exclude?; dateYmd?; todayYmd? })`(`lib/query.ts`) — date는 **미래만 방출**(buildRandomQuery 동형). date는 🍃 헤더 배지 기준일만 바꾸고 **코스 구성 무변**(§6.8과 같은 축).
- **문구 확정**: 전 슬롯 실패 EMPTY_POOL = "주변에서 코스를 만들 만한 곳을 찾지 못했어요. 다른 여행지를 뽑아 다시 시도해 보세요." · 슬롯 생략 notice = "주변에서 카페를 찾지 못해 마무리 스텝은 뺐어요." 류(슬롯 생략 사유 명시) · 재뽑기 실패 = "주변에서 새로 보여드릴 곳을 찾지 못했어요."(존댓말 + 행동 제안 문형 통일).
- **저장·공유는 범위 밖**(§11.1 백로그): 코스 저장(rt.courses.v1)·코스 카톡 공유·스텝 다녀옴/찜 연동·지도 경로 표시. 1단계는 "만들고 → 재뽑고 → 지도 링크로 여는" 흐름까지.
- **테스트(TDD)**: 신규 — `geo.test.ts`에 `haversineM`(서울-부산 ≈ 325km·동일점 0·경계) · `course.test.ts`(`COURSE_SLOTS` shape · `courseLegs` null 다리·합계 · 🚗 힌트 경계 10km) · `query.test.ts`에 `parseContentIds`(형식·중복·상한) + `buildCourseQuery`(slot/exclude 방출·date 미래만·오늘 생략) · `kst.test.ts`에 `fmtYmd`(M/D·앞 0 제거·형식 이상 원문 — lib 승격 동반). 서버 뽑기부(drawCourse)는 네트워크라 단위 테스트 비대상(drawNearby 동형). 기존 테스트 무변 확인(fmtYmd·mdOf 통합은 문자 단위 동일).
- **손대는 파일**: 신규 — `lib/course.ts`(+테스트) · `components/CoursePanel.tsx` · `app/api/course/route.ts`. 기존 — `lib/geo.ts`(haversineM) · `lib/kst.ts`(fmtYmd 승격) · `lib/constants.ts`(CAFE_CAT3·MEAL_REJECT_CAT3·COURSE_RADII·COURSE_DRIVE_HINT_M·NEARBY_RADIUS_M 주석 정정) · `lib/query.ts`(parseContentIds·buildCourseQuery) · `lib/tourapi.ts`(pickItemFrom exclude·drawCourse·drawCourseSlot) · `lib/kakaoShare.ts`(mdOf→fmtYmd) · `components/ResultCard.tsx`(코스 버튼·fmtYmd import) · `app/page.tsx`(courseStatus·리셋·배선) · `types/tour.ts`(CourseStep·CourseResponse).
- **호출 예산(§5.6)**: 코스 1회 = 슬롯 3 × (count 1 + item 1) = **전형 6콜**(개요 생략). 워스트: count 15콜(볼거리 3타입×3반경 9 + 식사 3 + 카페 3 — 동일 앵커 재시도는 24h 캐시 히트) + 식사 item 최대 9콜(카페 거부 재호출 3 × 내부 인덱스 3, 전형 1콜). 재뽑기 = 슬롯당 1~2콜(count는 같은 좌표·반경·타입이면 캐시 히트). 연타 방지는 로딩 중 disabled로 충분.
- **검증 기준**: 검증 세트 + ① 도시 앵커(서울권) — 3스텝·다리 거리·카카오맵 링크 ② 시골 앵커(완도권) — 카페 슬롯 생략 + notice, 장거리 코스에서 🚗 힌트 표시 ③ 스텝 재뽑기 — 코스 내 중복 없음·그 행만 로딩·↻ 라벨 낭독 ④ 🍃 헤더 배지 — 한적 시·군·구 앵커에서 **sigunguName 포함** 표시, pctRank>0.5·미매핑에서 생략 ⑤ 미래 날짜 칩 선택 후 코스 — 구성 무변·배지 날짜만 전환 ⑥ 좌표 없는 결과에서 버튼 미렌더 ⑦ 전체 재생성 — 버튼 재클릭 시 새 코스·로딩 중 disabled.

### 7.11 🔭 빈 곳에서 뽑기 (M21)

> 정복 지도의 빈 조각(미방문 시·군·구) ∩ 한적 예측 시·군·구에서 다음 여행지를 뽑는다 — **"지도에 없던, 한적한 동네."**
> 미방문 단독이 아니라 한적 교집합인 이유: "안 가본 곳" 자체는 분산과 무관하다는 반박을 교집합으로 봉쇄.
> (이모지: 로드맵의 🧭는 M20 반나절 코스와 충돌 → 🔭로 확정 — M20의 🧭→🕒 버튼 전례와 동일.)

- **스파이크 확정 (2026-07-10 실측)**:
  - **법정동 파라미터(`lDongRegnCd`/`lDongSignguCd`) 기각** — 3자리 분할 형식(`11`+`110`)으로 동작하지만 행정구역 개편 **신체계 혼재** 실측: 완도군이 구 법정동 46890이 아닌 `12`/`850`(전남광주통합 신체계), 세종은 두 필드 모두 5자리(형식 불일치). 기존 `CONGESTION_SIGUNGU_CODES`(구 법정동)와 조인이 개편 지역에서 깨짐 → 재생성은 백로그(§11.1).
  - **재래식 `areaCode`+`sigunguCode` 채택** — 개편 지역 포함 동작(완도 38+18 → 타입12 34건). 단 신체계(76건) 대비 커버리지 절반 실측 — 풀 다양성 감소는 수용, 정합성은 아래 좌표 검증이 보장.
  - **granularity 불일치 → N:1 셀** — `areaCode2` 코드표는 17시·도 합계 **234개(시 단위)** vs 앱 `KOREA_SIGUNGU` 250개(구 단위: 고양시덕양구 등). 고양시(1코드) ← 앱 3구 같은 N:1을 "셀"로 묶는다.
- **셀 매핑 — `lib/tourSigungu.ts`(생성물) + `scripts/genTourSigungu.mjs`**: `areaCode2` 17콜 → `KOREA_SIGUNGU`와 (area+이름 접두) 조인. **특례는 §6.7 확정 4건을 시드로 선탑재**(미추홀 · 세종 "세종시"↔"세종특별자치시" · 부천 · 군위 — TourAPI는 대구(4) 소속 실측: 대구 9엔트리). 산출물 `TOUR_SIGUNGU_CELLS: { area, sigunguCode, name, members: string[] }[]`(members=통계청 code) — **assert: 250 전수가 정확히 한 셀 소속**, 실패 시 **미매칭 전수 목록을 한 번에 출력** 후 throw. 체크인 산출물엔 **상시 vitest**(네트워크 0): members 합집합=250 전수·중복 0. ⚠️ 코드값 직결 금지 원칙 유지(TourAPI sigunguCode ↔ 통계청 ↔ 법정동은 서로 다른 3체계 — 다리는 항상 (area+이름) 조인 산출물).
- **풀 정의 — 신규 순수부 `lib/emptySpot.ts`**:
  - `QUIET_SIGUNGU_CUT = 0.5` — 배지 컷(congestionBadge의 0.5)을 상수로 승격해 공유(`lib/congestion.ts`). 🔭의 "한적" = 배지의 "한적" 동일 정의 → 정상 모드 결과엔 🍃 배지가 항상 동반.
  - **`sigunguSet` = { 미방문 ∩ 한적 시·군·구(통계청 code) }** — `${area}:${name}` → `CONGESTION_SIGUNGU_CODES` → 법정동 pctRank **중앙값 ≤ cut**(배지와 같은 다리·같은 규칙), 매핑 없음·랭크 결측은 한적 아님(보수). **후보 수·좌표 검증·EMPTY_POOL 판정·배지 주장 전부 이 한 집합**(정의 분열 금지).
  - `eligibleCells` = { cell : 멤버 중 **1개 이상** sigunguSet 소속 } — 부분 방문 셀도 통과(방문 구 차단은 좌표 검증 몫). ~~멤버 전원 미방문(보수)~~은 기각: 부분 방문 셀의 미방문 구가 count에 세이고도 영영 안 뽑히는 분열(N>0인데 EMPTY_POOL) 발생. **성질: sigunguSet ≠ ∅ ⇔ eligibleCells ≠ ∅**(성질 테스트로 고정).
  - **성능 저하 모드**: congestion 실패/stale(48h) → sigunguSet = 미방문 전체 + notice(문구 ③) + 🍃 배지 억제(BadgeCtx ranks null 유지 → 자연 억제). 버튼이 죽는 것보다 정직한 축소 고지(🍃 soft-skip 선례).
- **뽑기 절차 — `drawEmptySpot`(lib/tourapi.ts 동거)**:
  1. `getCongestionDay(오늘)`(1h 캐시) → `rankDaily` → 풀 계산. **📅 date는 1단계 UI 미배선**(항상 오늘 — 조건 패널의 dateYmd 상태와 소리 없이 결합하는 것 차단). 서버는 `date` 수용(§6.8 축 대칭) — 배선은 백로그.
  2. sigunguSet 비면 EMPTY_POOL(문구 ④-a).
  3. `shuffle(eligibleCells)` → 셀 순회(최대 `EMPTY_SPOT_CELL_TRIES = 3`): 타입 셔플(`RANDOM_DEFAULT_TYPES`) → (셀,타입) `getTotalCount`(`queryParams`에 `sigunguCode` 방출 **신설** — URL 캐시 자동 분리) → `pickItemFrom`에 **`accept` 콜백 신설**(좌표 → `sigunguAt` → sigunguSet 소속 검증. 빈 응답·좌표 없음·검증 실패를 **한 재시도 루프**(MAX_INDEX_TRIES=3)로 통합 — 기존 호출부 무변). 성공 시 개요·배지 결합.
  4. 3셀 소진 → EMPTY_POOL(문구 ④-b — **시도 소진을 '정복 완료'로 오귀속 금지**).
  - **배지 배선 명시**: drawEmptySpot이 `BadgeCtx`(sigunguRanks·congestionBaseYmd·targetYmd)를 직접 구성 — 현행은 quiet=1일 때만 채워지므로 "기존 경로 그대로"로는 배지가 조용히 안 뜬다.
  - ⚖️(§6.9)와 독립 — 셀 선택은 균등(교집합 자체가 분산 산물, 이중 가중 불필요).
- **API**: `GET /api/random?emptySpot=1&exclude=<통계청 code CSV>(&date)` — `near`처럼 **특수 분기**(areas/types/특수조건 무시 명기).
  - **`buildEmptySpotQuery` 별도 빌더**(모드 무관 — `buildNearbyQuery` 전례). `buildRandomQuery`는 무변 — pure 모드 `""` 불변식 보존(🔭 버튼은 기본 모드 pure에서 눌리므로 buildRandomQuery 확장 시 조용히 일반 뽑기로 강등되는 무성 실패).
  - exclude 경계 규약: 클라 `conqueredSigunguCodes(visited)` → **정렬 CSV**(쿼리 문자열 결정성) → 서버 `parseSigunguCodes`(**문자열** 화이트리스트 — KOREA_SIGUNGU code, parseCodeList의 숫자 기반과 구분) → 내부 `Set<string>` 통일(타입 경계 테스트). 최악 250개 ≈ 1.5KB — GET 유지. *프라이버시 각주: 방문 시·군·구 목록이 URL 로그에 남음 — 좌표·개별 장소가 아닌 시·군·구 수준이라 수용(판단 변경 시 POST 전환).*
  - **불변식(로드맵)**: emptySpot 미지정이면 exclude는 파싱조차 안 함 — 기존 뽑기와 동일 동작(테스트).
- **count**: `GET /api/random/count?emptySpot=1&exclude=...` → `{ totalCount: |sigunguSet|, approx: false }` — **TourAPI 0콜**(1h 캐시 + 순수 계산), 단위 = 시·군·구. congestion 실패/stale → `{ dynamic: true }`. emptySpot 시 다른 파라미터 무시. **전용 훅 `useEmptySpotCount` — /map CTA 캡션 전용**. 기존 CandidateBadge 쿼리에 emptySpot·exclude가 절대 안 실림(테스트) — totalCount 단위 오버로드가 "≈ N곳 후보" 배지로 새는 것 차단.
- **UI·진입점**:
  - ~~**홈 히어로**(정복 타일 아래): 버튼 + **무수치 캡션**(문구 ①)~~ → **§7.13B에서 제거**(2026-07-28) — 진입점은 `/map` 하나. 아래 동작부는 `/map` 신호 경로에 그대로 남아 있다: `import("@/lib/conquer")` **동적 로드**(1회 성공 후 캐시, 실패는 기존 에러 상태로 커밋) → exclude 계산 → runDraw. `store.ready`·`store.synced` 게이트와 로컬 pending 재진입 차단도 유지.
  - **번들 분리 선행**: `visitedAreaCodes`(koreaMap 비의존)를 **`lib/visitedAreas.ts`로 분리**하고 홈(`page.tsx`)·`MapHero.tsx`의 정적 import를 교체 — lib/conquer의 정적 참조가 홈에 남아 있으면 동적 import가 무효(번들러가 메인 청크에 포함). 검증 기준에 번들 실측 포함.
  - **/map CTA**(정복 모드 하단): 수치 캡션 "빈 곳 N곳"(useEmptySpotCount — koreaMap 기로드 화면). 클릭 → `router.push("/?emptySpot=1")` **신호 → 홈이 store.ready && 신호일 때 1회 소비**(즉시 URL에서 제거·StrictMode 이중 실행 ref 가드) 후 자동 실행. "/map 내 실행"은 기각(결과 표면·상태가 전부 홈 로컬).
  - **홈 17타일 정합**: 홈 히어로는 시·도 타일이라 "지도에 없던"이 시·군·구 단위임을 문구로 명시(①·②) + 결과 카드에 **/map 딥링크**("내 지도에서 확인") — 이미 칠한 시·도 안의 빈 동네가 뽑혀도 모순으로 안 읽히게. 검증도 /map에서.
- **결과 카드**: `PickedInfo`에 emptySpot 표식 필드 신설(`types/tour.ts` + 기존 shape 테스트 동반 갱신) → "🔭 지도에 없던 동네" 배지 + 🍃 배지 동반. 방문 0 사용자에게도 참(전부 없던 곳).
- **문구 확정 (§7.9 원칙 적용)**:

| # | 표면 | 문구 |
|---|---|---|
| ① | 버튼·캡션 | **🔭 빈 곳에서 뽑기** / ~~홈 캡션 "아직 발도장 없는 한적한 동네에서"~~(§7.13B 제거) / map 캡션 "지도에 없는 한적한 동네 **N곳**" (count dynamic 시 "안 가본 동네에서 — 혼잡도 확인 불가") |
| ② | 결과 카드 배지 | **🔭 지도에 없던 동네** (+ 🍃 "{시군구명} · 한적 예측 · 집중률 하위 N%" 동반) |
| ③ | notice(성능 저하) | 혼잡도 데이터를 못 불러와 이번엔 '지도에 없는 곳' 기준으로만 뽑았어요. |
| ④-a | EMPTY_POOL(빈 풀) | 지도의 빈 곳 중 한적할 것으로 예측되는 동네가 지금은 없어요. |
| ④-b | EMPTY_POOL(시도 소진) | 이번엔 못 찾았어요 — 한 번 더 뽑아보세요. |

- **테스트 (TDD — 순수부만)**: `emptySpot.test.ts`(sigunguSet 제외·컷 경계 0.5 포함·결측 보수·전멸 / eligibleCells N:1·부분 방문 통과 / **성질 sigunguSet≠∅⇔cells≠∅** / string 타입 경계) · `query.test.ts`(buildEmptySpotQuery 방출·정렬 결정성·parseSigunguCodes 화이트리스트·기존 불변식 무변) · **tourSigungu 산출물 상시 검증**(250 전수·중복 0) · count 계획(정확 집계·dynamic 폴백).
- **파일**: `lib/emptySpot.ts`(+test) · `lib/tourSigungu.ts`(생성물) · `scripts/genTourSigungu.mjs` · `lib/visitedAreas.ts`(분리) · `lib/congestion.ts`(QUIET_SIGUNGU_CUT 승격) · `lib/tourapi.ts`(drawEmptySpot·queryParams sigunguCode·pickItemFrom accept) · `lib/query.ts`(+test) · `app/api/random/route.ts` · `app/api/random/count/route.ts` · `hooks/useEmptySpotCount.ts` · `app/page.tsx` · `components/MapHero.tsx`(import 교체) · `app/map/page.tsx`(CTA) · `components/ResultCard.tsx`(배지) · `types/tour.ts`(+shape 테스트 갱신)
- **호출 예산**: 뽑기 1회 전형 **3~4콜**(count 1 + item 1~2 + overview 1), 워스트 3셀×(count 5 + item 3)+overview 1 = **25콜**. 후보 수(count) **0콜**. 생성기는 개발 시 1회 17콜.
- **검증 기준**: ① 방문 기록 계정에서 🔭 뽑기 → **/map 정복 지도**에서 결과 시·군·구가 빈 조각 + 결과 카드 🍃 배지 동반 ② 방문 0 → 전국 한적 시·군·구에서 뽑힘 ③ 기존 뽑기 쿼리·응답 회귀 없음(불변식 테스트) ④ /map 캡션 N = count 응답 = 빈 조각∩한적 수(성질상 뽑기 가능 집합과 동일) ⑤ congestion 정지 → 성능 저하 모드 + notice ⑥ **홈 초기 번들에 koreaMap 미포함 실측**(next build 분석).

### 7.12 🎰 정복 지도 룰렛 (M23)

> 뽑기 버튼을 누르면 **홈 정복 지도의 시·군·구 조각들이 룰렛처럼 빠르게 켜졌다 꺼졌다** 하다가, 결과가 나오면 **당첨 시·군·구 한 조각에 착지**한다. "어디가 걸릴까"를 결과 카드가 아니라 **지도 위에서** 보게 하는 연출 — §7.9 분산 서사("전국 어디든 같은 출발선")를 시각적으로 반복한다.

- **지도 공유 — `components/ConquerSvg.tsx` 추출**: 현행 `ConquerMap` 안의 SVG(250 path + 시·도 외곽선 + 모듈 로드 시 1회 `ringsToPath` 계산)를 그대로 분리해 **`/map`과 홈이 같은 컴포넌트를 공유**. props `{ conquered: Set<string>, flashing?: Set<string>, spinning?: boolean }`. `ConquerMap`은 이걸 쓰도록 교체 — 렌더 결과 동일(회귀 없음).
- **번쩍임은 별도 색 레이어**: 룰렛은 정복색(emerald)이 아니라 **amber**로 켠다. 정복 fill을 껐다 켜는 게 아니므로 **룰렛 종료 시 복원 로직 자체가 불필요**하고, 이미 정복한 조각이 "지워졌다"는 착시도 없다. fill 우선순위 = `flashing` > `conquered` > 미정복.
- **spinning 중 transition 제거**: 현행 path의 `transition-[fill] duration-500`은 룰렛 tick(수십~수백 ms)에서 번쩍임을 뭉갠다 → spinning일 때 transition 클래스 미부여.
- **순수부 `lib/roulette.ts`**:
  - `rouletteFrame(elapsedMs, totalMs)` → `{ intervalMs, count }` — 경과에 따라 tick 간격은 **늘고**(fast→slow) 동시 점등 개수는 **줄어든다**(감속). 단조 함수라 프레임 상태를 들고 다닐 필요가 없다.
  - `pickFlashCodes(codes, count, rand)` — 중복 없이 count개. 풀보다 count가 크면 전량, 빈 풀은 빈 배열.
  - **착지**: 목표 코드가 정해지면 마지막 구간은 그 코드만 점등(두어 번 깜빡) → **켠 채로 유지**.
- **배선(`app/page.tsx` · `MapHero`)**:
  - `status.kind === "loading"` → spinning 시작. `status.kind === "ok"` → **`sigunguAt(place.lat, place.lng)`**(lib/conquer 기존 export)로 목표 코드 → 착지. 좌표 없음·판정 실패(먼바다 등)면 착지 없이 조용히 종료.
  - 회전 최소 시간은 기존 **`MIN_SPIN_MS = 1200`**(§7.9)이 이미 보장 — 별도 타이머 신설 불필요.
  - `status.kind === "error"`도 룰렛 종료(연출만 계속 도는 무성 실패 차단).
  - 결과 카드 자리의 `SlotMachine`은 **그대로 둔다**(위=지도, 아래=슬롯 — 위치가 달라 겹치지 않음).
- **홈 히어로 교체(`MapHero.tsx`)**: 정복 뷰의 **17개 시·도 타일 그리드 → `ConquerSvg`**(래퍼 `components/HomeConquerMap` — koreaMap 의존을 이 파일 하나에 가둔다). 경계 데이터(~207KB)는 `dynamic(ssr:false)`로 분리해 **초기 청크 무변**. 정복률 링도 **시·군·구 기준으로 통일**(`/map` 히어로와 같은 숫자) — 지도가 시·군·구를 칠하는데 링만 시·도 %면 두 수치가 어긋나 보인다.
  - ⚠️ **§7.11 번들 보호의 실질 축소**: "홈 초기 청크에 koreaMap 미포함"은 그대로지만, M16의 *"핀 뷰만 볼 땐 아예 안 받는다"* 이점은 사라진다 — **정복 뷰가 홈 기본값**이라 홈을 열면 사실상 항상 지도 청크를 받는다. 첫 페인트를 막지 않는 것(dynamic)까지가 이제 보장 범위. 룰렛이 홈에서 돌아야 한다는 요구의 직접 대가라 수용.
- **착지 결과는 남는다(2026-07-28 사용자 요청)**: 착지 후 당첨 조각을 해제하지 않고 **다음 뽑기의 회전이 덮을 때까지 유지**한다(구현: 해제 스텝 삭제 — `LAND_STEPS_MS`의 마지막 원소). "한 번 보여주고 스르륵 사라져" 결과 지역을 지도에서 다시 확인할 수 없던 문제. 남아 있는 동안 지도 캡션은 `🎯 노란 조각이 방금 뽑힌 동네예요.`로 바뀐다(정체불명의 노란 점 방지). 좌표 없음·에러 결과는 종전대로 즉시 해제. ⚠️ 알려진 한계: 유지 중인 조각을 곧바로 '다녀옴' 저장하면 정복 초록이 amber에 가려진다(다음 뽑기에 정리) — `flashing > conquered` 우선순위를 뒤집으면 *이미 정복한 곳이 뽑혔을 때 착지가 안 보이는* 퇴행이 생겨 그대로 둠.
- **접근성**: `prefers-reduced-motion: reduce`면 룰렛 생략, 당첨 조각만 한 번 점등(역시 유지). SVG `aria-label`은 정복 현황 그대로 두고 **룰렛은 장식**으로 처리 — 결과 안내는 기존 결과 카드 `aria-live`가 이미 담당하므로 `aria-live` 중복 낭독을 만들지 않는다.
- **🎉 시·도 정복 토스트는 유지**(설계 초안 정정 — 구현 중 판단): 토스트는 히어로 우측 상단 absolute 배지라 **타일과 독립**이고, 헤더의 시·도 정복 pill(`conqueredAreas / 17`)도 그대로 남아 정합이 유지된다. 지우는 쪽이 오히려 코드를 건드리는 일이라 그대로 뒀다. **시·군·구 단위로 세분화하는 것**만 범위 밖(§11.1 — 2026-07-28 사용자 결정: 나중에). 단 타일과 함께 쓰이던 `animate-tile-pop`은 사용처가 사라져 CSS에서 제거.
- **테스트(TDD — 순수부만)**: `roulette.test.ts` — 감속 **단조성**(elapsed 증가 시 intervalMs 비감소·count 비증가)·경계(elapsed 0·elapsed≥total)·`pickFlashCodes`(중복 없음·count>풀이면 전량·빈 풀 안전·rand 주입 결정성). 타이머 루프·DOM은 단위 테스트 비대상(SlotMachine 전례).
- **파일**: 신규 `lib/roulette.ts`(+test) · `components/ConquerSvg.tsx`. 기존 `components/ConquerMap.tsx`(SVG 추출) · `components/MapHero.tsx`(타일→지도·링 기준 통일) · `app/page.tsx`(룰렛 배선).
- **호출 예산**: **추가 API 0콜** — 전부 클라이언트 렌더.
- **검증 기준**: ① 홈에서 뽑기 → 조각들이 번쩍이다 결과 시·군·구에 착지 ② 착지 조각 = 결과 카드의 지역 ③ 이미 정복한 조각이 룰렛 후에도 초록 유지 ④ reduced-motion에서 번쩍임 없음 ⑤ **홈 초기 번들에 koreaMap 미포함 실측**(§7.11 회귀 방지) ⑥ `/map` 정복 지도 렌더 무변.

### 7.13 🧱 홈 2열 레이아웃 + 🔭 진입점 단일화 (2026-07-28 사용자 요청)

- **문제**: 홈이 세로로 너무 길었다. 지도 히어로가 전폭 613px를 쓰는데 정작 `ConquerSvg`는 `max-w-[320px]`라 **좌우가 통째로 놀았고**, 그 아래 뽑기 덱이 또 쌓였다.
- **변경(A) 2열**: `[지도 | 뽑기·결과]`를 `lg:grid-cols-2`로 나란히, **기록 패널은 그 아래 전폭**으로 내렸다. `lg` 미만은 종전대로 세로 스택. 실측(뷰포트 1280): **결과 표시 시 1814px → 1373px(-441px, 24%)**, idle 1173 → 1124.
  - idle 상태에서 오른쪽 열이 비는 건 **알려진 트레이드오프** — 2026-07-28 사용자가 "그대로 두자"로 확정(지도 열 축소·기록 패널 이동 안은 기각).
- **변경(B) 🔭 진입점 단일화**: 홈 히어로의 빈 곳 CTA(버튼 + 캡션 "아직 발도장 없는 한적한 동네에서")를 **제거** — 진입점은 `/map`(전체 지도 크게 보기) **하나**. §7.11의 "홈 히어로 + /map 두 진입점"을 대체한다. 홈의 `runEmptySpot`과 `?emptySpot=1` 신호 소비는 **그대로 유지**(/map CTA가 이걸 쓴다). MapHero 는 `onEmptySpot`·`emptySpotPending`·`storeSynced` prop 을 잃어 순수 표시 컴포넌트가 됐다. 지도 섹션 613 → 535px.
- **⚠️ 함정(실측)**: 홈 버튼을 지우자 **원본엔 없던 ESLint 에러**가 났다. `runEmptySpot` 이 MapHero prop 으로도 참조되던 게 사라지면서, 선언보다 위에 있던 신호 소비 `useEffect` 가 `react-hooks/immutability`(선언 전 접근)에 걸린 것. **훅을 `runEmptySpot` 선언 뒤로 이동**해 해결했고, 그러자 `react-hooks/set-state-in-effect` 가 새로 걸려 그 한 줄만 사유와 함께 disable 했다(URL 신호→뽑기 시작은 의도된 동작 + ref 로 1회 가드). 훅을 다시 위로 올리면 재발하므로 코드에 주석으로 못 박아 뒀다.
- **파일**: `app/page.tsx`(레이아웃·훅 이동) · `components/MapHero.tsx`(CTA 제거·prop 축소). 추가 API 0콜.
- **검증**: tsc·ESLint 0 · vitest 467 통과 · `next build` 성공 · 모바일 375px 1열 스택(가로 스크롤 없음) · **`/map` CTA 왕복 실측**(클릭 → 홈 이동 → "🔭 지도에 없던 동네" 결과).

### 7.14 🎨 Genesis 리스킨 + 홈 지도·뽑기 통합 (M24, 2026-07-28 디자인 핸드오프)

> 출처: `claude-design/Genesis 디자인 시스템 적용.zip` → `design_handoff_genesis_reskin/`(README·genesis-DESIGN.md + `.dc.html` 시안 2종). 두 작업이 섞여 있다 — **A 리스킨**(색·타입·모양만, 구조 무변) + **B 홈 통합**(2열 → 1열, 지도를 룰렛 본체로).

- **A. 토큰 전면 교체 (emerald/zinc → Genesis indigo)**: `app/globals.css`에 `--g-*` 변수를 두고 `@theme inline`으로 Tailwind 유틸리티(`bg-g-surface`·`text-g-text-2`·`border-g-border` …)로 노출. **다크는 `dark:` 클래스가 아니라 변수 스왑**(`prefers-color-scheme`) — 컴포넌트가 라이트/다크를 짝으로 쓸 필요가 없어져 "한쪽만 써서 다크가 깨지는" 실패 모드가 구조적으로 사라진다.
  - **`--g-on-primary` 신설**: 채워진 indigo 위 잉크는 **테마마다 달라야 한다** — 라이트 `#FFF` on `#6366F1` = 4.47:1(통과)인데 다크는 `--g-primary`가 `#818CF8`로 밝아져 흰 글자가 2.98:1로 **실패**한다. 다크 잉크 `#0B0B0E` = 6.59:1. 색이 아니라 토큰으로 둬 호출부가 실수할 여지를 없앴다(실측 확인: 라이트 4.47 / 다크 통과).
  - **그림자 전면 제거**: Genesis는 정적 요소에 elevation을 안 쓴다(border-first). `shadow-*` 0건 실측. 예외는 **떠 있는 것**(iOS 설치 안내 팝오버)만.
  - **반경 스케일 고정**: 카드·배너·조건 패널 `12px`(=`rounded-xl`) / 카드 안쪽 블록 `8px`(`rounded-lg`) / 버튼·세그먼트 `6px`(`rounded-md`) / 칩·pill·아이콘 버튼 `9999px`.
  - ⚠️ **세그먼트에 `transition-colors` 금지**(핸드오프 실측): 다크 전환 시 Chromium이 CSS 변수만 바뀐 `color`/`background-color`를 재계산하지 않아 값이 이전 테마에 붙는다. `transform`/`opacity` 트랜지션은 무해. hover 색은 `hover:` 유틸로.
  - ⚠️ **11–12px 캡션에 `--g-neutral`(2.75:1) 금지** — 읽는 텍스트는 `--g-text-2`(5.7:1), `--g-neutral`은 순수 장식·구분자만.
- **A. 타이포 — General Sans(display) / DM Sans(body)**: 둘 다 **한글 글리프가 없어 폴백(Apple SD Gothic Neo·Malgun Gothic)이 실제 한글을 렌더한다** — 의도된 동작(Pretendard 등 추가 금지, 사용자 결정). General Sans는 Fontshare만 배포해 `next/font/google`로 못 받으므로 woff2 4종(400·500·600·700)을 `public/fonts`에 셀프호스팅 + `next/font/local`(외부 요청 0).
- **A. 이모지 → 선 아이콘 전면 교체**: `components/icons.tsx` 신설(24 viewBox · stroke 1.7px · round cap/join · 39종). **데이터에 박힌 이모지는 아이콘 키로 치환** — `COURSE_SLOTS[].emoji → icon`, `lib/level.ts TIERS emoji → icon`, `REVISIT_OPTIONS.emoji` 제거(3단계는 라벨 + 의미색으로 구분). 서버 notice 문구의 ☔·🍃도 조건 이름(`‘비 안 오는 곳’`·`‘한적한 곳’`)으로 교체. 렌더 텍스트 이모지 **0건 실측**(홈·`/map` 양 뷰).
  - 남긴 것: `↓`(코스 다리)·`≈`(후보 수)·`✓`(SVG 조각 툴팁)는 **시안이 그대로 쓰는 기호**라 유지.
  - **후속 정리(2026-07-28)**: `SEA_CAT3`·`SEASONAL_CALENDAR`의 `emoji` 필드는 M24 당시 "API 응답 형태라 유지"로 남겼으나, 소비처 0을 재확인해 **제거**했다 — 상수 25행 + `SeasideBadge`/`SeasonalBadge` 타입 + `tourapi` 주입 3곳 + `season.test.ts` 픽스처. `/api/random` 응답에서 `picked.seaside.emoji`·`picked.seasonal.items[].emoji`가 사라지는 shape 변경이지만 소비자가 이 앱 자신뿐이라 안전하고, designGuide의 "데이터에 이모지를 박지 않는다"와도 일치한다. 카톡 공유에 제철 이모지를 되살리는 안은 기각 — `lib/kakaoShare.ts`는 `Place`만 받고 제철 배지를 모르므로 `ShareContext` 확장 + 호출부 배선이 필요한 **새 기능**이다(필요해지면 그때).
- **B. 홈 1열 통합 카드**: `max-w-[1140px]` 2열(§7.13) → **`max-w-[720px]` 단일 컬럼**, 지도와 뽑기가 **한 카드** 안에. 위→아래 한 흐름: `스탯 행(56px 링 + n/250 + 다녀온 곳·시·도) → 지도(max-w 380) → (뽑는 중) 릴 → hairline → 뽑기 영역(모드 토글·조건 패널·뽑기 버튼·안내 1줄)`. 결과 카드·코스·기록은 카드 **밖** 아래로(`mt-3`).
  - **삭제(정보 과다 해소 — 되살리지 말 것)**: 카드 안 `정복/핀 지도` 뷰 토글(→ `/map`에만) · 헤더 `정복 9/17 · 53%` pill(→ 스탯 행에 흡수) · 지도 하단 캡션 2개 · 페이지 하단 "탐험 로그 …" 안내문 · 슬롯 릴 2개 → **1개**(지역명만). `components/MapHero.tsx`는 남는 게 토스트뿐이라 **삭제**하고 `HomeConquerMap`이 스탯 행 + 지도 + 결과 칩을 담당(정복 집계가 koreaMap 의존이라 그 청크 안에 있어야 한다 — §7.11 번들 보호 유지).
  - **뽑기 카피 변경**(지도가 룰렛 본체가 됐으므로): `여기서 한 곳 뽑기` → **`지도 굴리기`** / `여행지를 뽑는 중…` → **`지도를 굴리는 중…`** / `다시 뽑기` → **`다시 굴리기`**. idle 안내 = `버튼을 누르면 전국 250개 시·군·구 위에서 주사위가 굴러가요.`
- **B. `ConquerSvg` phase 확장 — JS 룰렛 → CSS phase(§7.12 개정)**: props가 `{conquered, flashing, spinning}` → **`{conquered, phase, highlight}`**로 바뀐다.
  - `idle` 기존과 동일 · `loading` 오버레이 `<g>`가 조각 28개(`i=3; i+=9`)를 `--g-primary`로 깔고 `map-flick 0.9s` **9단 스태거**(0~720ms) · `result` 뽑힌 조각만 `fill-opacity 1`, 나머지 전부 `0.45` + 마커 2개(펄스 링 `r=9` `pin-pulse 1.8s` + 점 `r=7` `stroke: --g-surface` 3px `non-scaling-stroke`).
  - ⚠️ **시안 코드의 스태거는 실제로 안 걸린다**: `(i % 9) * 90`은 `i`가 항상 `3 mod 9`라 전부 270ms(스태거 0)로 계산된다. 핸드오프 본문이 명시한 "delay 스태거"를 따라 **뽑힌 순번 기준**으로 벌렸다(의도 우선).
  - 마커 좌표 = 조각 정점 평균 → 순수부 **`ringsCentroid`**(`lib/conquer`, TDD). 무게중심이 아니라 정점 평균인 이유: 마커는 "이 조각이다"를 가리키는 장식이라 정확한 중심이 불필요하고, 정점 평균은 섬·홀이 섞인 다중 링에도 그냥 동작한다.
  - **`lib/roulette.ts`(+test) 삭제** — 감속 tick 루프가 CSS 스태거로 대체돼 사용처가 사라졌다. 정보 손실은 없다(당첨 조각을 마커+칩으로 더 분명히 가리킨다). 결과 칩("전남 여수시")은 지도 청크 안에서 렌더되므로 **로딩 플레이스홀더와 겹칠 창이 구조적으로 없다**(핸드오프의 `mapReady` 게이트 요구를 배치로 해소).
- **B. `/map` 🔭 CTA 위치 이동**: 정복률 히어로 **바로 아래**(지도 위)로. 기존엔 페이지 최하단이라 스크롤해야 보였다. 데스크톱 버튼+캡션 한 줄 / 모바일 세로 스택. `ConquerMap`을 쪼개지 않고 **`cta` prop 슬롯**으로 끼웠다 — 쪼개면 정복 판정(좌표 투영+ray casting)이 두 번 돈다.
- **핸드오프 대비 의도적 차이 2건**: ① 릴 아래 **분산 서사 로테이션 문구 유지**(시안엔 없지만 §7.9가 요구하는 카피 — 핸드오프의 "삭제된 것" 목록에도 없다) ② 다크 트리거는 `[data-theme]`이 아니라 `prefers-color-scheme`(앱에 테마 토글 UI가 없어 `[data-theme]`은 죽은 코드가 된다).
- **파일**: 신규 `components/icons.tsx`·`public/fonts/GeneralSans-*.woff2` / 삭제 `components/MapHero.tsx`·`lib/roulette.ts`(+test) / 수정 `app/{globals.css,layout.tsx,page.tsx,map/page.tsx}` · `components/{ConquerSvg,ConquerMap,HomeConquerMap,VisitedMap,ResultCard,RecordPanel,CoursePanel,FilterPanel,ModeToggle,SlotMachine,StoryBanner,InstallButton,AuthButtons}.tsx` · `lib/{conquer,course,level,constants,tourapi}.ts`. 추가 API 0콜.
- **검증**: tsc·ESLint 0 · vitest 478 · `next build` 성공. 실측 — 다크 토큰 스왑(bg `#0b0b0e`·surface `#141419`·primary `#818cf8`·on-primary `#0b0b0e`) · 뽑기 버튼 잉크 대비 라이트 4.47:1 · `main` 안 `box-shadow` 0건 · 렌더 이모지 0건 · loading(flick 28조각·delay 0~720ms·릴 h56 r8 slot-spin 0.6s·버튼 opacity .6 cursor default) · result(250조각 중 249개 `0.45`·당첨 1개 강조·펄스 `transform-box: fill-box`·점 stroke 3px·칩 "서울 강서구") · 모바일 390px 가로 스크롤 0.

### 7.15 📈 임팩트 대시보드 (M26) — 분산 효과 논증 화면 (2026-07-28 설계)

> §14.5 심사 대비 백로그 1번 승격. 목적: "랜덤 추천이 실제 분산을 만드는가?"라는 기획력·적정성 최대 반론에 대한 **정량 방어 화면**. 공개 페이지 **`/impact`** — 심사 시연은 URL 직접 진입, 분산 서사(§7.9)의 근거 화면으로도 상시 사용. 진입점은 푸터 링크 1개(홈 노출은 §14.5 2번 "정면 노출"과 함께 후속).

- **화면 3패널 (위→아래 서사: 문제 → 설계 → 실측)**:
  - ① **현실 — 방문 편중**: 외지인 방문자 시·도 17 점유율 가로 막대(`visitor_daily` M17 적재분 + M22 귀속·입도 중복 제거 로직 **재사용**). 캡션에 극단 배율(서울↔세종 71.1배 실측, §6.9B) — "전국 방문은 이만큼 기울어 있다"가 문제 정의.
  - ② **설계 — 뽑기 확률**: 기본(시·도 균등 1/17, §6.9A)과 ⚖️ ON 가중 확률(`areaWeights` 정규화)을 같은 축에 겹침. **§14.5 초안("1,000회 시뮬레이션 승격")을 해석적 계산으로 개정** — `weightedPickOrder` 첫 선택 확률은 w/Σw 로 정확해 표본 노이즈가 0이다. 시드 고정 1,000회 스냅샷(`scatter.test.ts`)은 **샘플러 검증 테스트로 유지**(화면 산출물 아님). 라벨 규칙(§6.9 "실측 사칭 금지" 계승): **"설계 확률 — 실측 아님"** + "시·도 선택 기준"(후보 0 지역 스킵이라 최종 분포의 근사임) 병기.
  - ③ **실측 — 실사용 분포·반응 퍼널**: 서버 수집 이벤트(아래 P1) 집계 — draw·redraw 시·도 분포를 ②에 겹쳐 비교 + 퍼널 **"제안 N → 찜·길찾기 M → 다녀옴 K"**(수락 행동 = "따라간다"의 직접 증거). **표본 수 상시 노출**, n<100이면 "표본 누적 중" 배지 — 적은 표본으로 결론을 사칭하지 않는다. ⚠️ 퍼널은 전역 카운트(세션 조인 아님)라 **인과가 아니라 연관**임을 화면 주석으로 명시.
- **§12.6 P1 승격 — 익명 이벤트 서버 수집**: 착수 조건("배포하고 남에게 공유하기 시작할 때")이 공모전 제출로 충족. 심사 시점(9~10월)까지 실데이터가 쌓이려면 **조기 배포가 곧 표본 크기**다.
  - DB **`travel_event`**(additive 신규 테이블): `event`·`mode`·`areaCode`·`contentTypeId`·`contentId`·`sessionId`(익명 UUID)·`ts`. **users FK 없음 = 로그인 계정과 조인 불가를 스키마로 강제**(§12.4 익명 유지). 인덱스 `(event, ts)`.
  - **`POST /api/events`**: §12.5 스키마 그대로(P0 링버퍼와 동일 — "스키마 미리 검증" 투자 회수). 검증은 화이트리스트(event 5종·타입 체크)·페이로드 상한, 미지 필드 무시·오스키마 400. 전송은 `navigator.sendBeacon` 우선(이탈 시 유실 방지)+`fetch keepalive` 폴백, **전송 실패는 조용히 무시**(로컬 링버퍼 기록은 계속 — UX 무영향).
  - **`/privacy` 고지 1장 + 푸터 링크**: 익명 수집이라도 §12.4 결정대로 수집 시작 시점부터 고지(무엇을·왜·개인 식별 불가·문의처). 기존 로컬 링버퍼 **백필은 하지 않는다**(고지 이전 데이터) — "내 기록 업로드" 옵션은 후속.
  - 남용 방어 v1 = 스키마 검증·크기 상한만(레이트리밋은 실트래픽 생기면 — YAGNI).
- **`GET /api/impact`**(1h 캐시): ①②③ 데이터 일괄 반환 — **DB 조회만, TourAPI 0콜**. DB 실패 시 죽지 않고 ②(설계 확률 — 순수 계산)만이라도 렌더(부분 실패 허용 + notice, §6.5 결).
- **순수부(TDD)**: 신규 `lib/impact.ts` — `visitorShares`(합=1 불변식)·`designProbabilities`(합=1)·`aggregateDrawEvents`·`funnelCounts`·`parseTravelEvent`(POST 검증). `lib/scatter`에서 시·도 합산부 **`areaVisitorSums` 추출**(= `areaWeights` 내부 sums 공용화 — `areaWeights`가 이를 사용, 동작 무변·기존 테스트 유지).
- **집계 의미론**: "제안" = draw+redraw 합(서비스가 실제 던진 분포 — redraw 제외하면 거절된 제안이 사라져 아전인수), 수락 신호 = like·navigate·visited.
- **차트 = 순수 SVG/div 가로 막대**(차트 라이브러리 0 — M12 선례). Genesis 토큰·아이콘(designGuide) 준수, 이모지 금지(M24). 카피는 공사 지칭 금지(§14.2) — 본문 출처는 "공공 관광 데이터" 수준(상세 출처는 푸터 공통 표기가 담당).
- **게이트**: 신규 외부 API 0(전부 기존 자산) — 유일 전제는 Neon `travel_event` additive 마이그레이션 **머지 전 선적용**(M10 규칙).
- **검증 기준(완료 정의)**: tsc·eslint·vitest·build + e2e — 프로덕션 뽑기 1회 → `travel_event` 1행 적재 / `/impact` 3패널·표본 수 표기 / `/privacy` 렌더·푸터 링크 / POST 오스키마 400 / ② 확률 합 1±ε.
- **비범위(v1)**: 시·군·구 250 실측 차트 · 기간 필터 · 세션 단위 퍼널(인과 추정) · 레이트리밋 · 실측-설계 통계 검정 · 로컬 백필 · 관리자 뷰.

## 8. 기술 스택 & 결정 근거

| 영역 | 선택 | 근거 |
|---|---|---|
| 프레임워크 | Next.js (App Router) + TS | 프론트 + API 프록시를 한 프로젝트에. CORS·키 노출 문제를 API Route로 한 번에 해결 |
| 스타일 | Tailwind CSS | 반응형을 빠르게, 별도 CSS 파일 관리 부담↓ |
| 저장 | localStorage | 찜·기록용 (P0 기준). 수익화 P1부터 이벤트용 DB 추가 — 로그인은 그때도 불필요 (§12.5) |
| 배포 | Vercel | Next.js 무료 배포, push 시 자동. 본인 폰에서 바로 접속 |
| 지도 | 카카오맵 링크 + JS SDK 임베드 | 링크(길찾기·열기)는 키 불필요 · 임베드(내 여행 지도)는 JavaScript 키, 일 30만 무료 (§7) |
| 날씨 | 기상청 초단기실황 API | ☔ 동적 필터용. TourAPI와 같은 공공데이터포털 키 체계 |
| 테스트 | **Vitest (M2 도입)** | M3부터 **TDD**(테스트 먼저 → 구현). 순수 로직을 분리해 단위 테스트 — **"조건 0개 = 완전 랜덤"** 불변식·코드 파싱/화이트리스트(`lib/query.ts`)·응답 정규화(`normalizePlace`). `npm test` |

## 9. 디렉토리 구조 (예정)

```
RandomTravel/
├─ app/
│  ├─ layout.tsx
│  ├─ page.tsx                # 메인 (모드 토글 + 뽑기)
│  ├─ globals.css
│  ├─ map/page.tsx            # 내 여행 지도 화면 (§7.1)
│  └─ api/
│     ├─ random/route.ts      # TourAPI 프록시 + 랜덤 로직
│     ├─ course/route.ts      # 🧭 반나절 코스 슬롯 뽑기 (§7.10)
│     └─ cron/congestion/route.ts # 🍃 혼잡도 일 1회 배치 적재 (§6.7)
├─ components/
│  ├─ ModeToggle.tsx          # 순수/조건 모드 전환
│  ├─ FilterPanel.tsx         # 지역·테마 선택
│  ├─ SlotMachine.tsx         # 뽑기 애니메이션
│  ├─ ResultCard.tsx          # 결과 카드
│  ├─ CoursePanel.tsx         # 🧭 반나절 코스 타임라인 (§7.10)
│  ├─ VisitedMap.tsx          # 내 여행 지도 — 핀(카카오맵 임베드 + 방문 마커, §7.1)
│  └─ ConquerMap.tsx          # 🧩 전국 정복 지도 — 시·도 SVG 채우기 (§7.4)
├─ lib/
│  ├─ tourapi.ts              # TourAPI 호출 래퍼 + 랜덤 알고리즘 + searchFestival
│  ├─ query.ts                # 파라미터 파싱·화이트리스트 + 쿼리 조립 — 순수 함수 (§8 테스트)
│  ├─ pipeline.ts             # 지역 풀 필터 파이프라인 — 순수 함수 (§8 테스트 대상)
│  ├─ weather.ts              # 기상청 초단기실황 호출 + PTY 판정 (☔ 필터)
│  ├─ congestion.ts           # 🍃 집계·백분위·한적 판정 — 순수 함수 (§6.7)
│  ├─ congestionCodes.ts      # 🍃 법정동↔시·군·구 매핑 (생성물 — genCongestionCodes.mjs)
│  ├─ kst.ts                  # 📅 KST 벽시계 헬퍼(kstYmd·ymdOffset·kstDateParts) — 중립 모듈 (§6.8)
│  ├─ tripDate.ts             # 📅 방문 시점 날짜 칩 계산(dateChips) — 순수 함수 (§6.8)
│  ├─ course.ts               # 🧭 코스 슬롯·다리 거리·교통 힌트 — 순수 함수 (§7.10)
│  ├─ emptySpot.ts            # 🔭 미방문∩한적 시·군·구 풀 — 순수 함수 (§7.11)
│  ├─ scatter.ts              # ⚖️ 시·도 방문자 역가중 — 순수 함수 (§6.9)
│  ├─ tourSigungu.ts          # 🔭 앱 시·군·구↔TourAPI sigunguCode 셀 매핑 (생성물 — genTourSigungu.mjs)
│  ├─ visitedAreas.ts         # 방문 시·도 집계 — koreaMap 비의존 경량 분리 (§7.11 번들 보호)
│  ├─ conquer.ts              # 🧩 정복 집계 — 좌표 점-다각형 판정→시·군·구 정복/정복률, 순수 함수 (§7.4)
│  ├─ koreaMap.ts             # 🧩 시·군·구 SVG 조각 + 시·도 외곽선(생성물) — scripts/genKoreaMap.mjs 로 재생성
│  ├─ events.ts               # 이벤트 기록 — P0은 localStorage 전용 (§12.6)
│  └─ constants.ts            # areaCode / contentTypeId / 격자좌표 / 🌊 바다 cat3 / 🦀 제철 달력 맵
├─ scripts/
│  ├─ genKoreaMap.mjs         # 🧩 시·도 경계 GeoJSON(public domain) → lib/koreaMap.ts 생성기 (§7.4)
│  ├─ data/                   # 🍃 공식 코드표 xlsx 원자료 (출처 주석, §6.7)
│  ├─ genCongestionCodes.mjs  # 🍃 코드표 xlsx → lib/congestionCodes.ts 생성기 (§6.7)
│  └─ genTourSigungu.mjs      # 🔭 areaCode2 → lib/tourSigungu.ts 셀 매핑 생성기 (§7.11, 17콜)
├─ db/                        # Drizzle 스키마·마이그레이션 + congestion.ts 적재/조회 래퍼 (§6.7)
├─ types/
│  └─ tour.ts                 # 응답 타입 정의
├─ .env.local                 # TOUR_API_KEY (git 제외)
└─ ...
```

## 10. 환경변수

| 변수 | 설명 | 비고 |
|---|---|---|
| `TOUR_API_KEY` | TourAPI 서비스키 | **서버 전용** — `NEXT_PUBLIC_` 접두사 금지(붙이면 브라우저 노출) |
| `WEATHER_API_KEY` | 기상청 단기예보 서비스키 (☔ 필터용) | 동일 — 서버 전용. 공공데이터포털에서 별도 활용신청 |
| `NEXT_PUBLIC_KAKAO_MAP_KEY` | 카카오맵 JS SDK JavaScript 키 (🗺️ 지도용) | 예외적으로 브라우저 노출 전제 키 — 카카오디벨로퍼스 도메인 등록으로 보호 (§7.1) |
| `CRON_SECRET` | 🍃 혼잡도 배치 크론 인증 (§6.7) | 서버 전용 — 임의 랜덤 문자열. Vercel env 등록 시 cron 요청 Authorization 헤더에 자동 첨부. dev 모드는 검증 생략 |

## 11. 마일스톤

| 단계 | 목표 |
|---|---|
| **M1** | 골격 셋업 + TourAPI 프록시 — 샘플 1건 뽑기 동작 |
| **M2** | 두 모드 + 조건 선택 UI (정적 조건: 지역·테마) |
| **M3** | 결과 카드 + 뽑기 애니메이션 + 카카오맵 링크 |
| **M4** | 찜 / 최근 기록 + 반응형 마무리 |
| **M5** | Vercel 배포 — 기본 기능 먼저 공개 |
| **M6** | 조건 확장 1차 — 🌊 바다 + 🦀 제철 (상수 기반, 추가 API·키 불필요) |
| **M7** | 조건 확장 2차 — 🎪 축제 ✅완료(기존 TourAPI 키). ☔ 날씨는 별도 키가 필요해 **M11로 분리** |
| **M8** | 🗺️ 내 여행 지도 ✅완료 — 카카오맵 임베드에 방문 마커 누적 (§7.1) / 딥링크는 M3에서 완료 |
| **M9** | 📱 PWA ✅완료 — manifest·아이콘·최소 서비스워커로 폰 홈 화면 설치 + 오프라인 앱 셸. **📲 설치 유도 버튼**(헤더 우측, `beforeinstallprompt` 캡처→네이티브 프롬프트 / iOS Safari는 "공유→홈 화면에 추가" 안내 팝오버 / 이미 설치 시 자동 숨김). 순수 판정 `lib/pwaInstall`(installMode·isIOS, TDD) + 배선 `components/InstallButton`. 추가 API·키 불필요 |
| **M10** | 🔐 로그인·동기화 ✅완료(구글·카카오) — OAuth + Neon(Postgres)로 찜/방문 **기기 간 동기화** |
| **M11** | ☔ 조건 확장 3차 ✅완료 — "지금 비 안 오는 곳" 필터(기상청 초단기실황 PTY, §6.1). 시·도 대표 위경도→격자 변환·30분 캐시·소스 장애 시 건너뛰기 |
| **M12** | 🧩 전국 정복 지도 ✅완료 — `/map`에 정복 지도 토글(§7.4). 방문 좌표를 점-다각형 판정해 **250개 시·군·구**를 실제 윤곽 SVG로 채움 + 정복률 게이지 + 시·도별 진행. 생성기(`scripts/genKoreaMap.mjs`)로 public-domain 경계→SVG. 추가 API·키 불필요 |
| **M13** | 💬 뽑기 결과 카톡 공유 ✅완료 — 결과 카드에 카카오 피드 공유 버튼(§7.5). `Kakao.Share.sendDefault`(기본 템플릿)로 사진+제목+주소 카드, 카카오맵/앱 홈 버튼. Web Share·클립보드 폴백. 추가 키 불필요(지도용 JavaScript 키 재사용) |
| **M14** | 📍 첫 여행지 주변에서 뽑기 ✅완료 — 순수 랜덤 결과를 앵커로, 결과 카드 "주변에서 뽑기" 버튼이 반경 20km 랜덤 추천(§7.6). TourAPI `locationBasedList2`(거리 배지 `dist`). 순수 모드 전용·별도 버튼. 추가 API·키 불필요(같은 `TOUR_API_KEY`) |
| **M15** | 📊 재방문 의향 평가 ✅완료 — 다녀옴 탭 각 행에 3단계(👎 또 안 감·😐 그럭저럭·👍 또 갈래) 세그먼트(§7.7). `SavedPlace.rating` + DB `user_place.rating`(nullable) write-through 동기화. 추가 API·키 불필요(같은 `DATABASE_URL`) |
| **M16** | 🎨 UI 리디자인(탐험 로그/1C) ✅완료 — 액센트 emerald 전환 + 홈 정복 지도 히어로 + 결과 카드 2단 액션 + 지도 탐험가 레벨·시·도 진행보드(§7.8). **실시간 후보 수 배지** 위해 새 `/api/random/count`(순수 `planCandidateCount` + `countCandidates`, `getTotalCount` 24h 캐시 재사용, 동적 조건은 `dynamic`). 새 순수 로직 `explorerLevel`·`relativeDay`·`visitedAreaCodes`·`naverMapLink`(TDD). 표현 계층만 — 데이터 계층 재사용 |
| **M17** | 🍃 한적 필터 ✅완료 — 혼잡도 데이터 축(§6.7). 한국관광공사 **관광지 집중률 예측**(+지역별 방문자수) OpenAPI 2종을 **일 1회 Vercel Cron 배치 → Neon 적재**하고 뽑기·후보 수는 DB 조회만. 🍃 "한적한 곳만" 토글(`quiet=1`) + 결과 카드 **"집중률 하위 N% · 한적 예측"** 근거 배지. 추가 키 불필요(같은 `TOUR_API_KEY`, 활용신청 2건) + `CRON_SECRET` 신설. **로컬 배치 실측 57.7s/252 시군구 0실패·7,560행** |
| **M18** | 📣 분산 서사 카피 ✅완료 — 히어로 서브카피·온보딩 배너(`StoryBanner`)·슬롯 로테이션(시점 중립 2문구)·정복 토스트("· 내 지도 +1")·카톡 공유(`DEFAULT_DESC`·`congestionShareLine` 3경로)·manifest/layout description 를 분산 서사로 리라이트(§7.9 카피 원칙 5). §6.5 설계분 **슬롯 최소 노출시간**(`MIN_SPIN` 1.2s, 에러 포함) 구현 + 공유 카드에 🍃 근거 1줄. 순수 `rotatingIndex`·`congestionShareLine`(TDD). 추가 API·키 불필요 |
| **M19** | 📅 방문 시점 칩 ✅완료 — 오늘/내일/이번·다음 주말(토)로 뽑기 기준일 선택(§6.8). 신규 순수부 `lib/kst`(KST 헬퍼 이동·중립화)·`lib/tripDate`(`dateChips`)·`parseDateYmd`·`resolveMonth`(TDD). 🍃·🎪·🦀 판정이 그 날짜 기준으로 전환(`DrawParams.dateYmd`·`countCandidates` 옵션화), 날짜 단독은 후보 무변(실측 count 21,225 동일)·☔는 오늘 전용 잠금(+notice). 결과 카드 🍃 배지 "M/D 한적 예측"·🎪 "(M/D 기준)". 추가 API·키 불필요(집중률 30일 예측 기적재분) |
| **M20** | 🧭 반나절 코스 1단계 ✅완료 — 뽑힌 곳을 앵커로 볼거리→식사→카페 3스텝 타임라인 + 스텝 재뽑기·전체 재생성(§7.10). `locationBasedList2` 재사용(부분 병렬·반경 5→10→20km 확대)·🍃 코스 헤더 배지(시·군·구 명시). 신규 순수부 `lib/course`(`COURSE_SLOTS`·`courseLegs`·`needsDriveHint`)·`haversineM`·`fmtYmd` 승격·`parseContentIds`·`buildCourseQuery`(TDD). 새 `/api/course`(전체+스텝 재뽑기+🍃 배지 재조립). 추가 API·키 불필요(같은 `TOUR_API_KEY`). **실측: 서울/완도/양평 3스텝·재뽑기·🍃 배지(완도군30%·양평군49%·서울 생략)·🚗 힌트(14.1km) e2e 통과, vitest 356** |
| **M21** | 🔭 빈 곳에서 뽑기 ✅완료 — 미방문∩한적 시·군·구 교집합에서 뽑기 + /map 역방향 CTA(§7.11). 생성기 `scripts/genTourSigungu.mjs`→`lib/tourSigungu.ts`(`areaCode2` 17콜 셀 매핑 N:1·229셀·250 members·특례 4건 시드). 순수부 `lib/emptySpot`(`emptySpotSigunguSet`·`eligibleCells`) + `QUIET_SIGUNGU_CUT` 승격·`sigunguPctRank` factor-out(배지와 한적 정의 단일화). `drawEmptySpot`(BadgeCtx 직접 구성→🍃 동반·좌표 검증 accept 콜백)·`countEmptySpot`(TourAPI 0콜)·`buildEmptySpotQuery`·`parseSigunguCodes`. 번들 분리 `lib/visitedAreas`(홈 초기 청크 koreaMap 제외 실측)·`store.synced` 게이트(로그인 기기 간 방문 병합 후 뽑기). 결과 카드 🔭+🍃 배지·/map 딥링크. 추가 API·키 불필요. **실측: e2e 정읍/완도/합천/순천 뽑기 emptySpot+🍃, /map "125곳", 역방향 신호+URL 스트립, vitest 391** |
| **M22** | ⚖️ 분산 모드 + 시·도 균등 승격 ✅완료 — (A) 무지역 뽑기 (타입×17시·도) 조합 균등화(§5.5 개정) — 버킷은 **lDongRegnCd**(areaCode 쿼리는 항목 ~40% areacode 공백으로 카탈로그 손실 실측 → lDong 도달률 99.99%, 세종 36110·전남광주 12 병합 특례) + (B) 외지인 방문자수 √역가중 토글(기본 OFF, §6.9). `visitor_daily`(M17 적재분) 활용 개시 — 2계층 귀속 + **입도 중복 제거**(시 상위+구 하위 공존 16행, 경기 ×1.54 오염 봉쇄), 극단 71.1배→확률비 8.43배(세종 15.68%↔서울 1.86%). 후보 수 무변(⚖️ ON/OFF 797==797 실측). 신규 순수부 `lib/scatter`·`lib/ldong`·`buildDrawCombos`(TDD). 추가 API·키 불필요. **vitest 454·돌연변이 15종 사살** |
| **M23** | 🎰 정복 지도 룰렛 ✅완료 — 뽑기 중 홈 정복 지도의 시·군·구 조각이 룰렛처럼 점멸하다 **당첨 조각에 착지**(§7.12). 홈 정복 뷰를 17개 시·도 타일 → 250조각 SVG 지도로 교체(`ConquerSvg` 추출로 `/map`과 공유, `HomeConquerMap`을 `dynamic`으로 분리해 초기 청크 무변). 번쩍임은 amber 별도 레이어라 정복 상태 복원 불필요. 신규 순수부 `lib/roulette`(감속 단조성·`pickFlashCodes`, TDD). 착지 판정은 기존 `sigunguAt` 재사용. 추가 API·키 불필요(클라 렌더 0콜). **실측: 12→9→1조각 감속 후 무주군 착지 = 결과(전북 무주) 일치, 정복 조각 초록 유지, reduced-motion 시 회전 0·착지만 1.2s, 홈 정적 HTML에 지도 청크 참조 0건, vitest 467** |
| **M24** | 🎨 Genesis 리스킨 + 홈 지도·뽑기 통합 ✅완료 — 디자인 핸드오프 그대로 구현(§7.14). emerald/zinc → **Genesis indigo CSS 변수 토큰**(`@theme inline`으로 Tailwind 유틸리티화, 다크는 `dark:` 클래스가 아니라 변수 스왑) + `--g-on-primary` 신설(채워진 indigo 위 잉크가 테마별로 달라야 다크 대비 통과) + 그림자 전면 제거(border-first) + General Sans/DM Sans 셀프호스팅(`next/font/local`, 한글은 폴백이 렌더) + **이모지 → 선 아이콘 39종**(`components/icons.tsx`, 데이터의 이모지도 아이콘 키로 치환). 홈은 **2열 → `max-w-[720px]` 1열 통합 카드**(스탯 행·지도·릴·hairline·뽑기 영역) — `MapHero` 삭제, 뽑기 카피 "지도 굴리기". `ConquerSvg`는 JS 룰렛(`lib/roulette` 삭제) → **`phase`/`highlight` CSS 연출**(loading flick 스태거 / result 조각 dim + 펄스 마커 + 지역 칩), 마커 좌표는 신규 순수부 `ringsCentroid`(TDD). `/map` 🔭 CTA를 히어로 바로 아래로(`cta` prop 슬롯). 추가 API·키 불필요. **실측: 다크 토큰 스왑·잉크 대비 4.47:1·shadow 0건·렌더 이모지 0건·flick 28조각 delay 0~720ms·result 249조각 dim + 칩 "서울 강서구"·모바일 390px 가로 스크롤 0, vitest 478** |

| **M25** | 🐕 반려동물 동반 + ♿ 무장애 필터 ✅완료 — **대상 한정 축 토글 2종**(§6.11). 🐕 = `KorService2` 통합 `detailPetTour2`(활용신청 불필요·**9,972건**) 2~3콜 소경로 — 목록 1건 → `detailCommon2` 조인으로 카드+개요 동봉(개요 지연 로드 예외) · 1단계 전국 전용이라 지역·테마 잠금 + 무시된 조건 notice. ♿ = `KorWithService2`(활용신청 승인·쿼터 독립, 전국 **10,188건**) — `tourFetch` 엔드포인트를 `서비스/오퍼레이션` 형태로 받게 해 `drawByType`·`buildDrawCombos`·`pickItemFrom`·lDong 버킷을 **그대로** 재사용(URL 이 갈려 24h count 캐시 자동 분리) + `/api/overview?detail=with` 지연 편의시설 칩. 대상 축(🌊·🐕·♿) 상호배타를 `buildRandomQuery`(하나만 방출)·상태(`toggleTarget`)·라우트(**동시 도착 400**) 3겹으로 강제. 🐕 풀이 상업 항목에 편중돼(관광지성 0/12 실측) **관광지성 우선 소프트 편향**(`pickPreferred` · 예산 4 · 소진 시 마지막 후보 수용) + 정직한 카피로 대응 — **0/12 → 3/12**. 신규 순수부 `lib/petTour`(동반 칩·우선 판정·`pickPreferred`)·`lib/barrierFree`(편의시설 28종 라벨)·`lib/text`(`stripTags` 공용화)·`planCandidateCount` 대상 축(kind `pet` / source `with`)(TDD). 추가 키 불필요(같은 `TOUR_API_KEY`). **실측: ♿ 단독/♿+서울+관광지(count 190 == 뽑기 totalCount, 서울 살곶이 다리)/🐕 동반 칩·9,972/🐕+♿ 400/후보 수 with 5,074·pet 9,972 e2e 통과, vitest 501** |
| **M26** | 📈 임팩트 대시보드 🔜**설계 완료(2026-07-28, §7.15) — 구현 대기** — 공개 페이지 `/impact` 3패널: ① 방문 편중(외지인 `visitor_daily` 시·도 점유율) ② 뽑기 설계 확률(균등 1/17 + ⚖️ 가중 — 해석적 계산, "설계 확률" 라벨) ③ 실사용 실측 분포·반응 퍼널(제안→찜·길찾기→다녀옴). **§12.6 P1 승격 동반**: `travel_event` 테이블(익명·users FK 없음) + `POST /api/events` + `/privacy` 고지. 추가 API·키 불필요(DB·기존 적재분만, TourAPI 0콜) |

> **M10 로그인 관련 — §12 "로그인 불필요" 전제의 정제(2026-07-04)**: §12.5/§12.6의 "P0는 localStorage 전용·로그인 없음"은 **기본값으로 유지**된다(비로그인도 그대로 동작). M10은 그 위에 **선택적 로그인+동기화**(전략 A)를 얹은 것 — 로그인은 여전히 **강제 아님**. 명분은 개인정보가 아니라 **기기 간 동기화**(localStorage는 브라우저 하나에 갇힘). 로그인+서버저장은 오히려 개인정보처리자 의무를 새로 만들므로, 저장은 provider ID+표시이름+찜/방문 최소화. **v1 한계**: 삭제는 기기 간 전파 안 함(합집합 병합이라 한 기기에서 지운 항목이 다른 기기 재로그인 시 되살아날 수 있음).
>
> M6~M7이 배포 뒤인 이유: push마다 자동 재배포라 늦게 붙여도 부담이 없고, 기본 랜덤이 먼저 돌아가는 게 검증에 유리. 상수 기반의 쉬운 확장(M6)부터.
>
> **git 작업 방식(2026-07-03 확정)**: 구현 코드는 **마일스톤 단위 브랜치**(예: `m1-skeleton`)에서 작업 후 `main`으로 PR 머지. 설계 문서만 바뀌는 커밋은 기존 승인대로 main 직커밋 유지.

### 11.1 기능 백로그 (마일스톤 미배정)

| 후보 | 내용 | 메모 |
|---|---|---|
| ~~🏴 전국 정복률~~ | ~~"17개 시·도 중 n개 정복" 게이지~~ | ✅ **M12로 승격 완료** — 🧩 전국 정복 지도(§7.4). 게이지를 넘어 시·도 실제 윤곽 SVG를 채우는 시각화로 구현 |
| 🔗 결과 공유 URL | `/place/[contentId]` 페이지 + OG 이미지 — 붙여넣기 링크 언펄링용 | **카톡 피드 공유는 M13 선행 구현(§7.5)**. 남은 건 아무 데나 붙여도 미리보기가 뜨는 /place+OG — P1 유입 단계 |
| 🗂️ KML 내보내기 | 방문 목록 → 구글 My Maps 수동 임포트 | §7.3 — 수요 확인 후 |
| 🅿️ 주차장 중심 코스 | 주차장을 **앵커(출발점)**로 삼아 반경 내 도보 여행 코스를 추천 — "목적지부터 정하지 말고 주차부터 정하라"는 역방향 | **시장 조사(2026-07-06): 정확한 선행 서비스 국내·해외 0건**. 시장이 A) 목적지→주차장 찾기(모두의주차장·티맵주차)와 B) 목적지 중심 코스(카카오맵 AI·AI콕콕)로 양분 — "주차장 앵커 + 도보 코스 자동생성" 두 축 동시 충족은 미점유. 최근접: Radius Travel(반경 시각화·주차 미언급)·티맵 어디갈까(destination-first)·Park-Once 도시계획론(앱 아님). **M14 주변 뽑기(TourAPI 반경) 자산 재활용 가능**. MVP는 실시간 주차 대신 공공데이터포털 '전국주차장정보 표준데이터'(정적)로 시작. 리스크: 주차 데이터 해자·도보 코스 품질·콜드스타트·수요 가설 미검증 → PMF 선검증 |
| ~~📅 방문 시점 칩~~ | ~~오늘/내일/이번·다음 주말 칩으로 뽑기 기준일 선택 — 🍃 판정·🎪 축제가 그 날짜 기준으로 동작~~ | ✅ **M19로 승격(§6.8)** — "날짜 단독 = 후보 무변" 불변식 그대로 정식 설계 |
| ☔ 단기예보 기준일 연동 | `getVilageFcst`(+3일 강수확률 POP)로 ☔를 미래 기준일에서도 판정 | M19는 ☔를 오늘 전용 잠금(§6.8) — 예보 연동 시 해제 |
| 📅 주말 토·일 합집합 판정 | '주말' 칩이 토·일 이틀을 함께 판정(축제 OR·집중률 min/median 결합) | M19는 토요일 단일 기준일(라벨 "(토)" 명시, §6.8) — 판정 규칙 복잡도로 분리 |
| ~~🧭 반나절 코스~~ | ~~뽑힌 곳을 앵커로 볼거리→식사→마무리 슬롯을 반경 내 자동 구성 + 스텝 재뽑기~~ | ✅ **M20으로 승격(§7.10)** — 볼거리→식사→카페 3스텝·스텝 재뽑기 그대로 정식 설계 |
| 🧭 코스 저장·공유·스텝 연동 | rt.courses.v1 저장 + 코스 카톡 공유 + 스텝 다녀옴/찜 연동 + 지도 경로 표시 | M20 1단계 범위 밖(§7.10) — 코스 사용 수요 확인 후 |
| 🧭 코스 다슬롯·순서 최적화 | 볼거리 2+ 슬롯 + nearest-neighbor 방문 순서 재검토 | M20은 3슬롯 의미 고정 순서(§7.10 — 시간 서사 우선으로 NN 기각) |
| ~~🧭 코스 반경 확대(30km+)~~ | ~~원거리 코스 허용 — radius 30km 동작은 실측 확인(2026-07-10 스파이크)~~ | ❌ **폐기(2026-07-27)** — 위치기반 조회 공식 스펙 최대 20km(OT자료 실측 표, §14.2). 30km는 동작해도 스펙 외라 의존 금지 |
| 🔭 lDong 신체계 매핑 재생성 | 행정구역 개편 신체계 lDong 코드 매핑 — 개편 지역 sigunguCode 커버리지(절반 실측) 회복 | 2026-07-10 스파이크: lDong 파라미터는 동작하나 신·구 체계 혼재로 기각(§7.11) — 상류 안정화 후 |
| 🏷️ 구코드→신코드 전면 마이그레이션 | `areaCode`·`sigunguCode`·`cat3` → 법정동(`lDong*`)·신분류(`lclsSystm1~3`) 전환 — constants·candidateCount·conquer·congestionCodes·course 일괄 | **구코드 폐기 예고 공지가 뜨면 착수**(§14.3 — 26년 예정·확정일 미공지, 선례상 3개월 예고). 연계 정의서 xlsx 확보됨 |
| 🔭 date 배선·조건 존중·홈 수치 캡션 | 빈 곳 뽑기에 📅 기준일 UI 연동 + 지역·타입 조건 존중 + 홈 캡션에 N 표시 | M21 1단계는 오늘 고정·조건 무시·홈 무수치(§7.11) |
| 한적 TOP5 히어로 리스트 | 홈 히어로에 오늘 한적 예측 상위 시·군·구 리스트 | 공모전 화면 후보 — 수요 확인 후 |
| ⚖️ 기본 ON 재검토 | 분산 모드 기본값 반전(조건 모드 진입 시 기본 분산) | M22는 기본 OFF 확정(2026-07-10 사용자 결정, §6.9) — 사용 데이터 보고 |
| 🎉 정복 토스트 시·군·구 세분화 | 「○○ 정복! · 내 지도 +1」 토스트를 시·도(17) → 시·군·구(250) 단위로. 지금은 시·도 단위 그대로 동작 | M23 범위 밖(2026-07-28 사용자 결정: 나중에, §7.12). `sigunguAt`로 판정 가능하나 250개면 토스트 빈도·문구를 다시 정해야 함 |
| ~~🔐 OAuth 동의화면 게시~~ | 구글 OAuth 동의화면을 "테스트 → 프로덕션(게시)"로 전환 — 미등록 테스트 사용자 `403 access_denied` 근본 해결(아무 구글 계정 로그인 허용) | **2026-07-13 로그인 400번대 진단 결과**: 어제 에러 = 동의화면 테스트 모드 + 데스크톱에서 미등록 계정 선택 → `403`(코드 무관, Google Cloud Console 설정 영역). 스코프 `openid·email·profile` = **비민감**이라 구글 검증 없이 즉시 게시 가능. 미완(사용자가 "다음에"). learning-notes #8. ✅ **완료 확인(2026-07-27)** — 콘솔 실측: TravelAnywhere(travelanywhere-501410) 게시 상태가 이미 **프로덕션 단계**(외부)로 게시돼 있었음. 별도 조치 불요 — 공모전 테스트 계정 요건(§14.1)의 구글 로그인 측 선행 조건 충족 |
| ~~🔁 redirect proxy env 배선~~ | [PR #33](https://github.com/Goospel/RandomTravel/pull/33)(`redirectProxyUrl`) **머지 후** Vercel **Production·Preview 양쪽**에 `AUTH_REDIRECT_PROXY_URL=https://travelanywhere-kr.vercel.app/api/auth` 등록 + `AUTH_SECRET` 양쪽 동일 확인 | 코드만으론 미작동 — 프로덕션에 값 없으면 프록시 비활성(Auth.js 문서). 미설정 시 Preview 배포 로그인은 `redirect_uri_mismatch`(400) 여전. learning-notes #7. ✅ **완료(2026-07-27)** — 확인 결과 **7/13에 이미 Production·Preview 양쪽 등록돼 있었음**(백로그가 낡음). 값이 Sensitive 타입이라 판독 불가 → 문서 확정값으로 `vercel env add --force` 덮어써 확정(같으면 무해·다르면 교정). `AUTH_SECRET`은 단일 항목이 양쪽 커버라 동일 보장. env 반영은 다음 배포부터 |

## 12. 수익 모델 (장기 구상) — 여행 행동 데이터 사업화

> ⚠️ **판매·서버 수집은 P0(MVP)에 반영하지 않는다.** 사용자 규모가 생긴 뒤를 전제로 한 확장 설계. 단, 이벤트 스키마(§12.5)는 **P0부터 localStorage에만 기록**해 미리 검증한다(§12.6 P0).

### 12.1 컨셉

사용자가 **뽑고 → 반응하고(찜/다시 뽑기) → 실제로 다녀오는** 과정에서 쌓이는 행동 데이터를 **익명 통계·트렌드 리포트**로 가공해 관광 업계(지자체·여행사 등)에 판매한다. 통신사·카드사가 지역 관광 소비 데이터를 지자체에 파는 것과 같은 구조의 축소판.

**이 데이터만의 차별점**: 일반 여행 플랫폼 데이터는 "검색·예약"이라는 *계획된 의도*지만, 이 서비스의 데이터는 **"무작위로 던졌을 때 사람들이 무엇을 수락하고 무엇을 거절하는가"** — 편향이 적은 순수 선호도 신호에 가깝다.

### 12.2 무엇이 데이터 자산이 되나

| 데이터 | 내용 | 팔리는 이유 |
|---|---|---|
| 뽑기 조건 로그 | 어떤 지역·테마 조합이 선택되는가 | 여행 수요 **선행지표** |
| 결과 반응 | 다시 뽑기(거절) vs 찜·길찾기(수락) | 여행지 단위 선호도·매력도 점수 |
| 방문 인증 (확장 기능) | 추천 후 실제 방문 여부 | 추천→방문 **전환율**, 실수요 증명 |
| 시간 패턴 | 계절·요일·시간대별 뽑기 경향 | 수요 예측, 캠페인 타이밍 |

### 12.3 잠재 구매처

| 구매처 | 사고 싶어할 것 |
|---|---|
| 지자체·지역관광공사 | 우리 지역 관심도·거절률, 어떤 테마로 소비되는지 |
| 여행사·OTA·숙박 플랫폼 | 뜨는 지역 조기 감지, 상품 기획 근거 |
| 상권분석·컨설팅 업체 | 관광 수요 보조 데이터 |
| 광고주 | (데이터 직판 대신) 지역·테마 타겟 광고 지면 |

### 12.4 ⚖️ 법적 전제 — 이 모델의 성립 조건

개인을 식별할 수 있는 데이터를 동의 없이 제3자에게 판매하는 것은 **개인정보보호법 위반**이다. 합법적으로 성립하는 형태는 셋:

| 형태 | 판매 | 비고 |
|---|---|---|
| **익명정보 (집계 통계)** | ✅ 자유 | "6월엔 제주×맛집 조합이 가장 많이 뽑혔다" 수준의 집계. **← 이 프로젝트가 채택하는 형태** |
| 가명정보 | ⚠️ 제한적 | 데이터 3법 이후 통계·연구 목적 활용 가능하나 절차 요건 있음 |
| 동의 기반 제3자 제공 | ⚠️ 가능은 함 | 명시적 동의 필요 — 사용자 거부감 크고 동의율 낮음 |

- **개인 단위 raw 데이터는 팔지 않는다.** 상품은 항상 집계·통계 형태로만.
- 방문 인증에 GPS 자동 수집을 쓰면 **위치정보법**(위치정보사업 신고 등)이 추가로 걸린다 → 방문 인증은 **사용자 수동 체크** 방식으로 설계해 회피.
- 이벤트 수집을 시작하는 시점부터 **개인정보처리방침 고지**가 필요하다.

### 12.5 아키텍처 영향 — 현행 설계와의 충돌

현행 "DB 없음·로그인 없음·localStorage" 전제를 데이터 사업화가 깬다. 다만 **로그인은 계속 없어도 된다** — 익명 세션 ID로 충분.

| 현행 (P0) | 데이터 사업화 시 필요 |
|---|---|
| localStorage에만 기록 | 서버 이벤트 수집 API + DB |
| 계정 없음 | 계정 불필요 유지 — 익명 세션 ID(UUID)면 충분 |
| 개인정보 미수집 | 식별자 최소화로 익명 유지 + 처리방침 고지 |

이벤트 스키마 초안:

```jsonc
{
  "event": "draw | redraw | like | navigate | visited",
  "mode": "pure | filtered",   // draw·redraw 에만 유효. like·navigate·visited 는 뽑기 시점 모드를 알 수 없어 null (M4 구현 확정)
  "areaCode": 39,
  "contentTypeId": 39,
  "contentId": "뽑힌 여행지 ID",
  "sessionId": "익명 UUID — 기기·개인 식별 불가",
  "ts": "발생 시각"
}
```

### 12.6 단계 로드맵

| 단계 | 내용 | 착수 조건 |
|---|---|---|
| **P0** | 수익화 없음. 단 §12.5 스키마로 이벤트를 **localStorage에만 기록** — 서버 전송 없음 = 법적 이슈·처리방침 불필요. P1 전환 시 "내 기록 업로드" 옵션 + 스키마는 이미 실전 검증된 상태 | 지금 (2026-07-03 확정) |
| **P1** | 익명 이벤트 로깅 추가 (위 스키마) | 배포하고 남에게 공유하기 시작할 때 |
| **P2** | 월간 트렌드 리포트 자동 생성 — 판매 샘플이자 홍보 콘텐츠 | 이벤트 수천 건 누적 |
| **P3** | 구매처 접촉·데이터 상품화 | MAU 수천 이상 + 사업자등록·법률 검토 |

### 12.7 현실 체크

- **데이터는 규모가 있어야 상품이 된다.** 혼자 쓰는 단계에선 가치가 0이므로 P0에 서버 수집은 넣지 않는다. 대신 로컬 기록(§12.6 P0)으로 스키마를 미리 돌려두고, P1 로깅을 일찍 켤수록 누적 히스토리 자체가 자산이 된다.
- 같은 노력 대비 진입장벽이 낮은 대안 수익원: 숙박·액티비티 **제휴 링크(어필리에이트)**, 광고. 데이터 판매는 이들보다 단가는 높지만 훨씬 뒤 단계의 이야기다.

## 13. 미결정 / 추후 검토

- **이미지 없는 여행지 처리** — ✅ **M1: 플레이스홀더(🏞️) 표시** 채택(arrange 미지정, 이미지 없어도 카드 정상). ✅ **M3 재검토 완료**: `arrange=O`(대표이미지만 필터) **미채택**(무이미지 항목도 정보 가치 있어 배제 안 함) — 대신 `<img onError>` 로 **깨진 이미지도 플레이스홀더로 폴백**(URL은 있으나 로드 실패하는 경우 방어). `components/ResultCard.tsx`.
- **순수 랜덤 타입 범위** — ✅ **M1: `RANDOM_DEFAULT_TYPES`=[12·14·15·25·28]**(숙박32·쇼핑38·음식39 제외) 잠정 채택. 사용 로그 보고 조정.
- **뽑기 애니메이션 수준** — ✅ **M3: 슬롯머신 채택**. 지역·테마 **2릴**을 CSS만으로 무한 회전(`components/SlotMachine.tsx` + `globals.css` `slot-spin`), 결과는 `card-reveal` 로 페이드+상승 등장. ⚠️ **추가 API 호출 0**(§5.6): 릴에 도는 이름은 로컬 상수(`AREA_CODES`·`CONTENT_TYPES`)일 뿐 실제 뽑기는 서버 1건. `prefers-reduced-motion` 시 애니메이션 off.
- **결과 카드 지도 링크** — ✅ **M3: 카카오맵 좌표 딥링크 채택**(§7.2). `lib/mapLink.ts`(순수·TDD): 좌표 있으면 `link/map`·`link/to`(길찾기), 없으면 이름으로 `link/search` 폴백. M1~M2 임시 구글맵 링크에서 전환. 카카오 장소ID 매칭은 백로그 유지.
- **배포 함수 리전** — ✅ **M5: 서울(`icn1`) 고정** (`vercel.json` `regions:["icn1"]`). 기본 `iad1`(US)에선 뽑기 1건당 한국 TourAPI를 3회 **순차**(개수→항목→개요, 의존 체인이라 병렬 불가) 왕복해 1.4~4.2s 소요 → 함수 실행 리전을 서울로 옮겨 단축. ⚠️ Hobby는 **단일 리전만**(배열에 2개↑면 빌드 전 배포 실패). ⚠️ Next.js `preferredRegion='icn1'`은 **함정**(Node 런타임 무시, Vercel Edge는 `auto|global|home`만) — 반드시 `vercel.json`으로. 검증: 응답 `x-vercel-id` compute 세그먼트가 `icn1`.
- **🌊 바다 + 🦀 제철** — ✅ **M6 구현 완료**(상수 기반, 추가 API·키 0). **🌊 바다**: `areaBasedList2` 에 바다 소분류 `cat3` 4종(해안절경·해수욕장·섬·항구, `lib/constants.ts` `SEA_CAT3`)을 얹어 대상을 바다로 한정 — 타입은 관광지(12) 고정. 4종의 `totalCount` **합산 가중**으로 분류 선택(`weightedIndex`, 개수 적은 분류 쏠림 방지, §6.3). **🦀 제철**: `lib/constants.ts` `SEASONAL_CALENDAR`(품목→월·주산지, 22종 12개월 커버) + `lib/season.ts`(순수·TDD) 로 이번 달 제철 산지와 지역 풀 **교집합**, 결과 카드에 "지금 제철 🧄마늘·🐚전복" 배지(§6.4). 둘 다 조합 가능(강원 바다+강원 제철). 빈 풀(제철∩지역=0)은 `EMPTY_POOL`(404)+안내. `drawRandom` 을 `drawByType`/`drawSeaside` 로 분기. 파라미터 `seaside=1`·`seasonal=1`(`lib/query.ts` `parseBool`).
- **🎪 축제** — ✅ **M7 구현 완료**(추가 API·키 0 — `searchFestival2`가 기존 TourAPI 키 그대로). `lib/festival.ts`(순수·TDD) + `getFestivalMap`(1h 캐시)로 오늘 진행 중 축제 지역과 지역 풀 **교집합**, 결과 카드에 "🎪 (축제명) 외 N" 배지(§6.2). 실측(2026-07) 2대 발견: ① `eventStartDate=오늘`은 '진행 중'이 아니라 '아직 안 끝난(진행중+예정)'을 줘서 `시작≤오늘≤종료` **클라이언트 필터** 필수. ② 응답 `areacode` 거의 비고 `areaCode` 필터도 0 → **`lDongRegnCd`(법정동 시도 코드)를 `LDONG_TO_AREA`로 TourAPI areaCode 변환**(강원 42→51·전북 45→52 특별자치도 신·구 공존, 오염코드 12는 매핑 없어 자동 제외). `drawRandom` 파이프라인에 축제 교집합 단계 추가(축제→제철 순), 배지는 `BadgeCtx`로 통합. 파라미터 `festivalOnly=1`. **§6.5 소스 장애 처리**: `getFestivalMap` 실패 시 `drawRandom`이 try/catch로 잡아 **축제 필터만 건너뛰고 결과는 살리며** `picked.notice`로 "축제 조건 없이 뽑았어요" 안내(바다·제철은 로컬 상수라 원격 축제 API 하나로 전체 실패 방지). `getFestivalMap`은 `arrange="A"`(제목순)라 한 페이지 절단이 진행중 축제를 무성 누락할 수 있어 **totalCount 만큼 페이지네이션**(100/page, 상한 1500).
- **🗺️ 내 여행 지도** — ✅ **M8 구현 완료**(§7.1). `app/map/page.tsx` + `components/VisitedMap.tsx`가 다녀온 곳(localStorage `rt.visited.v1`, 좌표 포함)을 **카카오맵 JS SDK 임베드**에 마커로 표시하고 `setBounds`로 전부 보이게 자동 맞춤. 마커 클릭 시 인포윈도우(장소명, `textContent`로 XSS 방지). `hooks/useKakaoLoader.ts`가 SDK를 `autoload=false`로 1회 로드(`kakao.maps.load`) — 상태 loading/ready/error/no-key. 순수 로직(`lib/mapView.ts`: `visitedWithCoords`·`boundsOf`·`kakaoSdkUrl`)은 TDD. 키는 `NEXT_PUBLIC_KAKAO_MAP_KEY`(브라우저 노출 전제, 도메인 등록으로 보호). ⚠️ 카카오 콘솔에서 **[카카오맵] 제품 사용 설정 ON + JavaScript SDK 도메인 등록**(운영 URL·localhost)이 없으면 지도 로드 실패 → error 상태로 안내. 메인 헤더에 `/map` 링크. 타입은 `types/kakao.d.ts`에 쓰는 부분만 최소 선언.
- **📱 PWA** — ✅ **M9 구현 완료**(추가 키 0). `app/manifest.ts`(Next가 `/manifest.webmanifest` 자동 서빙 + `<link rel=manifest>` 주입), 아이콘은 **파일 기반 자동 감지**(`app/icon.png` 파비콘·`app/apple-icon.png` 애플터치 — ⚠️ `metadata.icons`를 명시하면 이 자동 감지가 무시되므로 지정하지 않음), manifest 아이콘은 `public/icon-192·512.png`(주사위 🎲, sharp로 SVG→PNG 생성, 512는 maskable 겸용). `themeColor`는 Next 15+에서 `viewport` export로 분리(라이트 인디고/다크). `public/sw.js` 최소 서비스워커(네비 네트워크 우선→오프라인 셸 폴백, 정적 캐시 우선, `/api/*`는 캐시 금지) + `ServiceWorkerRegister`(프로덕션에서만 등록 — dev HMR 충돌 방지).
- **TourAPI 버전** — ✅ **KorService2 확정**(§5). 오퍼레이션 접미사 `2`, `detailCommon2`는 `contentId`만.
- **빈 결과 대응** — ✅ **M1: `EMPTY_POOL`(404)** + 인덱스/조합 재추첨 구현. UI 완화 제안(조건 끄기 등)은 M2 조건 UI와 함께.
- **`searchFestival` 날짜 파라미터 의미 검증**: ✅ **M7 확정** — `eventStartDate=오늘` 단독은 '진행 중'이 아니라 '해당일 이후 안 끝난(진행중+예정)'을 반환. "오늘 진행 중"은 `시작일 ≤ 오늘 ≤ 종료일` 클라이언트 필터로 추출. `areacode` 미신뢰 → `lDongRegnCd` 변환(위 M7 항목).
- **시·도 대표 격자좌표(nx, ny) 확정**: 기상청 격자 변환표에서 17개 지점 추출
- **"비 안 옴" 판정 범위**: PTY=0만 인정할지, 눈(3·7)을 어떻게 볼지 (겨울 여행 관점)
- **🌊 바다 분류코드 보완**: 등대 등 누락 cat3를 `categoryCode` 조회로 확정, KorService2 신분류체계(lclsSystm) 전환 검토
- **🦀 제철 달력 검수**: ⚠️ **M6 1차 편성 완료**(`SEASONAL_CALENDAR` 21종, 널리 알려진 특산물·산지 위주로 12개월 커버 — 연중 상비 향신료 마늘 제외). **잔여**: 품목별 월·주산지를 공신력 있는 자료로 교차 검증(현재는 상식 기반) — 사실로 카드에 노출되므로 오류 시 정정.
- **🌊 조건과 테마 충돌 정책**: ✅ **M6 결정**: 바다 ON이면 테마 칩을 **비활성화(disabled)** 하고 "🌊 바다를 켜면 테마는 관광지로 고정돼요" 안내 노출. 서버도 seaside 시 타입 12 고정.
- **🗺️ 카카오 장소ID 매칭**: TourAPI 좌표→카카오 장소ID 변환(카카오 로컬 API 검색) 가능 여부 — 안 되면 좌표 기반 링크로 충분한지 (§7.2)
- **🗺️ KML 내보내기 착수 여부**: 수요 확인 후 결정 (§7.3)
- **📣 M18 카피 문안(배너·슬롯 로테이션·공유 `DEFAULT_DESC`)** — 히어로 서브카피는 ✅ 확정("어디 갈지 고민될 때, 운명에 맡겨 — 전국 어디든 같은 출발선.", 2026-07-10 사용자 선택, §7.9). 나머지 문구는 §7.9 카피 원칙 5 안에서 구현 PR에서 확정.
- **순수 랜덤의 "균등" 정의** — ~~M1 잠정: 타입 균등~~ → ✅ **M22 확정(§6.9A): 지역 균등 채택** — 무지역 뽑기를 (타입×17시·도) 조합 균등으로 승격(2026-07-10 사용자 결정). 배포 카피(§7.9)·⚖️ OFF 의미와 정합.

## 14. 공모전 대응 — 2026 관광데이터 활용 공모전 웹·앱 구현 부문 (지정과제 2번 오버투어리즘)

> 2026-07-27 예비심사 **합격** 통보(운영사무국 메일). 공지 단일 출처 = **공식 노션 페이지**(사무국이 "모든 변동·공지는 이 페이지로" 명시) — 마일스톤 완료 시점마다 확인. 배포 자료 원본은 레포 밖 `공모전_문서/`(gitignore).

### 14.1 확정 일정 (2026)

| 일정 | 내용 |
|---|---|
| 07-27 ~ 09-21 | **서비스 개발 기간** (남은 기간 8주) |
| **09-21(월) 16:00 정각** | **1차 심사 서류 제출 마감**(한국관광콘텐츠랩 접수 페이지, 세부 가이드·양식 추후 공지) — 시간 엄수, 미준수 책임 지원자 |
| 10월 중 | 1차 심사(기능심사) — 개발 완료된 **완성 서비스**만 대상 |
| 10-21(수) | 1차 합격자 발표(상위 3팀 개별 안내) |
| 10-28(수) | 최종 발표심사(웹·앱 개발 부문과 통합, **PPT 발표자료 필수**) |
| 11-05(목) | 시상식 |

- 1차 배점: 구현성 30 · 기획력 30 · **데이터 활용 적절성 20**(공사 OpenAPI 필수) · 발전성 20. 최종: 적정성 30 · 완성도 30 · 실용성 25 · 발표 15.
- 1차 제출물: 팀·지정과제(번호+**해시태그**)·서비스 정보 / **서비스 URL**(웹은 URL만 — 앱스토어 절차 불필요) / **테스트 계정**(로그인 필수 기능 있어 제출 필요 — 동기화 데모 계정 준비) / **활용 API명+신청정보(신청자명·인증키)** / 기능설명서(양식 추후 공지).
- 과제 결합 허용: 심사 기준은 선택한 2번(오버투어리즘)이되, **1번(실시간 변수 대응 — 날씨·혼잡·동선)** 요소(☔·🍃·🧭 코스)를 부가 해결로 기능설명서에 서술 가능.

### 14.2 규정 대응 방침 (2026-07-27 확정)

- **공사 지칭 금지** — "개발 서비스 내 '한국관광공사'·'KTO' 단어/로고 사용 금지". UI 노출 문자열에서 제거(FilterPanel 🍃 설명 "관광지 집중률 예측 기반"으로 수정). 코드 주석·레포 문서는 서비스 화면이 아니라 대상 아님. 이후 신규 UI 카피에도 동일 적용.
  - ⚠️ **예외 — 푸터 데이터 출처 표시(2026-07-28)**: TourAPI **운영계정 전환 조건**으로 공사가 사이트 내 OpenAPI 출처 표시를 요구(유선 통보, 지정 문안 없음) → `app/layout.tsx` 전 페이지 공통 푸터에 "관광 정보 한국관광공사 TourAPI · 날씨 기상청 단기예보 조회서비스" + 무관계 고지 1줄. **금지 규정과 문자상 충돌하나** 규정 표적은 브랜드 사칭·후원 오인이고 이쪽은 데이터 이용 조건이라 성격이 다르다고 판단 — **로고 없이 텍스트만** + "데이터 제공기관과 무관합니다" 병기로 양쪽 취지를 함께 충족시킨다. 잔여 리스크는 사무국 확인으로 해소 예정(`gongmo@stunning.kr`). 출처 표시 외 UI 카피에는 금지 규정 그대로 적용.
- **로컬 DB(M17 집중률 배치) — 문의·신청서 없이 기능설명서 명시로 갈음**. 근거: ① 규칙의 실제 표적은 **파일 다운로드로 API를 우회한 팀**(OT 영상 — 과거 실사례. "맞춤형 데이터 다운로드는 OpenAPI 활용 불인정"이 같은 취지의 명문) ② 우리는 관광 콘텐츠 전량 실시간 호출 + 집중률 **수치**만 일 1회 API 배치(파일 아님)라 호출 이력 검증을 통과 ③ 캐싱은 구조상 필수(전국 252 시군구 백분위 산출 — 뽑기당 252콜 불가) + 일 단위 예측값이라 실시간 이득 0. **기능설명서에 이 아키텍처(실시간 호출 범위 / 배치 범위·사유 / 크론 호출 로그 존재)를 명시**한다. 1차 제출 양식에 관련 항목이 있으면 그때 「TourAPI 로컬서버 저장 신청서」(OpenAPI 설명회 자료 p.33 양식) 첨부 재검토.
- **위치기반서비스사업자 신고 — 해당 없음**: 기기 GPS(geolocation) 미사용(코드 전수 0건). `near=`는 뽑힌 관광지·저장 장소의 공개 좌표.
- **위치기반 조회 공식 스펙 최대 20km** — M14(20km)·M20(5→10→20km)은 규격 내. 30km 실측 동작(§7.10 스파이크)은 스펙 외이므로 확대 백로그는 사실상 폐기(§11.1 메모).
- **비용 지원 없음**(프리티어 권장 — 현행 Vercel Hobby·Neon 무료로 충족). AI 코딩 도구 제한 없음. 서비스 소유권 참가자.

### 14.3 리스크 워치

- **구코드 폐기(26년 예정, 확정일 미공지)** — TourAPI 공지 207(2025-05-14): 구지역코드(`areaCode2`)·구분류코드(`categoryCode2`) 정보 "추후 삭제 예정(26년)". 우리는 폐기 대상 **오퍼레이션 미호출**(코드표 상수 내장)이고 노출은 목록 조회 파라미터(`areaCode`·`sigunguCode`·`cat3`)뿐. KorService1→2 전환 선례상 **3개월 예고 공지 후 컷오프** — 2026-07-27 현재 예고 없음 = 심사 기간 내 차단 가능성 낮음. 대응: 공지 페이지 주기 확인 + 예고 시 §11.1 마이그레이션 착수(연계 정의서 xlsx 확보됨 — 공지 97, 2026-01-12 첨부).
- **법정동 개편(공지 2026-06-29: 인천·전남광주통합특별시 변경, 07-01 시행)** — 광주29·전남46 구코드 전면 폐지 → 신코드 12(+하위 시군구), 인천 3개 구 신설(제물포·영종·검단). 실측(07-27): 구 areaCode 파라미터(5·38)는 정상, 콘텐츠 주소·법정동 필드는 신체계로 전환 완료, 🍃 집중률 셀(장성·광산·강화)도 정상. `congestionCodes`(구 법정동 기반)는 집중률 API가 구코드를 유지하는 동안 유효 — **일일 크론 로그·252셀 적재 수가 조기 경보**(개편 반영 시 셀 실패로 드러남).
- **🎪 축제 코드 12 누락 버그(07-27 발견·즉시 수정)** — `LDONG_TO_AREA`에 신코드 12 미매핑 + `areacode` 폴백 빈 값으로 **전남·광주 축제가 🎪 필터에서 전량 탈락**하고 있었다(실측: 진행 중 50건 중 12번 7건, 폴백 가용 0건). M7 당시 "오염코드 12 자동 제외"로 봤던 것이 실은 개편 신코드의 조기 등장이었음. 수정: 통합시가 areaCode 5·38에 걸쳐 시도 단위 매핑 불가 → `lDongSignguCd`로 판별(광주권 5개 구 210·240·270·300·330 = 5, 그 외 22 시·군 = 38 — ldongCode2 실측 27개 전수). **교훈: 상류 개편 공지(6/29 류)는 "동작하던 매핑의 무성 탈락"으로 나타난다 — 축제처럼 폴백이 조용히 null을 먹는 경로는 개편 시 분포 실측으로 재검증.**

### 14.4 리소스

- 공식 노션(공지 단일 출처): `https://lowly-polyanthus-1fb.notion.site/2026-3a75dce406e38034a9a8d058a1b55596`
- OT 영상: `https://youtu.be/fKdxT1w_X1o` · OT 자료+OpenAPI 가이드 드라이브: `https://drive.google.com/drive/folders/1Lyyl3k3y4IIdnQZRjq2ALSWrNtgfeEPW`
- TourAPI 공지사항: `https://api.visitkorea.or.kr/#/cmsNotice` (공지 207 = 구코드 폐기 예고 / 97 = 신분류 연계 정의서)
- 문의: 사무국 `gongmo@stunning.kr`(070-4060-4046, 평일 10~17시) · OpenAPI `tourapi@knto.or.kr`(070-4287-3219, 평일 9~18시)

### 14.5 심사 대비 백로그 (2026-07-28 비판적 자가평가 기반)

> 배점 축별 자가평가: **데이터 활용 20 = 최상위권**(TourAPI 오퍼레이션 8종+·집중률 2종·기상청 전량 실시간 호출), **구현성·완성도 = 강함**(프로덕션 배포·동기화·테스트 500+). 반면 **기획력·적정성**은 "랜덤 추천이 실제 분산을 만든다"는 행동 근거가 없고(사용자가 재뽑기로 유명지를 고르면 효과 0), 오버투어리즘 대응 기능(🍃·🔭·⚖️)이 기본 OFF 토글로 숨어 심사 화면에서 안 보이며, **발전성**은 확장 서사가 문서에 없다. 남은 기간은 코드보다 **논증 보강**이 점수 대비 효율이 높다 — 아래 3건이 그 대응. 채택 시 마일스톤으로 승격.

| # | 항목 | 내용 | 방어하는 반론 / 배점 축 |
|---|---|---|---|
| 1 | ~~**📈 임팩트 대시보드(분산 효과 논증)**~~ | ✅ **M26 승격(2026-07-28, §7.15)** — 3패널(방문 편중 / 설계 확률 / 실측·퍼널) + §12.6 P1 서버 수집 동반. 초안의 "1,000회 시뮬레이션 승격"은 **해석적 확률 계산으로 개정**(w/Σw 정확값 — 표본 노이즈 0, 스냅샷은 테스트로 유지) | "추천을 따라간다는 근거가 있나?" — **기획력 30·적정성 30**의 최대 반론에 대한 유일한 정량 방어 |
| 2 | **🍃 오버투어리즘 정면 노출** | ① 한적 TOP5 히어로 리스트(§6.6 백로그 기존 행 — 공모전 화면 후보) 승격 ② 문제 정의 화면(집중률 데이터로 상위 지역 편중 시각화 → 우리 해법 서사) ③ 심사 시연은 🍃·⚖️ ON 상태로(§6.9 결정 — 기본값은 OFF 유지, 시연만 ON) | "오버투어리즘 대응이 어디 있나?" — 핵심 기능이 기본 OFF 토글 뒤에 숨는 문제. **적정성 30·실용성 25** |
| 3 | **📄 발전성 한 장** | 지자체 한적 관광지 홍보 채널(B2G)·집중률 데이터 피드백 루프·§12 여행 행동 데이터 사업화를 기능설명서·발표 PPT 한 장으로 정리. 코드 아님 — 문서·발표 산출물 | "다음이 있나?" — **발전성 20·발표 15**. 현재 plan에 확장 서사 부재 |

- 추가 방어 논리(발표용): "랜덤"이 아니라 **조건 랜덤**(날씨·한적·시점·대상 한정) — 의사결정 피로 해소 + 분산 유도를 함께 하는 설계임을 우리가 먼저 프레이밍(니치 타겟 반론 선제 대응).
- 경쟁 구도 유의: 1차 통과 상위 **3팀** — "잘 만든 서비스"만으로 부족, 배점 축마다 명시적 득점 필요.
